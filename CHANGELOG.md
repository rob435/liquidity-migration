# Changelog

The dated operational log: deploys, incidents, repairs, and change points,
newest first. One entry per change or per first report of a fault, updated in
place when the same matter moves on; a refused deploy, a re-fire of a known
incident, or a check that changed nothing gets no entry. Current truth lives
in [STATE.md](STATE.md); when something happens, add the dated entry here and
edit STATE.md to match.

Older history: Retained in git history (pre-September 6).

- **2026-09-12 — `AUDIT.md` worked through on `ee789cd`, then its twenty owner decisions taken up on the owner's approval: 50 findings fixed and 1 in part, 15 refuted from source, 14 declined with reasons, none left open. A cover book that never cleared on a large contract count is the one real defect the second pass found; nine of the twenty did not survive reading the source.**
  - Source: the source-first repository ledger `AUDIT.md` (LM-001–LM-080), written by a connector session that could read the tree but run nothing. Every verdict here is from source read on this branch plus a run of the gate; the ledger's own claims were re-derived, not taken on its word. `AUDIT.md` §Resolution carries one row per finding, and §Open questions now answers the three the repository itself settles. Gate on this branch: 2,711 Rust tests passed, 0 failed, 9 ignored; 1,986 Python passed, 1 skipped; Ruff, mypy, ShellCheck over every tracked shell file, rustfmt and strict Clippy clean; `repo_doctor` reads `ready`. The seven baseline failures on this box were three missing tools (rsync, ssh-keygen, GNU time), not repository faults; with those installed the box is clean. No decision, quantity, WAL record or venue behaviour of the funded engine changes. No deploy, no credential, no live order, no arming change.
  - **Refuted in the first pass, six.** `configs/long_native_v11a.json` is live: `rules/long_native.py` builds its path with an f-string over `("v11a", "v12")`, `_registered_long_profiles()` checks its strategy identity at import, and deleting it breaks LONG (LM-051). `.github/workflows/vps-deploy.yml` *is* the repository-local CI: on every push to `main` and every pull request it runs `scripts/dev.sh lint`, `shellcheck`, `types`, `test` plus `cargo fmt --check`, strict Clippy and `cargo test --workspace --all-targets --locked`; the file's name describes its other job (LM-068). Generated realm-field drift is already a test failure, not a hazard (LM-034). The 0.5 ms hold in the flock stress test widens the window a broken lock is *caught* in; the assertion is `overlaps == 0`, which scheduling cannot make fail (LM-031). The recorder's timing tests wait on events with 5 s ceilings that never fire: 117 tests, 4.18 s measured (LM-030). The import-integrity suite is 0.93 s for three siblings, and merging the children would destroy the property under test (LM-029).
  - **Tape reader, `market_tape/load.py`.** A source's venue comes from `status.json`, the source's name, or the caller, and a source that says none of those is refused: an unreadable or malformed status file used to make every row read as Bybit (LM-001, LM-075). An rclone archive lands as `<name>.partial`, is checked against the size the listing carries, is opened as a tar and fsynced before the rename, so an interrupted copy can no longer become a durable cache entry; a cached archive whose size no longer matches the listing is fetched again (LM-004). `lsjson` and `copyto` carry deadlines and failures are typed (`RemoteTimeout`, `RemoteCommandError`, `CacheError`, all `SourceError`) (LM-006, LM-012). A source read that fails mid-stream is the source's error: the feeder records it and the reader raises after checking zstd's exit status, because a torn tar gives zstd a clean EOF on a frame boundary (LM-008). `iter_rows`/`iter_snapshots` take `strict=True` and raise `TapeRowError` naming the member and line; the lax path still counts and now logs once per member with its first cause (LM-010, LM-011), exposed as `market_tape rows|bars|book --strict` and `--venue`. Source detection needs a recorder file or a real hour/segment directory (LM-002); an hour is listed from its symbol directories and a finished hour is remembered (LM-003); tar member lists are cached by (path, size, mtime) (LM-013); the cache key carries 128 bits of digest (LM-005).
  - **Tape storage and recorder.** The compressor counts failures, keeps the last error with its stamp, publishes `compressor.{pending,compressed,failed,last_error,alive}` in `status.json` and the log line, and goes on to the next segment; a failed segment stays raw for the next start's recovery (LM-015 in part, LM-016). Each zstd call has a 600 s deadline and is killed on it; `close()` has a bounded join that reports how many segments it left raw (LM-017). Compressed bytes are hashed as zstd produces them: one write, one verification read, no third pass (LM-018). Startup recovery is one `os.walk` instead of three `rglob` passes over the whole tape (LM-024). `atomic_json` uses `mkstemp`, so two writers cannot share a temporary (LM-025). Retention counts and names the files it could not stat (LM-021) and directory cleanup reports anything that is not `ENOTEMPTY`/`ENOENT` (LM-022). The recovery point objective is now stated where it is set and where it is read — `StorageSettings`, the storage module, `market_tape/README.md`, `--fsync-every-records` help: a power loss takes at most `fsync_every_records - 1` acknowledged rows from each symbol's open segment (LM-039). `StorageSettings` is the one declaration of the storage defaults; the TOML loader and `capture_bybit_forward.py` both read them from it, with a test pinning every CLI default to its field, and that CLI's dead `sticky_days=2` (rejected by `Universe`) became `sticky_hours=48.0` (LM-074).
  - **Engine.** `heartbeat/legacy.rs`, a second complete heartbeat renderer imported only by the test that compared production output to it, is deleted; three field-level contract tests replace the byte-equality check — escaping, number spelling with the non-finite-to-null rule, and the account-reading stamp (LM-026, LM-063). MEXC contract parsing: a leverage the venue left out is `None`, one it sent malformed now refuses the whole contract row instead of reading as absent and defaulting `max_leverage` to 1.0 (LM-077). The execution-study test asserts the shipped config's policy inventory equals `Policy::ALL` rather than `policies.len() == 8` (LM-049, LM-064).
  - **Ops and the data layer.** `scripts/dev.sh check` lifts an explicit `--basetemp` out of the arguments and resolves it through the same containment check as the default, so a path inside the repository is refused before the first gate whichever way it is given (LM-037). `durable_create` never deletes an artifact it has already published: a directory that will not sync raises `ArtifactDurabilityError` saying the name is not proven durable, where it used to unlink the artifact and swallow the second failure (LM-058). Telegram control polling uses capped exponential backoff with jitter, reset by a success, instead of a fixed 5 s (LM-052). The Binance client has a 300 s retry budget across every attempt: nine attempts each waiting a capped `Retry-After` used to be eleven minutes (LM-053). Fleet liveness makes one bounded `systemctl is-active` call and reads `unknown` for every unit when it does not line up, instead of falling back to one subprocess per unit exactly when systemd is struggling (LM-060). Every external command the storage reclaimer runs has a 600 s deadline; a hang is exit 124 and a failed step, not a stalled reclaimer (LM-071). The archive downloader's `curl` backend and its `LIQMIG_ARCHIVE_DOWNLOAD_BACKEND` switch are deleted: one HTTP stack, one timeout and retry semantics (LM-054, LM-072). A retired dataset generation that will not delete is named rather than `ignore_errors=True` (LM-056). `_DISK_FORECAST_SEC` is pinned by test to `OnUnitActiveSec + AccuracySec` read from the host-liveness timer (LM-062 in part). New: `tests/repo/test_dependency_lock.py` fails on a pinned package owned by nothing, a declared dependency not pinned, or a duplicate pin (LM-067); `rename_noreplace` gains the tests its fail-closed contract never had (LM-059); the stand-in rclone covers interrupted copy, stale cache, hang, non-zero exit and missing binary (LM-065).
  - **Declined, fourteen, each with its reason in `AUDIT.md` §Resolution.** Mostly: the work the finding calls wasteful is what a correct answer costs (`hours()` must list the whole remote; the merge needs one decompressor per open symbol; retention already stats each file once), or the change would weaken a test (the heartbeat key list is now the one written-down schema the watchdog reads by name).
  - **The twenty, second pass.** The owner approved all of them. Nine were refuted on a second read of the source, and the pattern is worth keeping: a structure holding a float beside an exact value is usually that value's validated projection rather than a second source of truth, and a branch that looks like dead compatibility is usually read by something this repository still writes.
  - **Cover dust, the one real defect (LM-045).** `CoverBook` judged a leftover cover against an absolute `QTY_EPS = 1e-12`. Three sends totalling 1,000,001.1 contracts on a whole-contract venue, each fully absorbed by the account reading, left a residual of 1.16e-10 — the subtraction's own dust at that magnitude, above the floor — so the record never cleared and `StrategyCtx::in_flight` reported a position the reading had already shown, on every quote, for the life of the process. The threshold is now the larger of half the instrument's quantity step and 64 ULPs of the residual's own magnitude, read from the engine's instrument table; a symbol with no metadata is judged on float resolution alone. Three tests, each failing against the old constant.
  - **Exact numerics, what was left and what was not there.** An order term with no exact decimal behind it is now refused at the wire instead of formatted from a float — the engine already could not produce one, since a symbol without exact instrument metadata is refused with `EXACT_INSTRUMENT_METADATA_UNAVAILABLE` and the only order-without-terms path was `#[cfg(test)]`, so a property that held by construction now holds by refusal in every build profile (LM-044). `DecimalField::legacy` is `compat_f64`, named for what it costs, and `tests/repo/test_engine_numeric_boundary.py` lists all seven remaining call sites with the reason each is still correct, failing on a new one (LM-076). `InstrumentRule::from_exact` derives the strategy-facing float rule from the `ExactInstrumentSpec` the engine quantizes against; Lighter's duplicate `min_base_amount`/`min_quote_amount` fields and its `10f64.powi()` grid are deleted (LM-078). Refuted: `BookLevel` staying `f64` leaks no rounding, because the bridge is `strategy_decimal` over `f64::to_string()` — Rust's shortest round-trip decimal — and an exact `BookLevel` would end `Depth`'s `Copy` and put 100 heap allocations on every depth update (LM-046, now measured by `a_book_float_recovers_the_decimal_the_venue_sent`). `OpenTradeLot`'s four float fields feed only `maker_share` and `arrival_shortfall_bps`; every money figure is projected from `Exact` (LM-047). Every venue adapter already publishes exact position and account amounts through `account_numbers::assign`, which refuses an incomplete join (LM-048). `legacy_quantity` is reached only by replay of a recorded legacy step (LM-079).
  - **The two safety features, built now they are approved.** The compressor's work list is bounded by `storage.compress_backlog_max_mb` (4,096) of **raw bytes** awaiting compression; above it a closed segment is deferred — left raw, counted on `compressor.deferred`, compressed by the next start's recovery. Deferring rather than blocking is what makes the ceiling honest: `submit` runs on the frame loop, so a blocking put would stall the socket read and lose rows the venue is still sending, while a deferred segment is already written and fsynced (LM-015). `engine-clock:<unit>` warns when the host wall clock moves more than 60 s against the engine's own `uptime_s` between two watchdog readings, naming direction and size; it suppresses nothing, because a missed dead engine costs more than a page that names its own doubt (LM-061).
  - **A silent config failure the audit walked past (LM-073).** Every capture-config table ignored unknown keys, so `max_disk_bg = 18` left that recorder on the 60 GB default while the comment in the same file says the two recorders' caps must sum under the filesystem or they race `min_free_disk_gb`. Every table now refuses a key it does not have and names the nearest one it does; `[storage]`'s accepted keys are read off `StorageSettings`, so no second list can go stale. `market_tape/examples/*.toml` state only what differs from the defaults, pinned by a test; `deploy/capture/*.toml` keep every number that binds on the host, on purpose.
  - **Durability, one implementation (LM-057).** The dataset part writer and the control-artifact writer had diverged with a direction: the artifact raised on a parent directory that would not sync, the dataset part swallowed it and reported success, leaving a bucket whose name could vanish on power loss. `durable_atomic_write(path, write)` in `core/durable_file.py` is now the one temporary-name/fsync/rename/directory-fsync sequence for callers whose payload a library writes to a path, and `fsync_directory` is shared by all three. One test drives both down the same failure and asserts the same `ArtifactDurabilityError`.
  - **A gate beside the gate (LM-036, LM-038).** `scripts/dev.sh quick` runs Ruff and mypy over changed Python, ShellCheck over changed shell, rustfmt and Clippy plus `cargo test -p` over the touched crates, and any changed test files. `check`'s promise is untouched: `quick` names every suite it did not run and says to run `check` before a push. It found a formatting slip on its first real use.
  - **Venue schema in ordinary CI (LM-027).** The captured `instruments-info` payload the first pass said the repository did not hold was there all along, as the recorder's own `_meta` instrument snapshot under `tests/market_tape/fixtures/host/bybit-linear/2026-08-30/00`. Its four rows, re-wrapped in the venue's envelope and byte-identical field for field, now run through `instrument_wire` and `normalize_instruments_reporting` on every CI run; the whole-catalogue run stays `--ignored` for a live capture.
  - **Already done, refuted (LM-069, LM-070, LM-032, LM-033, LM-050).** Artifact-based deploy is what the repository does: `build_engine()` refuses to run without the runner's tar — *host compilation is disabled* — `release_artifact verify` recomputes every binary's sha256 against the recorded manifest, staging is a fresh empty `mktemp -d`, the candidate lands in `releases/<commit>`, and an incumbent is published by `mv`. The legacy daily tape layout is not residue: `market_tape/pack.py` still packs daily-layout days as `<day>.legacy.tar`, so the reader branch reads what this repository writes; `market_tape/README.md` states when it retires. The historical carry configs are the deployed rule's registered lineage — `descends_from` runs unbroken v7 → v5 → v4 → v3 → v2 → v1 — and a test now walks it from whatever config the deployed units name.
  - **Adapter semantics pins.** Five moved (binance, bybit, hyperliquid, lighter, mexc) because `legacy` was renamed `compat_f64` with the same body and three gateways stopped binding a float their order-term helper no longer takes. No request encoding, order type, quantity conversion or fill interpretation changed, so no capability receipt was invalidated; the reason is recorded beside the constants in `engine-public/src/registry.rs`.

- **2026-09-11 — Robustness workstream on `5fff263b`: operator commands, amend authority and quota, bounded venue work, an enforced canary policy, WAL cost measured, malformed spool rows quarantined on both sides, a restore-check tool and the fenced-recovery runbook; two findings characterised rather than fixed, two drills left as `unknown`.**
  - Source: the robustness action plan against `92453d8c` (`R01`–`R09`). Baseline: `5fff263b`, one commit past the audit, already carried the exit-141 root cause fix (`lib_realms.sh` readers drain their input). Every finding was re-derived from source before it was changed. Local qualification at the workstream head `8e59f953` (`scripts/dev.sh check`, 2026-09-11 23:40–23:58 UTC on the build box): 2,708 Rust tests passed, 0 failed, 9 ignored; 1,954 Python passed, 1 skipped; Ruff, mypy, rustfmt, strict Clippy and CI's ShellCheck command clean. The 24 baseline failures on this box were four missing tools (rsync, rclone, ssh-keygen with zstd, GNU time), not repository faults. Not deployed; STATE.md's host rows are unchanged. No deploy, no credential, no live order, no arming change.
  - **R01 operator commands.** `ops.sh` builds the realm list, the deploy modes and the canary realms on first use by the route that needs them: `help`, `logs`, `units`, the unit verbs and `flatten` read the realm table zero times (nine before), `attest-flat` and `verify-account-identity` nine (eighteen). An ERR trap under `set -E` prints `ops.sh <command> failed (exit N) at line L: <command>` once, from the outermost shell; the router holds no secret. `_lm_realm_field_rows` refuses a data row that is not three fields, decided after draining, so a late malformed row is never hidden by an early match. Five new tests, each failing on the previous tree.
  - **R02 amend authority.** `AmendRequest` carries the permission into `VenueGateway::amend_orders_under(requests, epoch)`; Bybit and Binance re-read `authority_refusal` after their quota wait and before signing, refuse only the lapsed members with the never-sent `BadRequest`, and skip the transport call when nothing survives. A reduce-only order's reprice whose amended quantity does not exceed the remaining quantity carries no authority: an exit's reprice is never refused at the send boundary. Transport boundary tests assert zero recorded requests for a lapsed amend and `Transport` (never `BadRequest`) for an acknowledgement lost after the write. The trading-WebSocket worker re-reads the authority once more immediately before the socket write (`SendGate` on `Command::Request`, no await between the gate and the write): a frame queued across reconnect backoff is refused unsent when its permission lapsed, and a session dropped after the write still answers `Transport`. Three of those tests fail without the gate wiring.
  - **R03 batch pricing.** The venue task plans an amend batch before pricing it and shrinks it to the largest prefix the amend window takes now; the rest stays queued, so a risk-off cancel is never parked behind an in-call quota wait. `sim --seed 7 --strategies quoter` byte-identical.
  - **R04 bounded work.** A full venue mailbox surfaced as `Transport("venue task queue unavailable")` and `service_order_dispatches()?` carried it to the run loop: the process exited, and `StartLimitBurst=5` in 300 s left the unit failed with positions open. Risk-off now travels an urgent lane of 1,024 commands, everything else an ordinary lane of 4,096 drained only while the ready set holds fewer than 4,096 ordinary commands; both lanes are read before every dispatch, urgent first; a command carries at most 10 requests; the dependency index is built once per turn. A full lane refuses at the client, naming the lane and its capacity, and every call site answers it without the run loop: openings take the never-sent path and release their reservation, cancels stay working with one note and go back on the halt queue, stops stay intended, amends note, leverage leaves its dependents queued. `VenueQueueGauges` publish the queue for the heartbeat. Seven tests; the flood and the engine-level test fail on the previous tree (`a full dispatch lane ended the run`). `sim --seed 7 --strategies quoter`: 23,168 records equal once the Boot record's `-dirty` commit suffix is normalised.
  - **R05 canary policy.** `LiveCanary` permitted ordinary execution with one boot warning and every cap was a ratio of equity. `[canary]` is required on a live-canary realm and refused elsewhere, compiled in `runner::run` before the log claim: `started_at`/`expires_at`, `accepted_unproven` equal to exactly the realm's unproven set (the registry's `unproven_from` is its one writer, so a receipt arriving or going stale refuses the deployed config), the sleeves that may open, an optional instrument list, `max_positions`, `max_open_orders`, absolute `max_gross_notional_usdt`, `max_position_notional_usdt`, `max_loss_usdt`. Judged in `journal_and_admit_intent` after the existing refusals and before the risk reservation through the same `IntentRefused` records; eight `OpeningRefusal` codes; reductions, cancels, stops and flatten never reach it. The mexc and hyperliquid templates carry deliberately wide ceilings (50 positions, 100 orders, 100,000 gross, 50,000 per symbol, 10,000 loss, in the account's settlement asset), LONG only, 2026-09-12 to 2027-09-12: the equity-relative `[risk]` caps decide exposure during the test and the policy binds on the window, the unproven set and the sleeve list. 26 tests; `a_deposit_does_not_enlarge_the_canary_ceilings` is the point. The realised half of the loss ceiling is seeded at boot from the replayed closed trips since `started_at` (R05b).
  - **R06 WAL, characterised.** `engine-tools wal-cost --rotations N` and `engine_wal::measure_rotation` time a rotation and its two halves at 64 KiB, 1 MiB and 8 MiB base records; `WalWriter::durability_mode` names whether barriers run on the sync thread or, when it could not be started, on the caller. Measured here (virtio disk, release build): rotation 1.0–1.3 ms p50 at 64 KiB, 4–12 ms at 1 MiB, 58–70 ms at 8 MiB, once per 256 MB; on `/dev/shm` the 8 MiB case still costs 11 ms, so the base record's serialisation, not the fsync, dominates. The funded engine hands the rotated segment to its callback reader on the same tick (`has_reader()` is true for the life of a process whose log carries strategy-callback state), so an off-thread rotation would settle immediately; not built; the design on record is a `rotate` that pushes the buffer, hands the archive fsync and the next segment's create, base frame, `sync_data` and directory `sync_all` to the sync thread, buffers appends meanwhile, and settles on the next barrier, reader or drop, which only pays off once `order_news::rotated` takes the reader lazily. By frequency the synchronous `barrier()` in `signal_intake.rs:481` (per accepted signal) and `strategy_effects.rs:15` (per strategy flush) is the larger item at 0.17–0.36 ms p50 and 84 ms observed max; not changed. Heartbeat carries the durability mode and the last rotation's duration and base size (R07).
  - **R07 alerting and heartbeat.** Five heartbeat objects, every absent value null: `venue_queue` (ready counts per lane, oldest queued age, in-flight class and age, capacities, refusals), `protective_backlog_oldest_ms`, `wal` (`durability_mode`, `last_rotation_ms`, `last_rotation_base_bytes`, `segment_bytes`), `canary` (window, exposure, loss, unvalued trips, blocking reason), `loop_iterations`. Watchdog rules: protective work unanswered over 60 s `CRITICAL` whatever the process state; caller-thread barriers `WARNING`; a loop count unchanged across two fresh readings within 60 s `CRITICAL` (state in the counters file the rate checks keep); a canary restriction `NOTICE`, a policy inside a day of expiry `WARNING`; none of the last three blocks a deploy. `scripts/ops.sh alert-drill` runs the documented host delivery drill (one Telegram test, one no-op routine fire, one dead-man ping), host scope only. notifications.md carries the drill table and states maintenance suppression as the code does: a window exists only while a process holds the deploy lock, so an interrupted deploy leaves none. **Not run:** the alert delivery drill and the host-loss drill; STATE.md carries both as `unknown`, and the routine was paused with the alerts chat id unconfirmed at the last host reading.
  - **R08 spool quarantine.** The signal worker's `inventory()` no longer fails on one entry: a candidate over 65 MiB, not valid JSON, without a string `kind`, or without a `{20 digits}-{hash}.json` name is renamed into `<spool>/quarantine/<name>` with a `<name>.reason` sidecar (reason, size, mtime, wall time, original path); bounded at 256 files / 256 MiB, never deleted, refused-in-place and reported `unreadable` at the bound or on a name already held. Heartbeat: `spool_quarantined_files`, `spool_quarantined_bytes`, `spool_unreadable_files`, `spool_quarantine_reasons`; liveness: quarantined `WARNING`, unreadable `CRITICAL`. Eleven tests; the crash-loop regression (`Durable::open` with a malformed row) fails on the previous tree. The engine had the same fault: `read_one`'s `SignalError::Source` for a truncated, oversized or unnamed row became `EngineError::State` and ended the run. The scanner now quarantines the row where it meets it with the worker's contract (`source: "engine"` in the sidecar), skips for the life of the process a row it cannot isolate, and the missing sequence records `SignalGapRecorded` so the destination's openings stop until the prefix is repaired; a name carrying no sequence no longer ends every catch-up pass. Boundary accepted: a transient read error on a good row is quarantined too, which is strictly better than the exit it caused. The deploy pre-creates each realm's `quarantine/` group-writable, since both writers run under umask 0027.
  - **R09 recovery.** `engine-tools restore-check --wal PATH [--spool DIR] [--controls DIR] [--max-age-min N] [--json]` reads a restored family without the lock and without truncating: segment trust and torn tails, the reader's format, `may_open`, open orders, positions with the protection the log expects, unresolved order decisions (cohort's classification), the newest stamp's age, the spool's deliverable rows and quarantine, the controls directory; every absent value `unknown`; the verdict is the exit status (`ready-to-reconcile` 0, `reconcile-required` 3, `stale-backup` 4, `incompatible-reader` 2, `unreadable` 1). docs/operations.md §4 states RPO (15 min, the backup cadence: venue reconciliation is mandatory before arming) and a proposed 60 min manual RTO, the compatible recovery set, per-venue fencing by credential deletion or provider power-off, readiness before arming, stop conditions, the returning host, and a drill table whose cells read `unknown`: the drill has not been run. Spool and controls are in the backup set; `stream.sock` is not and the engine rebinds it.
  - **Still proposed, not built:** off-thread rotation (above); moving the two live-loop synchronous `barrier()` sites onto `barrier_begin`; a `read_one` that keeps the `io::ErrorKind` so the engine can leave a transiently unreadable row in place as the worker does; conditional retention of the boot's closed trips (kept for the process life today, one boot replay's worth); the sim's synthetic venue does not pace itself, so the batch-shrink and lane paths are exercised by unit tests, not by `sim`.

- **2026-09-11 — Audit follow-through on `7e048891`: two research arithmetic faults, two tape-reader faults, one packaging fault and one CLI fault fixed; boot, dispatch, risk and worker paths shed redundant work; the docs stopped contradicting the config. One audit finding refuted; the six items first deferred landed in a second round, below.**
  - Source: the eleven-area combined audit of `7e048891` (`00-scope` through `11-docs`, ranked twenty). Every change below was re-derived from source before it was made; nothing was taken on the audit's word. No decision, quantity, WAL record or venue behaviour of the funded engine changes; the engine edits are lifetime, scan and message changes with the existing suites as the guard.
  - **Refuted, F3.** The audit read `entry_valid_minutes_after_settlement = 20` as an S+20m EXODUS entry cutoff. The reducer subtracts its fixed 15-minute `ENGINE_ENTRY_CUTOFF_MS` (`native_exodus/plan.rs`, `plug.rs`; `validate()` requires the key above 15), so the effective deadline is the documented S+5m. `docs/trading_logic.md` §5 now shows the derivation; `research_findings.md` is untouched.
  - **Lab drawdown (P1).** `research/lab/backtest.py` `stats()` measures drawdown against a peak floored at the initial capital: `[-0.1, 0, 0, …]` reports `maxdd = -0.1` where it reported `0.0`. Every published lab `maxDD` from a book whose equity never exceeded 1.0 was understated by exactly its distance below the start. `ann_ret`, `ann_vol`, `sharpe`, `t`, `total` unchanged.
  - **Lab position model (P2), a strategy change.** `run_book` is a daily rebalance to target equity weights; turnover now includes the rebalance back from price drift: `turnover[t] = Σ|w_held[t] − w_drift[t-1]|`, `w_drift = w_held(1+r)/(1+Σw_held·r)`, `turnover[0] = Σ|w_held[0]|`, drift suppressed when the day's equity factor is not positive. Audit case: two names 50/50, one doubles, target unchanged → pre-trade 2/3 and 1/3 → turnover 1/3 → cost `2.593333e-4` (2.593333 bp) where it charged 0. A single fully-invested long still has zero drift turnover. On the 12-day fixture: day 7 `0 → 0.00738916`, day 9 `2.0 → 2.00738916`, day 10 `0 → 0.05457501`. Costs rise and net falls for every lab book that holds through price moves; rerun before quoting old lab numbers. `vol_target(lag=0)` no longer raises (P4). `Panel.funding` keeps NaN for a missing cell and `run_book` reports `missing_ret_exp` / `missing_fund_exp`, the gross weight standing on cells it zero-filled (P5). The module docstring names the model and states that `FEE_PER_SIDE` is one all-in constant, not the engine's per-symbol snapshot (K3, T5).
  - **Lab panel windows (P3).** `research/lab/panel.py` `adv_30/adv_90/rv_30/rv_7/rv_90` are calendar-day windows through `core._common.calendar_roll` instead of row counts, `min_samples` unchanged. Identical on contiguous history; with one missing day in the first 30, `adv_30` at day 30 goes from a 31-calendar-day value to null and first lands at day 35. `age_days` stays a row count.
  - **Tape bars (M2).** `market_tape/bars.py` replays book rows through `market_tape.book.Book`, one per `(symbol, depth)` kept across bar boundaries; `best_bid`/`best_ask` are the whole book's best levels and move only while the book is valid. Two wrong prints on depth-50 feeds are gone: a delta on a lower level reported as best (99 while 100 stood) and a zero-size delete of the best leaving the stale level (100 while 99 was best). Bars built from depth-50 deltas before this commit carry those errors; rebuild where used.
  - **Tape reader (M1, M3, M4).** `market_tape/load.py`: the rclone hour cache is namespaced per remote, `<cache>/<sanitised remote:path>-<sha256[:8]>/<hour path>`, so two remotes holding the same hour (`gdrive:tapes/bybit-linear` and `gdrive:tapes/binance-usdm`) stop serving each other's archive (copy calls per hour `[1, 0] → [1, 1]`); entries under the old flat layout are never read again, delete them by hand. `lsjson` runs once per `RcloneRemote` (25 reads → 1 listing) with `refresh()` to relist. `iter_rows` holds one zstd process per symbol instead of one per segment, chaining a symbol's segments in order, and tar members are read by their stored `TarInfo` rather than a per-member name scan.
  - **Engine (C1, C2, C4, K1, K2, V3, V4, W4, C5).** `runner::run` drops the decoded WAL replay after boot instead of holding it for the life of the process. `service_order_dispatches` evaluates `leverage_pending()` once per scan, not per candidate. `assembly::symbol_order`, `boot_subscriptions` and `market_feed_for_registry` index membership with hash sets, with a 3,000-symbol test asserting the emitted symbol and subscription order is byte-identical to the old scanning bodies. `evaluate_inventory` folds post-snapshot fills once and passes them into `projected_book`/`projected_portfolio`, which became `&self`. The unused `NativeCarry::reduce` and `NativeExodus::reduce` wrappers (no callers) are deleted. Lease refusals name every realm in `VenueName::ALL` and both account-id shapes instead of "demo or mainnet" and "a whole number". The shared request budget's refusal returns the real wait until capacity can free instead of a 10 ms poll. `engine-public::http` and the `engine-core` crate/runner docs describe the pool and boot call map as built.
  - **WAL readers (W1, W5, T3).** `engine_wal::replay_chain_visit(family, visit) -> Result<bool, WalError>` streams a whole family record by record; `replay_chain` is that walker collected, so segment trust, renumbering and the torn flag have one implementation. The flag's documented meaning is corrected to what the code has always done: any trusted segment read ended in a torn tail, not necessarily the newest. `engine latency` folds through the visitor instead of loading the family; `fills`, `latency`, `cohort` and `replay` print the segment meaning from one shared sentence.
  - **`engine-tools` CLI (T1).** Every operator subcommand declares its options and refuses anything else by name: `engine: <command> does not take <args>`. A repeated flag is refused too (the first used to win silently). `engine bench --contention --json`, which ran an ordinary bench and printed the table, now prints the result as JSON with its `contention` block; text output names the workload. Every operational caller (`ops.sh`, `deploy_remote.sh`, `flatten_account.sh`, `telegram_control_helper.sh`, the units) was checked against the declared sets.
  - **Fee snapshot (T2).** `configs/bybit_fee_rates.json` is compiled into `engine-tools` and is the default for `engine backtest` and `engine sim`; `LIQUIDITY_MIGRATION_FEE_SNAPSHOT=PATH` is the only override and a missing path is still an error. The CWD and source-tree lookups are gone, so a packaged binary prices fills identically off a checkout (the copied release's bare `sim` failed on the missing source-tree path). Reports carry `snapshot_source`; the pinned sha256 `13ea6684…` and the quoter log fingerprints are unchanged.
  - **Signal worker (S1).** `commit_event_batch` moves the encoded observation vector into the journal entry and borrows it back for publication; `compact_candidate_checkpoint` takes the vector by value. One allocation and one copy per encoded observation per batch gone; bytes, order and sync unchanged, 227 worker tests as the guard.
  - **Tests and CI (Q1, Q2, Q3).** The four `engine-types` integration files link one binary under `tests/integration/main.rs` (11 tests, unchanged). The CI Rust job's test step is split into `Build tests (locked)` (`--no-run`) and `Tests (locked)` so compile-and-link is timed apart from execution. Every fix above carries a test that fails without it; the equivalence tests were shown to bite by mutation.
  - **Deploy and scripts (D2, D3, D4, D5).** `lib_sleeves.sh` memoizes `lm_validate_fleet_manifest` on the manifest's `cksum`, path and realm-table identity: ten helpers validate once per unchanged manifest and a changed file re-validates by itself. New read-only `scripts/runtime/engine_status.py` renders one engine heartbeat as HEALTH / EXPOSURE / BLOCKERS with absent fields as `unknown`; `scripts/ops.sh why [REALM]` runs it on the host and `deploy verify` prints it per realm. `scripts/dev.sh check` now supplies, prints and refuses-inside-the-repository the pytest basetemp and ends with `check complete; ran: …; skipped: …`; the pre-push hook is a three-line delegation. The engine units' memory comment is two dated 2026-09-01 observations (53 MB → 322 MB, 165 MB → 522 MB) plus the 256 MB rotation cap, with `MemoryMax=2G` named as sitting above an extrapolation; rendered from `policy/realms.py`, no setting changed.
  - **Docs (F1, F2, F4, F6, F7, F8, K4, S5, P6).** Strategy IDs are per-realm config positions: `architecture.md` §4 points at `trading_logic.md` §1, `README.md` labels its column `ID (mainnet)` and its CARRY row says daily book, not "sticky 48h". The IPC contract reads one way everywhere: the spool row is the delivery, `stream.sock` the wake (`architecture.md`, `data.md`, `trading_logic.md`). Control spool path corrected to `controls/<realm>/` (`architecture.md`, `engine.md`). Probe distance 0.5% (`offset_bps = 50`) and liveness cadence delegated to `notifications.md` (`observability.md`). `data.md` separates deployed lifecycle from capture retention; CARRY's row states `early_exit_enabled=false`, `presettlement_exit_enabled=false`. Funding failure-mode row 10 deducts settlement-time cash at the instrument's interval (Bybit 8 h, MEXC `collectCycle`, Hyperliquid hourly). `liquidity_migration/README.md` import ranks match `tests/repo/test_import_order.py` (seven ranks, `ops` and `cli` peers). `engine.md` gains the log-reader CLI contract table.
  - **Second round: the six deferred items, all landed.** Same method, each re-derived from source, each with a test that fails without it, and sim runs byte-identical against a clean base wherever boot or dispatch changed. On the host by [deploy run `34638883797`](https://github.com/rob435/liquidity-migration/actions/runs/34638883797) (`5fff263b`, `vps` job 19:41–19:51 UTC, `deploy-ok` 19:51:40, rollback target `9a21c7f5`); the first dispatch, [deploy run `34637178253`](https://github.com/rob435/liquidity-migration/actions/runs/34637178253) on `92453d8c`, failed its `ci` job on the D1 fault below and never reached the host.
  - **Dispatch (V1/C3).** `VenueGateway::quota_wait(QueuedCommand) -> Duration` answers, without waiting or spending quota, how long the adapter would hold a command of that shape back. The venue task (`engine-core/src/venue_runtime.rs`) prices every ready command, picks among those the quota takes now, then by class, then arrival, and when every selectable command is over quota parks for the shortest of their waits, preempted by whatever arrives meanwhile. A held opening or amend whose dispatch authority has already lapsed is answered never-sent at once; one that lapses during the hold, when the hold ends. MEXC's `Pacer` computes the wait and the reservation with one function (`wait_for`) and classifies a signed POST and a queued command from one table (`quota_lanes`, `posted_command`); Bybit and Binance answer from their own rolling windows; the other adapters take the default of zero. Quota constants, class order, the still-queued-send rule, the adapter's second authority reading and the `OrderDispatchAttempted` barrier are unchanged; the adapter-source pins in `engine-public` were refreshed for the adapters touched. Measured with `bench --contention --quota` (a local venue of MEXC's quota shape at 5 ms, 4 symbols at 200 Hz, every resting order pulled every 50 quotes so risk-off arrives while openings are still queued), three runs each on binaries differing only in `venue_runtime.rs`: a cancel's wait for the venue task at p99 1,943–1,955 ms before, 6.6–7.5 ms after; the opening's in-call quota hold at p50 1.94 s before, 275 ns after, reappearing as its queue wait; expiries 0 and 64–71 sends either way. The plain `--contention` workload cannot show it (cancel p99 207 → 202 ms): it mints a cancel only after that symbol's opening is answered, so no cancel arrives during a hold. Six paused-time tests pin the property in `tests/venue_priority.rs`, and each pacer's wait query is pinned to the reservation it precedes. `engine-tools` now writes its log to stderr, so `--json` on stdout parses; it did not.
  - **Bybit book (V2).** One sequence contract for the live feed, the recorder, the tape rebuild, the backtester's tape reader and the quote lab: a delta continues a topic only at `u == last_u + 1`; a frame whose `type` is not `delta`, or any `u == 1`, re-bases it; anything else is a gap until the next snapshot; `seq` is recorded, never chained. Decided from the recorded hour `tests/market_tape/fixtures/host/bybit-linear/2026-08-30/00`: 1,429 depth-50 deltas (BTCUSDT 1,179 over 53 s, PENDLEUSDT 250 over 123 s), every one `u + 1`, zero jumps, zero `seq` regressions, and all 225 depth-1 rows snapshots. The live rule was already this; the tape accepted any `u` increase and flagged `seq` regressions (`market_tape/venues/bybit.py`, `book.py`, `engine-tools/src/backtest/tape.rs`, `research/execution/quote_lab/book.py`, the last of which also read `u == 1` as unhealthy). `tests/fixtures/bybit_orderbook_sequence.jsonl` holds 17 raw venue frames with the verdict each side must reach; `engine-marketdata`, `engine-tools` and `tests/market_tape/test_bybit_sequence.py` all read it. The recorder answers a gap by re-subscribing that one topic within a fraction of a second, one outstanding per topic, counted as `shards[].resyncs` in `status.json`; before, a gapped depth-50 book stayed dead until the hourly re-anchor. `market_tape/README.md` §Book sequence contract is the table.
  - **Boot replay (W2), third design.** `engine_wal::open_current_with(family, keep)` drops refused records at decode time, decides trust on the first decoded record and derives the writer's next sequence from the decoded count, so the writer is filter-independent. `assembly::boot_wal` refuses `Intent`, `IntentRefused`, `Verdict`, `CancelSent`, `QuoteFill`, `VenueTiming` and `LatencyLedger`, which no boot reader matches and which are 48.6–48.8 % of frames and 49.2–49.4 % of bytes on two real `engine backtest` logs, and hands boot a `BootReplay` that carries each kept record's WAL sequence. The first filter would have corrupted callback recovery: `CallbackPages::replay` and `OrderNews::attach` read a record's position + 1 as its WAL frame sequence, so a filtered vector renumbered every durable cursor (measured: a queued callback's cursor read sequence 1 where the log says 9). They now read `BootReplay::sequence` and `by_sequence`; §3 of `docs/engine.md` states the invariant. Synthetic 50,000-record, 57 MB segment: resident growth 117.8 MB unfiltered, 9.2 MB filtered. `engine sim` on `--seed 1 --seeds 3 --crashes 2 --strategies mainnet`, `--strategies mexc` and two more cells is byte-identical between a clean `d4a8282` build and this tree (built with one `ENGINE_GIT_COMMIT` so the Boot record cannot differ). The three boot memory tests now measure the live resident set from `/proc/self/statm`; the two older ones read a spawned child's high-water mark, which never moves, so they passed whatever the code did. Not W2's, found by its base build: `engine sim --seed 1 --hours 6 --strategies mainnet` fails on `d4a8282` itself, `signals_consumed_exactly_once` after the injected death (four published rows never reached the log) and, with `--crashes 0`, `strategies_healthy: carry: CARRY decision universe has fewer than 50 symbols`. Not root-caused here; it is the next thing to look at under the funded template.
  - **Signal worker (S2, S3, S4).** `prepare_event_batch` cloned the whole worker per committed batch, one owned `String` per kline bar among it: 41.8 ms median at 400 symbols × 720 bars (plus funding and whale series). Per-symbol series sit behind `Arc` and `Arc::make_mut` copies only the series a batch writes: 0.17 ms, the checkpoint bytes identical to the layout recorded from the old types (`tests/fixtures/worker_state_checkpoint.json`), all-or-nothing commit unchanged, and a rollback test that compares rows rather than a shared handle. Spool inventory parses each row's `kind` alone; a row whose body no longer parses counts toward its class instead of failing the worker's open, and a row with no readable `kind` still fails it. `universe_endpoint` named `api.bybit.com` for the hyperliquid realm whose universe comes from `api.hyperliquid.xyz`; fixed, with the cold-start placeholders routed through it. The heartbeat and `check-config` key `public_bybit_host` is `public_host` and carries the venue's own market host on mexc and hyperliquid (Bybit realms unchanged; nothing outside the worker read it). The liveness alerts say `public WebSocket`, keys unchanged so open incidents do not re-fire. Not renamed: the four `bybit_*` keys of `sources`, because `signal_feature_contract_sha256` hashes that whole block into both feature identities and every realm would cold-start; the consumers of each identity are now a table in `docs/engine.md`. The release-only `perpetual_resource_envelope` test was red at `d4a8282` against a hardcoded 34-day carry retention written before `minimum_replay_days = 90`; it derives retention from the config and bounds the checkpoint at 256 bytes per retained row (measured 188).
  - **Deploy (D1).** `liquidity_migration/policy/realms.py` renders `deploy/realm_fields.tsv` (`realm|field|value`, 60 rows per realm) beside the units; `deploy/lib_realms.sh` is a lookup in that file and derives nothing, the 150-line AWK twin gone. The deploy ships the fields file and the helpers to the host before the checkout exists, in place of the table; `ops.sh`, `flatten_account.sh`, `lib_sleeves.sh` read the same file. Every realm × field compared byte-identical against the old script: 240 lookups, zero differences; a realm or field reaches the shell only after `python -m liquidity_migration.policy.realms render`, and the drift test catches the omission. Its first landing on `main` (`92453d8`) failed 50 tests in the runner's `ci` job at 19:08 UTC while the same gate passed here: `lm_realm_field` quit its awk on the first matching row while the row filter was still writing the 19 KB table in 4 KB pieces, the filter died of SIGPIPE, and under `pipefail` the lookup answered 141 with the right value on stdout, so `ops.sh help` exited 141 printing nothing and `realm_run_ready` said `unsupported readiness realm: mexc`; on this box the writer won that race every time. Every reader in `lib_realms.sh` now reads to the end, and a test looks the first row up in a 600 KB table, more than a pipe holds, which fails 20 times in 20 without the fix.
  - **Still proposed, not built:** pin the parameters that drifted (strategy block order, probe offset, liveness cadence, EXODUS cutoff) to their config sources with one `tests/repo` test; add `shellcheck-py` to the dev extras so the local gate stops skipping the ShellCheck CI enforces; a per-row checkpoint bound in the worker's heartbeat, since nothing in the runtime caps `checkpoint_bytes` today.

- **2026-09-10 — Tier 1: the funded sleeves run under `engine sim` against a synthetic producer, every decision in the log names its cause and every refusal is a typed record, each sleeve's execution requirements are checked against the venue's row at boot, and the sim found four engine faults and one of its own, all fixed; the first deploy surfaced a fifth in the signal worker, fixed and deployed the same night. Deployed on the owner's direction: `7e048891` by [deploy run `34544358127`](https://github.com/rob435/liquidity-migration/actions/runs/34544358127) (handover 00:12–00:20 UTC 2026-09-11), then `9a21c7f5` by [deploy run `34549775253`](https://github.com/rob435/liquidity-migration/actions/runs/34549775253) (01:28–01:36 UTC) with the worker fix; [deploy run `34548356616`](https://github.com/rob435/liquidity-migration/actions/runs/34548356616) for `3115c92a` failed its own gate on the contention cell and touched nothing.**
  - Merged into `main` as merge commits from `claude/tier1-sim`, `claude/tier1-cause`, `claude/tier1-compat`, `claude/tier1-bench`, `claude/tier1-f1`, `claude/tier1-f2`, `claude/tier1-f3`, `claude/tier1-f4`, with the integrator's re-pin and three small commits between them. Plan: `~/Desktop/tier1-plan.md`, written against `257eb0ce`; three of its cited facts were wrong (its "working supervisor" `PendingAction` site is a test; hyperliquid's `caps()` already agrees with its `ReduceBelowMinimum: Unknown` row; the `deployed_templates` helper rewrote only the demo and mainnet profile paths) and three more fell to running the sim (a legacy source cannot make a dependent sleeve ready on `boot_as_exact`; a native sleeve declares no subscriptions, so the venue must list its whole catalogue and rows carry `market_subscriptions`; LONG features close at UTC midnight, not on the hour).
  - **Engine fault: an account refresh during leverage administration refused the opening.** `complete_leverage` compared the whole snapshotted `AccountView` against the current one, so a refresh that re-read the same state, only `observed_ns` having moved, refused the opening whose leverage it was administering; with `leverage_authority = "sole"` and `leverage_at` empty after every boot the first opening per symbol has to win that race, and a sleeve that loses re-requests and loses again. `sim --strategies mexc --hours 12 --seed 1`: 61 openings, every one refused `never sent: account state changed during leverage administration`, zero fills; after, 2 openings, 1 fill. `account_reading_changed` (`engine-core/src/engine/free_helpers.rs`) ignores `observed_ns`. The funded mexc and hyperliquid engines run LONG through this path; their journals hold no opening attempt yet, so it has not fired live. Tests `a_refresh_of_the_same_state_is_not_an_account_change`, `a_realm_day_opens_every_symbol_it_decides_on`.
  - **Engine fault: a wake parked on a busy symbol livelocked the drain.** A journaled transition's effect deferred on a busy symbol is released into `ready_actions`, which `drain` loaded only when `host.pending` emptied; later actions from the same strategy sat in `host.pending` and were pushed back on every pass by the ordering guard, so neither queue could move. The run loop stayed in drain mode at ~85 % CPU with the market and signal lanes shut off, the heartbeat still written (`beat` runs inside `on_tick`), and any open position unmanaged; the dead-man's switch cannot see this class. Precondition: a sleeve's transition holds an order or stop for a symbol inside a venue mutation while the same sleeve is fed again, which the boot after a death (a deploy handover) produces when it recovers a stop fill the dead process never saw, latches on the drift, and fires a portfolio exit on the symbol the sleeve is also closing. `sim --strategies mexc --hours 2 --pump 1.0 --crashes 1` never finished; it completes in 6 s with the stop fill owned. Fix in `engine-core/src/engine/scheduling.rs`: a queue that has rotated whole without progressing resumes a parked wake through `load_ready_wake`. Test `a_parked_transition_runs_before_the_later_actions_that_wait_on_it`.
  - **Engine fault: a venue close of a holding summed from binary64 fills was refused as an overfill.** An execution without exact amounts states its quantity as a binary64 field, and the holding it closes is the sum of those fields, which lands a rounding off the grid the venue quotes: 0.6 + 0.1 on a 0.1 grid is 0.69999999999999998. The forced-close allocation resolved the execution's quantity onto that grid and compared it with the unresolved sum, so a venue stop closing the whole 0.7 read as more than was owned, was refused `forced execution does not reduce the owned physical net`, and the holding stayed in the log while the venue was flat: `sim --strategies mexc --hours 80 --seed 5 --faults light` failed `positions_agree` and `sleeve_attribution_agrees` with `symbol 2: log 0 venue -0.7`. `Attribution::owned_on_grid` (`engine-core/src/attribution/allocated.rs`) resolves the sleeve rows onto the same grid and `portfolio_allocation::allocate` compares and slices against them (`AllocationInput::owned`), so both sides are on the grid or neither is. Live it needs a venue-native close (a stop or a liquidation) of a position whose fills carried no exact amounts, which is what execution-history recovery reads on every venue; a position the log cannot account for is the condition the funded engine latches on at boot. Test `a_venue_close_of_a_binary64_holding_under_its_grid_value_is_charged_to_its_owner` (fails on `fcfbe9f2` with that refusal); the recipe and seeds 1–4 and 6 at 80 h light pass.
  - **CARRY: the day after a consumed thin day** (the Hyperliquid matter above, moving on). `859723b1` fixed the first thin day and moved the page to the next one: consuming a thin batch leaves `state.scorer.last_decision_ts_ms` at 0, and the worker builds the 45-day replay window only while `last_carry_decision_ts_ms` is `None` (`signal-worker/src/worker.rs` `publish`), so the next UTC day's batch is one day long and `score_decision` refuses it with `CARRY replay is below the 45-day floor`: rejected, `last_error` set, the realm's liveness CRITICAL from the first day boundary after the deploy, on both alt realms. Fix in `native_carry/plan.rs`: `nothing_to_decide` treats `REPLAY_BELOW_FLOOR` like `UNIVERSE_TOO_THIN` when the day's own decision universe is under `MIN_DECISION_SYMBOLS`, so both reducers consume the batch when entries are off and the sleeve holds nothing; a short window over a full universe still errors, so a lost checkpoint on a wide realm stays visible; entries-on behaviour and every decision are unchanged; `scorer.rs` names the refusal `REPLAY_BELOW_FLOOR`. Test `the_day_after_a_consumed_thin_day_carries_one_day_and_is_consumed_too`. The sim had hidden it: its producer re-sent the whole 46-day replay every day, a window the worker never sends; it now sends the replay on the cold start's batch alone and one day per batch after (test `only_the_cold_start_s_carry_batch_carries_the_replay_window`). With the producer matched and the plug unfixed, `sim --strategies mexc --hours 80 --seed 3` rejects 3 of 175 CARRY rows on that refusal; with both, 175 of 175 are consumed and all fourteen checks pass. Change point `c52f70ef`. **Moving on, 2026-09-11:** that fix covered thin days only. On `hyperliquid_mainnet` the decision universe grew to 151 symbols on its second day, so the one-day batch over a full universe was refused `REPLAY_BELOW_FLOOR` by the freshly deployed `7e048891` at 00:20 UTC (watchdog `CRITICAL strategy-errors … carry: CARRY replay is below the 45-day floor` from 00:20:42, every 30 s; the incident routine was paused), and would have been every day after: the worker's memory of having published a decision is not the scorer's state, and no handshake carries it. Every `carry_feature_batch` now carries the replay window, as the cold start's does (`signal-worker/src/worker.rs`): `score_decision` takes only the days after its checkpoint, so a seeded scorer decides exactly as before and one that has never decided can on any day; the worker keeps and repairs the replay depth of klines, funding and trading intervals whether or not it has published (`required_carry_history_hours` takes the config alone). The daily batch is about 4 MB on a 151-symbol realm (`minimum_replay_days` 90), under the 16 MiB payload cap. The sim producer sends the window on every batch again. Tests `every_carry_batch_carries_the_replay_window` (worker, fails on `7e048891` with the second day one day long) and the sim producer's test of the same name. The hyperliquid sleeve's `last_error` is runtime state: it cleared at the `9a21c7f5` restart (01:35 UTC; watchdog `ok` from 01:35:58) and its scorer seeds on the 2026-09-12 batch.
  - **The funded sleeves under the simulator.** `engine sim --strategies quoter|demo|mainnet|mexc|hyperliquid` runs a deployed template's own generated `[[strategy]]` blocks, byte for byte from `deploy/engine.<realm>.toml.template` on `configs/operational.json` (both compiled in), against a synthetic producer on the signal worker's own grids: LONG features close at UTC midnight and republish hourly; CARRY publishes readiness, hourly market snapshots, funding on the eight-hour grid and a daily feature batch. The replay feed holds the worker's two-round producer lifecycle handshake. Flags `--hours` (12), `--tape-step-s` (10 realm, 1 quoter), `--capital` (500 realm, where LONG's target clears every catalogue minimum: BTC 0.002, ETH 0.03, SOL 0.6), `--shock on|off` (one symbol falls 20 % over 60 s from 1.5 h in and holds an hour), `--pump` (0.5), `--gate`. Injected: signal rows delayed past the entry window, delivered twice, or withheld so the source has a hole (`FaultySignalFeed`), and the shock. Six judges join the eight: `strategies_healthy` (reads `intent_refused.code`), `signals_consumed_exactly_once`, `checkpoint_identity_holds`, `sleeve_attribution_agrees`, `no_opening_before_readiness`, `working_entries_settled`; `SimReport` gains the signal counts, per-sleeve orders and fills, `strategy_errors` and `refusals_by_code`. Cells (`engine-tools/tests/integration/sim.rs`): mexc 3 h twice (LONG trades, a venue stop fill is owned), mexc 2 h × 6 seeds with light faults, the shock and a death, mexc 2 h heavy with the shock and two deaths byte for byte, mexc 12 h (the leverage fix's regression test); the 80 h recipe lives in `docs/engine.md` §10. The simulated venue implements `set_stop_exact`, so a native sleeve's stop is restored after a boot instead of the engine latching shut on a position it reads as naked. Realm mode does not model the worker, the spool files, the socket, or the venue's real latency. Two runs of one seed write one log from the CLI as from the suite: the Boot record's config identity is taken with the seed's scratch directory abstracted out of `operational_profile_path`.
  - **Quoter pins.** `SimReport::wal_sha256` cannot be pinned: `engine-core/build.rs` stamps the build's git commit (plus `-dirty`) into the `Boot` record, so the file hash moves with every commit; with `ENGINE_GIT_COMMIT=257eb0ce…` the tree reproduces all eight `257eb0ce` hashes byte for byte. The three quoter cells pin a sha256 over every record after `Boot` plus the record count per seed, and skip the fingerprint when `configs/bybit_fee_rates.json` is not the pinned snapshot. The pins moved on two change points in this entry: `set_stop_exact` on the simulated venue, and every refusal becoming a typed record (the clean cell grows from 7,331 to 10,897 records: the quoter re-proposes a refused quote every second, and each refusal that was one suppressed note is now one record).
  - **Causal ids.** `WalRecord::Intent` carries `cause`: the reducer callback the decision came out of, as one `Cause` (`signal` with source, sequence and observation id; `timer`; `market`; `order`; `boot`; `feed_reset`; `strategy_event`; `entry_permission`; `flatten_directional`; `working` for the engine's own working supervisor; `restored` for an effect replayed at boot) plus `callback_wall_ms` in engine realtime milliseconds beside the intent's monotonic `decided_ns`. Defaulted and skipped when absent, so every log written so far replays with no cause rather than a wrong one; engine-originated intents (portfolio emergency close, portfolio exit) carry none.
  - **Typed refusals.** New record kind `intent_refused`: `wall_ts_ms`, `strategy`, `symbol`, `tag`, the `client_order_id` when the refusal came after an Allow verdict, a bounded snake_case `code` (40, pinned in `engine-core/src/engine/refusal_code_tests.rs`: 16 engine words, the 13 `OpeningRefusal` words, 12 `DenyReason::code` words with `stale_quote` shared on purpose) and the sentence behind it in `detail`. Written in `tell_refused` and nowhere else, every time, before the strategy is woken; the free-text `Note` and its 60 s suppression are unchanged. The reason each strategy hears in `EngineEvent::IntentRefused` is byte-identical at every site; one engine-core test extended its expected sequence by the one inserted record. An authority-expired opening is carried by `OrderUpdate::Reject`'s reason text (`never sent: authority: expired …`) and by nothing typed: `intent_refused` covers refusals before dispatch only.
  - **`engine cohort`.** The two lanes join: a decision's cause names the source row it came out of, so the report carries a `rows → intents → allowed → wire → filled` funnel per source and in total, and every other decision is counted under what woke it. Two ages reported as unmeasurable are measured, source stamp to decision and engine consume to decision, both against `intent.cause.callback_wall_ms`; `decided_ns` is never subtracted from a wall stamp; a decision without a stamp is counted in `without_a_stamp`. Refusals come from the typed records grouped by `code` (`DenyReason::code` replaces the reader's own PascalCase names: `rejected_by_reason` keys are `stale_quote`, not `StaleQuote`); the two note parsers are deleted; a log holding the old refusal notes and no typed record counts its refusals unresolved and says why. `not_an_opportunity` is renamed `other_records`; `Interval.unmeasurable` leaves the JSON. On a mexc sim WAL the report balances and shows the mechanism by cause: every signal-caused LONG intent is refused `stale_quote` because a native sleeve's symbol is admitted at its first decision and has no quote yet, and the orders that go out come from the later timer, market and working wakes.
  - **Reader boundary.** `cause` is backward compatible: a binary built before this commit skips the unknown field and replays the intent. The `intent_refused` kind is not: an unknown `kind` fails deserialization, so a binary older than this commit cannot replay a log holding one, and a rollback across such a log needs a hand fix (stop the engine, roll forward, or truncate the segment). Only the running engine writes the kind: the takeover, rebind, latch-clear, bench and pre-check paths append only their own kinds.
  - **Strategy × venue compatibility.** Every plug declares the venue behaviours its own actions need (`Strategy::execution_requirements`): the native sleeves `submit`, `cancel`, `fill-attribution`, `exact-quantity`, `protection-place`, `protection-change`, `protection-trigger`, plus `post-only` when the config rests entries; `quoter` adds `amend`; `probe` `submit`, `cancel`, `post-only`, `fill-attribution`, `protection-place`; `bench` `submit`, `protection-place` (`cancel` and `post-only` in contention mode). `assembly::compatibility` checks the set against the chosen realm's capability row in `runner.rs` after the sleeve identities are resolved and before the venue, lease, credential or socket exists: a capability whose row is `unknown` ends the run naming the sleeve, plug and realm; one the realm holds no current receipt for is accepted and logged per sleeve as `WARN forward test: this sleeve's execution requirements hold no current live receipt on this realm`. `amend` is absent from every native set on purpose: the resting-entry reprice is the engine's working supervisor's, which MEXC refuses with a WAL `Note` while the entry rests unrepriced for its 30 s window, and LONG runs that way on `mexc_mainnet`. `every_deployed_template_is_compatible_with_its_venue` covers demo, mainnet, mexc and hyperliquid, so the check cannot be what stops a funded engine; the Bybit templates list nothing unproven, the mexc and hyperliquid `long` sleeves list `fill-attribution`, `exact-quantity`, `protection-place`, `protection-change`, `protection-trigger`. No `live-canary` run refusal is re-added. `Capability` moves to `engine-types`; `engine-public` re-exports it.
  - **Caps, rows and adapter-semantics pins.** `adapter_semantics.rs` asserts `VenueCaps` equal to the capability row for every compiled realm (`amend`/`amend_in_place`, `protection-place`/`native_position_stop`, `reduce-below-minimum`/`close_position_below_minimum`), under default features and under each single venue feature; all ten realms agree, no row changed, the generated matrix block is byte-identical. `VenueName::adapter_semantics_fingerprint` pins one sha256 per venue over every non-test `.rs` file under `engine-venue/src/venues/<venue>/`, the source an `observed` receipt was taken against; any change under an adapter fails the suite naming the venue, both fingerprints, the realms holding current receipts, and the review to do (`set current: false` on those receipts if request encoding, order types, quantity conversion or fill interpretation changed; otherwise update the pin). Pins: bybit `dd0edc1a`, binance `bf69d5f3`, hyperliquid `1639dd4e`, lighter `7c94f6fc`, mexc `08b22a3d`, variational `a79c459e`. `current` stays a hand flag.
  - **`engine bench --contention`, measurement only.** `engine bench --contention [--cancel-after N] [--ttl-ms MS]` rests a post-only entry one tick under the bid on each of four symbols, pulls it after 3 quotes, against a mock venue that holds placements for `--venue-delay-ms` (200) and answers pulls at once; 4 000 quotes at 200 Hz; read back from the run's own `VenueTiming` rows by `operation` (`place`, `cancel`) plus the openings the dispatch TTL refused unsent. Release run at the defaults: 102 openings, 102 cancels, 0 expired; cancel wait for the venue task p50 820.8 µs, p90 203.24 ms, worst 204.00 ms (one gateway call plus ~4 ms, never the queue: 51 of 102 waited a full call, 51 went out at once); opening wait p50 401.68 ms, worst 595.52 ms; the cancel call itself p50 268.1 µs. `--ttl-ms 300`: 196 placements, 98 answered, 98 refused unsent. Queue depth is a symbol count, not a rate: the engine holds a symbol busy for its own command, so a cancel reaches the venue task only after its placement was answered. The numbers argue against a venue-task concurrency change. `docs/execution.md` §1 gains the probe's row. The suite's two contention cells end on answered pulls (`BenchOptions::pulls`, twelve, at the `--contention` defaults with the 200 ms venue; `events` is the ceiling) instead of on 400 quotes against a 20 ms venue: the push's `checks` run for `fcfbe9f2` failed `a_cancel_behind_a_slow_opening_waits_for_the_gateway_call_in_flight` with `the workload did not cycle: 15 openings, 2 cancels` on the runner, beside the sim cells' fsyncs. The second deploy run's gate then failed the same cell at 15 openings, 10 cancels in 100 s (four seconds per command: the runner's disk stalling every command's barrier while the sim cells fsync beside it), while the push's `checks` run of the identical commit passed; the bench cells now write their log to memory-backed storage where the box has it (`/dev/shm`), since the venue task's queue is what they judge. The CLI and the measurement path are unchanged.
  - Tests, beyond those named above: S `every_deployed_template_yields_its_blocks_byte_for_byte`, `the_quoter_config_and_tape_do_not_move`, `every_published_row_is_a_row_the_engine_would_accept`, `one_seed_publishes_one_set`, `a_pumped_day_carries_the_rule_s_own_trigger`, `every_carry_batch_carries_the_replay_window`, `a_realm_s_own_blocks_trade_under_the_producer`, `realm_signal_faults_and_a_death_leave_the_log_and_the_sleeves_agreeing`, `one_realm_seed_replays_byte_for_byte_under_heavy_faults`; C `an_intent_written_before_the_cause_field_reads_back_with_no_cause`, `an_intent_with_a_cause_round_trips`, `a_typed_refusal_frame_reads_back_and_round_trips`, `every_engine_event_names_a_cause`, `the_emitted_refusal_code_set_is_exactly_the_pinned_one`, `an_intent_record_names_the_callback_it_was_decided_in`, `every_action_this_supervisor_emits_names_the_order_it_continues`, `a_source_row_is_followed_to_its_order_and_its_fill`, `a_hundred_refusals_inside_one_suppression_window_all_count`, `a_typed_refusal_closes_the_intent_before_the_verdict_and_the_order_after_it`, `a_log_whose_refusals_are_only_notes_counts_them_unresolved_and_says_so`, the `engine-wal` pinned frames; B `a_resting_long_needs_post_only_and_never_an_amend`, `a_resting_carry_needs_post_only_and_never_an_amend`, `a_crossing_exodus_needs_neither_post_only_nor_an_amend`, `the_quoter_declares_the_amend_it_moves_its_quote_with`, `the_probe_declares_a_post_only_entry_with_a_stop_and_no_amend`, `the_bench_workload_declares_only_the_market_order_and_its_stop`, `the_mexc_template_assembles_whole`, `the_hyperliquid_template_assembles_whole`, `every_deployed_template_is_compatible_with_its_venue`, `the_mexc_long_sleeve_boots_owing_its_protection_receipts`, `a_quoter_pointed_at_mexc_is_refused_before_the_venue_is_built`, `every_adapters_caps_agree_with_its_capability_row`, `every_adapters_source_matches_the_semantics_pin_its_receipts_were_taken_against`; E `a_cancel_behind_a_slow_opening_waits_for_the_gateway_call_in_flight`, `a_dispatch_ttl_under_the_queue_refuses_openings_unsent`, `the_two_operations_are_read_apart_and_expiries_are_counted`, `a_quota_hold_comes_out_of_the_cancel_call_span`, `only_the_cancel_path_gets_the_cancel_delay`, `the_contention_flag_brings_its_own_defaults_and_the_flags_narrow_them`, `the_contention_workload_declares_the_pull_and_the_post_only_rest`; the 80 h faults `a_venue_close_of_a_binary64_holding_under_its_grid_value_is_charged_to_its_owner`, `a_requested_row_leaves_the_withhold`.
  - **Simulator fault: a withheld signal row the engine asked for by name was never served.** `FaultySignalFeed` released a withheld row only when a later row of its source came out of the replay feed. After a death the engine learns the hole from the producer's frontier at boot, records the gap and asks the spool for the row, and the replay feed serves nothing past a hole it has been asked to fill, so the successor never arrived and both managed sources stalled for the rest of the run: `sim --strategies mexc --hours 80 --seed 3 --faults heavy` failed `signals_consumed_exactly_once` with `still missing 75 at the end`. The wrapper (`engine-tools/src/sim/faults.rs`) keeps the engine's gap requests, counts a requested row as owed and never withholds a row it has been asked for. Live the row is a file the spool retires only on the engine's acknowledgement, so the real feed re-reads it on the request; the engine's gap path is unchanged. Test `a_requested_row_leaves_the_withhold` (fails on `fcfbe9f2`: the wrapper still owes the row after 60 s); the recipe passes with 175 of 175 rows consumed, and two runs of the seed write one log.
  - Proposed, not built (owner's call): run `assembly::compatibility` in `engine-tools takeover verify` so a bad realm and config pairing is refused before the handover swap; carry each sleeve's unproven list and a drain-mode flag in the heartbeat so the fleet watchdog can see a forward test's owed receipts and a livelocked drain; the `upcoming` scorer in `native_carry/plan.rs` has the same unguarded thin-universe refusal for tomorrow's rows; a `debug_assert!` in `tell_refused` that `code` is in the pinned vocabulary; the real signal worker inside the sim on the virtual clock (the plan's §9 deferral).

- **2026-09-10 — Owner direction: Bybit mainnet stopped, Bybit demo opens nothing, MEXC and Hyperliquid start as a forward test. The `live-canary` boot refusal is gone; readiness stays derived from the matrix and the boot log names what is unproven.**
  - Owner direction 11:50 UTC: stop the Bybit demo and mainnet trading, start mexc and hyperliquid as forward testing to capture all errors and fix them.
  - `deploy/realms.tsv`: `mainnet posture=stopped` (the deploy stops and disables its engine, worker, liveness timer and the `execution-study` pair; `REAL_MONEY` stays armed and nothing is flattened: the INJUSDT long 4.6 with its native stop 5.805 stays on the venue, unmanaged); `demo` entries `false|false|false` (the practice realm stays `running` for the deploy soak, manages its INJUSDT 59.5 long's exit and keeps PROBE); `mexc` and `hyperliquid` `posture=running`. No generated file changed.
  - `engine/engine-public/src/registry.rs`: `VenueReadiness::permits_engine_run` accepts `LiveCanary`; `production-blocked` and `read-only` still refuse, and readiness is still derived from the capability row, never assigned. `engine/engine-core/src/runner.rs` logs `WARN forward test` at boot with the unproven capabilities. `scripts/vps/deploy_remote.sh` `realm_run_ready` accepts `live-proven|live-canary`; the unready line reads `units stay stopped, the engine refuses to run at that readiness`. `scripts/ops.sh` `CANARY_REALMS` is the practice realm plus every funded realm on a venue with no practice sibling, no longer read from posture.
  - Canaries on the installed `6e5ca627`, both PASS. MEXC 11:59:17–11:59:29 UTC: `client_id=lmcan-1a08b2fd4a5-087e-0000 venue_order_id=853000482766018560`, `order_px=77462.1 qty=0.0001 stop=65842.8` under `bid=77851.4 ask=77851.5`, `create=accepted`, `private_order=New`, `cancel=accepted`, `private_order=Cancelled`, `order_status=Cancelled cumulative_filled_qty=0`, four clean scans. Hyperliquid 11:59:20–11:59:28 UTC: `client_id=lmcan-1a08b2fcd82-08b6-0000 venue_order_id=541177774027`, `order_px=77460 qty=0.00014 stop=65841` under `bid=77850 ask=77851`, the same lifecycle. Both matrix rows carry `observed 2026-09-10` for submit, cancel and post-only; fill-attribution, protection-place, protection-trigger and reconnect-history-recovery stay `implemented`, so both realms remain `live-canary` and the forward test is what gathers them.
  - Followers of the table: `deploy/grafana/liquidity-migration-fleet.json` re-rendered (the realm variable narrows to `demo|mexc|hyperliquid`); the Telegram fleet status line for a funded realm with no owner heartbeat reads `no heartbeat, entry state unknown` instead of `not armed`, because a stopped realm can still be armed.
  - Tests: `a_submit_cancel_canary_does_not_qualify_general_protected_position_trading`, `the_alt_realms_owe_what_a_submit_cancel_canary_cannot_show` (engine-public); `production_readiness_is_explicit_for_every_registered_realm`, `a_live_canary_realm_takes_the_canary_and_runs_as_the_owners_forward_test`, `conformance_local_fixtures_do_not_promote_dormant_realms` (engine-venue); `only_the_practice_realm_and_a_live_canary_realm_can_reach_the_command` (engine-tools); `test_a_funded_handover_waits_for_a_readiness_the_engine_will_run`, `test_posture_drives_the_deploy_and_the_table_names_the_realms_that_run`, `test_canary_order_keeps_the_arming_switch_and_refuses_the_funded_bybit_account`.
  - The Mac's disk, not the host's: the pre-push gate hit ENOSPC on 7 GiB of headroom because `engine/target/debug/deps` held 95.2 GiB across 236,051 files, almost all the ten workspace crates' own debug artifacts from repeated gates (third-party dependency builds were 2.2 GB); the host's hourly `storage-reclaim` never covered the Mac. `scripts/dev.sh prune` now runs `cargo clean --profile dev -p` for every workspace member and drops `target/debug/incremental`, and `check` runs it first when the target volume has under `LM_TARGET_FREE_GIB` (30) GiB free. Tests `test_dev_prune_cleans_each_workspace_member_and_keeps_dependency_builds`, `test_dev_check_prunes_under_the_target_volume_floor_before_the_rust_stages`.
  - [Deploy run `34477667791`](https://github.com/rob435/liquidity-migration/actions/runs/34477667791), dispatched 12:36:24 UTC on `e7f3ca52`: `ci` 12:38:52, `rust` 12:47:27, release artifact 12:50:25, `vps` 12:50:29–12:57:01 **failed**. Demo handed over to `e7f3ca52` and soaked 300 s (12:51:41–12:56:41, the rolling-loss NOTICE only); `mainnet posture=stopped in deploy/realms.tsv: units stay stopped` at 12:56:44 (engine, worker, liveness timer and `execution-study` pair disabled and inactive); then `atomic mexc handover` 12:56:45 failed at 12:56:59 with `mexc native checkpoint configuration change is incompatible`. Rollback refused (`6e5ca627` has different runtime inputs); `e7f3ca52` stayed installed for a forward repair, `deployed-commit` stayed `6e5ca627`; `mexc-liveness.timer` was re-enabled by `restore_realm_timers` and has paged CRITICAL on the stopped realm since 12:57:41 (Telegram on the 30 min cooldown; the incident routine answered `Routine is paused`). Hyperliquid was never reached. Incident `mexc` realm down, open from 12:56:59 UTC.
  - Root cause, two halves. (1) `ensure_native_strategy_state` offered the last deployed commit's retained render (`checkpoint-configs/6e5ca627/engine.mexc.toml`) as the config the realm's state was written under; the realm last ran on `7f0214f7` (2026-09-09 08:52–15:27 UTC) and sat stopped through five later deploys that each re-rendered it, so the engine answered `checkpoint identity (1, "a9efbf93…") does not match configured (1, "32814936…")`. (2) With the right render (`7f0214f7`'s) the rebind still refused, `checkpoint rebind changes "carry" decision rules`: the mexc worker moved to MEXC's own public data on 2026-09-09 (`42dd7446`), which changed the carry `feature_contract_sha256` (`d9a96b4c…` → `32246961…`), part of CARRY's decision identity, while the realm was stopped. The carry sleeve on MEXC holds no exposure, no order in flight and no callback work (heartbeat 2026-09-09 15:27: `positions []`, `working_entries []`, `fills 0`), so there was nothing the old checkpoint protected.
  - Fix. `engine-tools/src/takeover/rebind.rs`: `compatible_config` answers whether the rules changed instead of refusing; a sleeve whose rules changed is re-initialised with its strategy's canonical initial checkpoint under the new identity when the replayed log attributes it nothing (`Attribution::rows`, `LedgerOfOrders` in-flight orders, `CallbackState` committed processes and queued inputs), printed as `reinitialized "carry": decision rules changed while it held no exposure, working orders or callback work`; one that holds anything is refused with `changes … decision rules while it holds exposure or pending work`; a wider EXODUS stop stays refused. `scripts/vps/deploy_remote.sh` `ensure_native_strategy_state` tries every retained render (`retained_realm_configs`: the deployed commit's first, then newest first) with a dry-run rebind and executes the first the engine accepts, printing `result=rebound previous=PATH`. Tests: `a_rule_change_on_a_sleeve_that_holds_nothing_reinitialises_its_checkpoint`, `a_rule_change_on_a_sleeve_with_exposure_or_pending_work_is_refused`, `rebind_refuses_unknown_source_identities_and_a_reordered_sleeve_table` (engine-tools); `test_native_rebind_tries_every_retained_render_and_keeps_the_one_the_engine_accepts`, `test_native_rebind_uses_retained_config_without_initializing_state` (deploy). Verified before the deploy on a copy of the realm's WAL with the new binary: the `6e5ca627` render is refused on identity; the `7f0214f7` render re-initialises carry and long (LONG's `feature_contract_sha256` moved with the same worker change, `1b9f6fc2…` → `2d80f005…`, and the old binary had stopped at carry) and reports `compatible checkpoint rebinds 2`; the installed `e7f3ca52` binary refuses the same render on the rule change. The log holds 1,472 records and no fill at all (`engine-tools fills`: `nothing has filled yet`), so neither fresh checkpoint discards a cooldown, an anchor or a holding.
  - [Deploy run `34482182228`](https://github.com/rob435/liquidity-migration/actions/runs/34482182228), dispatched 13:21:56 UTC on `ceac3c9b`: `ci` 13:24:39, `rust` 13:33:41, release artifact 13:37:23, `vps` 13:37:28–13:48:40 **failed**, one step further. Demo handed over and soaked 300 s; mainnet stayed stopped; the mexc handover's `ensure_native_strategy_state` walked the retained renders, the `7f0214f7` render re-initialised carry and long (`reinitialized "carry"`, `reinitialized "long"`, `compatible checkpoint rebinds 2`, `native-state-ok realm=mexc result=rebound previous=…/7f0214f7…/engine.mexc.toml` at 13:44:54) and the mexc engine started on `ceac3c9b` (pid `4032860`, `WARN forward test … unproven="fill-attribution, protection-place, protection-trigger, reconnect-history-recovery"`, `may_open=true`, `private_stream_ready=true`, lease taken 13:45:01). The mexc signal worker then exited five times in 40 s with `signal-worker: state: checkpoint public source contract has drifted; a new cold start is required` (exit 2) until systemd stopped restarting it at 13:45:33, and the deploy failed at 13:48:37 with `liquidity-migration-signal-worker-mexc.service restarts after each heartbeat`. Rollback refused again; `ceac3c9b` stayed installed; the mexc engine kept running with no signal source; Hyperliquid was not reached.
  - Root cause, the worker's half of the same 2026-09-09 change: `/var/lib/liquidity-migration-signal-worker-mexc/checkpoint.json` (74 MB, 2026-09-09 15:01 UTC) and `hot-input-journal.jsonl` were written under the Bybit public-source contract; `SignalWorker::restore` refuses a checkpoint whose `source_contract_sha256` is not the config's, and nothing performed the cold start it asked for, so the unit crash-looped. Fix in `engine/signal-worker/src/worker.rs`: `DurableSignalWorker::open_with_universe` archives a checkpoint whose contract drifted, with the journal and both pending files, into `<state_dir>/drifted-source-<sha8>-<unix_ms>/`, prints `checkpoint public source contract drifted from … to …; archived … and rebuilding the history under producer generation …`, and seeds a fresh checkpoint that keeps the producer identity the engine's registry holds (`source_generation`, `signal_lifecycle` epoch, both destinations, `last_input_sequence`, both output sequences) while the history, coverage and features start over. Test `a_checkpoint_from_another_public_source_is_archived_and_the_worker_cold_starts` (fails on the unfixed worker with the exit-2 refusal).
  - The first cut of that fix (`1d526b8d`, [deploy run `34486402411`](https://github.com/rob435/liquidity-migration/actions/runs/34486402411), `deploy-ok` 14:22:40 UTC) minted a random generation at the cold start. The mexc worker then sat at `waiting for engine destination registry: state: successor grant does not follow the durable producer seal`, `status=starting`, universe empty, and the 173 old spool files untouched: the engine's request still named `ff7ad772…` at epoch 1 as the active unsealed producer, `plan_producer_report` has no path to adopt an unknown generation while that one is unsealed, and the worker refuses to answer a grant that is not its own, so neither side could move. Repair on the host before the next handover: the mexc worker stopped, its random-generation checkpoint moved aside, and the archived `ff7ad772…` checkpoint restored to `checkpoint.json` so the corrected binary rebuilds under the identity the registry knows.
  - Hyperliquid, first minutes on `1d526b8d` (engine pid `4048771`, up 14:22:14 UTC, `may_open=true`, `private_stream_ready=true`, equity 52.4 USDC, no position): the worker's first universe refresh logged `universe refresh left a sleeve with no eligible symbol` and its retry a few seconds later succeeded (`status=ready`, 178 listed symbols, 2 LONG and 4 CARRY eligible, ticker coverage complete, carry outputs flowing). The engine then reported `strategy_errors: carry: CARRY decision universe has fewer than 50 symbols` on every carry batch, and `hyperliquid-liveness` paged CRITICAL on it from 14:31:30 UTC every 30 s (Telegram on the 30 min cooldown; routine paused). Root cause: `native_carry/scorer.rs` refuses a decision universe under `MIN_DECISION_SYMBOLS` (50) as an error, and the plug records it as a strategy fault, even for a sleeve whose entries are rendered off and that holds nothing, which is CARRY on both alt realms by design. Fix: `plan.rs` `reduce_signal` and `reduce_scorer_catchup` consume the batch without a decision when the universe is thin, entries are off and the sleeve holds no exposure, no working order and no opening order (`Skipped::DecisionUniverseBelowMinimum { symbol: "*", symbols }`, shown in the heartbeat's `entry_blockers` as `decision_universe_below_minimum`); with entries on or anything held the scorer's refusal stands. `ScorerCatchupInput` gains `holds_nothing`. Test `a_thin_decision_universe_is_consumed_without_a_decision_only_when_off_and_flat`. LONG's 2 eligible symbols are the cold start's kline backfill (`cold_start_lookback_days` 100 across 178 symbols) and are re-read after the backfill. The thin-universe path moves on in the Tier 1 entry above: the day after a consumed thin day (`c52f70ef`).
  - [Deploy run `34490257039`](https://github.com/rob435/liquidity-migration/actions/runs/34490257039) on `8d9915aa` (the identity-preserving cold start) was cancelled at 14:48 UTC before its host job started, so the CARRY fix below could ship in one handover instead of two.
  - Deploy receipt: [run `34491454825`](https://github.com/rob435/liquidity-migration/actions/runs/34491454825), dispatched 14:48:21 UTC on `859723b1`: `ci` 14:50:45, `rust` 14:59:34, release artifact 14:59:54, `vps` 14:59:59–15:07:57. Demo handed over (engine pid `4062319`, worker `4062227`) and soaked 300 s to 15:06:09; `mainnet posture=stopped in deploy/realms.tsv: units stay stopped` 15:06:13; mexc handover 15:06:14, `native-state-ok realm=mexc result=already-complete`, worker pid `4065991` (`checkpoint public source contract drifted from 37a18797… to 68e59719…; archived …/drifted-source-37a18797-1789052783211 and rebuilding the history under producer generation ff7ad772…`), engine pid `4066050`; hyperliquid handover 15:07:19, `native-state-ok realm=hyperliquid result=already-complete`, worker pid `4066983`, engine pid `4067043`; `deploy-ok commit=859723b1…` 15:07:46, `rollback-target 1d526b8d…`, `mainnet readiness=live-proven`, `mexc readiness=live-canary`, `hyperliquid readiness=live-canary`.
  - Verified on the host 15:09 UTC: `deployed-commit` `859723b1`; every fleet timer and both recorders `enabled active`; mainnet engine, worker, liveness timer and `execution-study` pair `disabled inactive`; `systemctl --failed` empty; watchdogs `ok scope=demo warnings-present-no-critical`, `ok scope=mexc units-and-heartbeats-healthy`, `ok scope=hyperliquid units-and-heartbeats-healthy`, `ok scope=host units-and-heartbeats-healthy`. MEXC: engine `may_open=true`, `private_stream_ready=true`, `strategy_errors=[]`, equity 30 USDT, no position, LONG entries on; the engine drained the 173 stale spool files (7 unlisted names dropped: `1000PEPEUSDT`, `ARCUSDT`, `FILUSDT`, `MONUSDT`, `RAYDIUMUSDT`, `SHIB1000USDT`, `TRUMPUSDT`) and the worker answered the lifecycle request at 15:09:10 as generation `ff7ad772` epoch 1 with frontiers long 21 / carry 208, `status=recovering`, 1,024 listed symbols, 119 LONG and 149 CARRY eligible. Hyperliquid: engine `may_open=true`, `private_stream_ready=true`, `strategy_errors=[]`, equity 52.4 USDC, no position, LONG entries on; worker `status=ready`, 178 listed, 2 LONG / 4 CARRY eligible, sequences long 4 / carry 629. Demo: engine `may_open=true`, every sleeve's entries off, no position (the INJUSDT 59.5 long exited during the afternoon; `orders_sent 1 fills 1` since its 15:00 boot is the probe). Bybit mainnet, read on the venue by `attest-flat` 15:09: INJUSDT long 4.6 with its native stop order still working, plus wallet dust (SOL, WLD, GRAM, MNT) and 2.4308 USDT in Easy Earn; nobody manages the exit.
  - Hyperliquid worker, two boot faults fixed (owner: "hyperliquid uses USDC pairs"; the engine's spelling stays `BTCUSDT`, the venue's is `BTC`, settle coin USDC). (1) `kline repair through …: Hyperliquid lists no coin for BTCUSDT` (also `ETHUSDT`, `ATOMUSDT`): the venue's symbol-to-coin table is filled only by a `meta` read, and a boot that restores its universe from the checkpoint never made one before the first kline repair. `HyperliquidPublicVenue::coin_of` now reads `meta` itself when the table is empty, once per process. (2) `universe refresh left a sleeve with no eligible symbol` once per boot: launch times come from a one-coin-at-a-time listing-history pass that started empty on every boot, so the first refresh saw no ages and both sleeves fell under `min_listing_age_days`. `PublicVenue::seed_listing_history` (default no-op) lets the live runner seed the venue from the checkpoint's `instruments[*].launch_time_ms` at start, so a warm boot has its ages and re-reads no coin. Tests `the_first_lane_read_fills_the_coin_table_from_meta`, `a_seeded_listing_history_is_not_read_again`; `a_coin_the_venue_stopped_listing_fails_only_its_own_lane` now seeds the table it expects a miss against. The one `HTTP 429` on Hyperliquid account recovery (15:08:59 UTC) was retried by the existing path and needs no change.
  - `attest-flat` and the Bybit inventory scan (owner: "if wallet balance is tiny then just say its flat"). Before: every non-cash wallet coin and every positive USDT holding outside the unified or funding wallet was a position, so mainnet's SOL 0.00004797, WLD 0.0042, GRAM 0.0232, MNT 0.00012169 and 2.4308 USDT in Easy Earn each blocked the proof. Now `parse_inventory_wallet` reads the venue's own `usdValue` and labels a positive holding under `DUST_USD_VALUE` (1 USD, under the venue's smallest sellable value) `wallet_dust`; `label_dust_holdings` gives the same coin's asset-overview rows `asset_account_dust:*`; positive USDT/USDC is cash in every account type except `TradingBot` and `CopyTrading`, where it is deployed capital and stays inventory; `AccountInventory::is_flat` ignores dust and `attest-flat` prints each dust row as `flat-dust …` before saying flat. A holding without a venue valuation counts in full. Canary guards ignore dust like wallet assets. Tests `wallet_inventory_ignores_cash_but_keeps_assets_and_liabilities`, `a_dust_coin_is_dust_on_every_surface_that_lists_it`, `dust_alone_reads_flat_and_anything_else_does_not`; the two asset-overview tests carry the cash rule. MEXC and Hyperliquid balance endpoints report no USD value, so their spot rows still count in full.
  - [Deploy run `34500835827`](https://github.com/rob435/liquidity-migration/actions/runs/34500835827) on `446d9f61`, dispatched 16:14:33 UTC: demo handed over and soaked; the mexc handover installed the binaries, then `liquidity-migration-signal-worker-mexc.service` exited five times in 30 s with `state: MEXC stream needs the contract table, which no lane has read yet` (exit 2) and the deploy failed at 16:36:16 (`did not publish a fresh heartbeat`); `446d9f61` stayed installed, `deployed-commit` stayed `859723b1`, Hyperliquid was not reached. Root cause, the same ordering fault as the Hyperliquid coin table: live mode never runs `bootstrap()`; `run()` resolves the universe (a no-op when the checkpoint has one), opens the public stream and starts the first kline repair before the instrument lane has filled the venue's process-local tables. The 15:06 MEXC boot escaped it only because its fresh state made `resolve_universe` read instruments. Fix: `LiveRunner::read_instruments_before_the_stream` reads the instrument page pair once, with the universe resolve's retry and backoff, before `open_stream`; the Hyperliquid lazy `meta` read stays as a second line. Verified on the host by the next handover.
  - Hyperliquid worker `degraded` from 16:07 UTC on `859723b1`: its public stream re-entered at epoch 2 and the repair gap never closed (`Bybit WebSocket repair gap open for 1735s; carry cycle is 1797s old`, `hyperliquid-liveness` CRITICAL every 30 s from 16:36:19). Root cause: `first_missing_kline_hour` judged a hole by candle presence alone, so an hour the venue has no candle for (CASHCATUSDT, four hours 61 days back; PONSUSDT, three hours 10 days back, both inside their checked coverage) was a repair job on every pass, `kline_repair_jobs` was never empty, `complete_repair_finished` never called `mark_gap_repaired`, and the carry watermark never advanced; the lane looped silently because nothing failed. Fix in `live/acquisition.rs`: an hour inside a checked coverage interval that came back without a candle is venue-empty, not missing. Test `a_checked_hour_without_a_candle_is_not_a_hole`.
  - `attest-flat --environment mainnet` with the installed `446d9f61` binary, 16:43 UTC: `flat=true samples=2 positions=0 open_orders=0`, with the four dust coins printed as `flat-dust` rows on both wallet and asset-overview surfaces and the Easy Earn USDT read as cash. The INJUSDT long 4.6 and its stop order were no longer on the venue: the owner closed them by hand between 15:09 and 16:43.
  - [Deploy run `34504641929`](https://github.com/rob435/liquidity-migration/actions/runs/34504641929) on `362e69ba`, dispatched 16:51 UTC: demo handed over and soaked; the mexc handover installed the binaries and its worker booted warm and healthy for the first time (`heartbeat-ok … signal-worker-mexc … pid=4102592` 17:08:27, `status=ready`), so `read_instruments_before_the_stream` holds; the hyperliquid handover then failed at 17:08:56 on `published an unhealthy heartbeat after startup`: the deploy's one sample, 14 s after start, landed in the seconds the worker's own instrument-lane reconfigure had its stream down (`reports 'degraded': Bybit WebSocket disconnected; repair gap open for 9s`), and the watchdog read `ok` from 17:09:07 on. `362e69ba` stayed installed, `deployed-commit` stayed `859723b1`. Fix: `wait_fresh_heartbeat` samples the heartbeat up to `HEARTBEAT_UNHEALTHY_SAMPLES` (5) times, one settle apart, and prints `heartbeat-unhealthy … sampling again` between them; a unit that stays unhealthy still fails. Tests `test_the_heartbeat_gate_samples_again_when_the_first_heartbeat_is_unhealthy`, and the always-unhealthy cases now pin four resamples before the refusal.
  - Hyperliquid worker, third boot fault, seen on the `859723b1` and `362e69ba` processes alike: `kline repair lane chunk: input: kline history rewrote timestamp 1789056000000`. The 16:00 UTC candle for HYPEUSDT closed on the stream at 79.933 with volume 1,203,746.79 and the venue's settled REST candle says 79.937 and 1,204,275.03; the other 148 symbols agree. `validate_kline_source_against_state` refused any row that differed from a held hour, the repair chunk aborted its lane, the hour stayed a repair job, and the gap could not close, the same stall as the venue-empty hours. Fix in `live.rs`: `reconcile_kline_source_against_state` keeps the held row, drops the restated one, and returns the list; both the repair lane and the pending stream-kline flush use it, the flush before it stamps coverage; the repair summary prints `N restated closed candle(s) kept as first seen: SYMBOL@ts`. A fetch that contradicts itself is still refused. Tests `a_restated_closed_candle_is_kept_as_first_seen_and_named`; `revised_source_history_is_rejected_before_durable_mutation` now pins that a restated row writes nothing and stops nothing.
  - Deploy receipt: pending.

- **2026-09-10 — Storage headroom for the recorders: tape window 6 h, recorder floor 12 GiB, release keep 1 day, and the host watchdog warns on a stale reclaim receipt.**
  - Owner direction (scheduled task, applied 03:34–04:12 UTC): apply every lever and keep the recorders writing. Before, at 03:34 UTC: 34.9 GB free on `/` (125.7 GB), low water 33.29 GB (tape floor 25 GiB + 6 GiB writer headroom), the 24 h tape window holding 20 GB of Bybit and 2.8 GB of Binance tape, 38 release directories (2.3 GB) with their staged tarballs (0.9 GB), 27.7 GB of verified sealed WAL retained across 103 segments, backlog 0, receipt 02:41:31 UTC.
  - `deploy/systemd/liquidity-migration-market-tape-upload.service`: `--keep-hours 24` → `6`. Drive keeps every hour permanently either way; a shipped hour now leaves the host six hours after it ended. About 15 GB returned over the first upload runs.
  - `deploy/capture/bybit-linear.toml`, `deploy/capture/binance-usdm.toml`, and the `market_tape.config.StorageSettings` default: `min_free_disk_gb` 25 → 12. The reclaimer reads the floor from the configs, so low water becomes `max(reserve 14.04 GiB, 12 GiB) + 6 GiB` = 21.5 GB: the engine reserve binds, and the gap between the reclaimer's mark and the recorders' floor widens from 6 GiB to 8.6 GB, over the 7.4 GB quarantine dump of Sep 6. Each recorder restarts once, by the deploy's `capture_fingerprint`.
  - `scripts/runtime/reclaim_host_storage.py`: `--release-age-days` 3 → 1.
  - `scripts/runtime/check_fleet_liveness.py`: `evaluate_reclaim_stamp`, alert key `storage-reclaim`, WARNING when `receipts/storage-reclaim.last-success` is missing or older than `--max-reclaim-age-hours` (3 h: the reclaimer stamps only a clean hourly run, so two failed runs in a row page and one does not). `deploy/systemd/liquidity-migration-host-liveness.service` passes `--reclaim-stamp-file`. Test `test_reclaim_receipt_ages_into_a_warning`; `test_the_flagless_defaults_are_the_ones_the_deployed_unit_relies_on` pins the new floor and keep.
  - Not applied: `/opt/liquidity-migration/data` (1.7 GB) is not research data. It is the signal workers' event roots (`deploy/lib_realms.sh` `long_root`/`carry_root`/`exodus_root`, owned by `liquidity-signal-worker`: `event_demo_klines_1h` 1.2 GB, `reports` 224 MB, `.cache`, the strategy event tapes), and `scripts/vps/deploy_remote.sh` (`ensure_native_strategy_state`) falls back to `carry_sizing_anchors.json` and `exodus_state.json` there when a realm's native strategy state does not verify. Nothing under it has been written since 2026-09-01 12:23 UTC and the workers' `ReadWritePaths` no longer include it. Deleting it is the owner's call, not a storage lever.
  - Deploy receipt: [run `34434792960`](https://github.com/rob435/liquidity-migration/actions/runs/34434792960), dispatched 03:49:22 UTC on `6e5ca627` (push 03:48:56 behind a green pre-push gate), `ci` 03:52:09, `rust` 04:00:24, release artifact 04:03:25, `vps` 04:03:29–04:11:54: both recorders `result=restarted` (Bybit 04:04:52 with `capture-ready` in 2 s, Binance 04:05:31 in 0 s), the 300 s demo soak from 04:06:06, `atomic mainnet handover` 04:11:08 with both INJUSDT longs held through it, `deploy-ok` 04:11:44, rollback target `ae95fedf`. The 04:10 upload already ran on the new unit: `keep_hours=6.0`, `pruned_hours=38`, `pruned_bytes=15671418894`; free 34.9 → 53.1 GB. `storage plan` at 04:13: low water 21.52 GB (the 15.08 GB reserve binds over the 12.88 GB tape floor), high water 33.99 GB, runway 437,010 s, backlog 0, 30 release directories and 30 tarballs (2.6 GB) due at 04:41. Host watchdog 04:12:35: `ok scope=host units-and-heartbeats-healthy` with the receipt row live. 04:41 run on the new floor: exit 0, `reclaimed_bytes=2615627776` (30 release directories and 30 tarballs; 9 of each remain), `wal_segments_reclaimed=0`, `unverified_backlog_bytes=0`, `estimated_runway_s=438480`, receipt 04:41:31 UTC; free 54.9 GB at 05:04.

- **2026-09-09 — Alt-realm readiness, 23:16–23:25 UTC: MEXC's identity is bound and proven flat; Hyperliquid's second canary placed and cancelled a real order the venue confirmed, but the harness could not recognise its own order because the adapter hashed the canary's client id. Fixed in the cloid codec; both reruns are the owner's.**
  - MEXC. The owner supplied account UID `19445654`. Written on the host at
    23:17 UTC: `/etc/liquidity-migration/mexc-account-bindings.json` (root,
    0644, schema 1, the key's sha256 fingerprint bound to `19445654`) and
    `EXPECTED_ENGINE_ACCOUNT_USER_ID=uid-19445654` in `engine-mexc.env` (the
    retired `key-…` value backed up at `/root/engine-mexc.env.bak-20260910`).
    `verify-account-identity` answers `account-identity-ok account=uid-19445654`;
    `attest-flat` reads `flat=true samples=2 positions=0 open_orders=0`;
    `real-money preflight-mexc` passes every precondition. The owner's canary
    at 23:24:22 reached `private_feed=ready` and was stopped by systemd one
    second later when the operator's shell disconnected (`Stopping
    liquidity-migration-canary-order-mexc-3838236.service`); no order was
    created, and `attest-flat` at 23:26 reads flat again. Rerun pending.
  - Hyperliquid, canary run 2 at 23:24:37 UTC. `verify-account-identity`
    answers `0xcef3cc6085897672efc4bf5d8401f757c9b85b17`; preflight passes;
    `attest-flat` reads not flat on `wallet_asset HYPE Buy 0.00230994` in the
    spot wallet (the credential-wide scan counts wallet assets; the canary's
    precheck is derivative-and-order flatness and does not). The canary
    priced `order_px=77698 qty=0.00014 stop=66044` against `bid=78089
    ask=78090`, the venue accepted the create (`venue_order_id=540726821427`)
    and the cancel, `orderStatus` by oid read `Cancelled cumulative_filled_qty=0`
    and four cleanup scans read `derivative_positions=0 open_orders=0`. The
    command still ended in `create returned an id, but neither the private
    feed nor open-order inventory proved New`: no `New`, no `Cancelled` on the
    private feed, no match in `frontendOpenOrders`.
  - Root cause, fixed. Hyperliquid's client id is a fixed 16-byte `cloid`.
    `engine-venue/src/venues/hyperliquid/cloid.rs` packed only
    `eng-<ms>-<n>` reversibly and hashed every other shape one way, so the
    venue's `orderUpdates` rows and open-order list came back with a cloid
    `from_cloid` could not read, the feed dropped them as not ours, and the
    inventory row carried the raw hex instead of `lmcan-…`. Everything keyed
    by the venue's own oid worked, which is why the order cleaned up. The
    canary's `lmcan-<hex ms>-<pid>-<nonce>` and `lmcls-…` ids are now packed
    under scheme bytes `0x03` and `0x04` with a five-byte zero tail as the
    proof of origin, and print back byte for byte or are not packed. Engine
    ids and the hashed fallback are unchanged. Tests:
    `a_canary_id_survives_the_round_trip`,
    `a_canary_shaped_id_that_would_print_back_differently_is_not_packed`,
    `a_stranger_id_that_starts_with_the_canary_scheme_byte_is_not_ours`
    (`cloid.rs`), and `a_canary_client_id_packs_into_the_cloid_and_looks_itself_up`
    (`tests/venue/hyperliquid_requests.rs`, which pinned the hashed
    behaviour and failed on the fix until rewritten). `hyperliquid_mainnet`
    and `mexc_mainnet` stay `live-canary` until a rerun passes on the
    deployed fix.

- **2026-09-09 — Audit deferred items completed: restart markouts (F20), opportunity cohort (F16), production day reconciliation (F17), and research standards (F18, F19, F21).**
  - **F20, owed markouts**: `Fills.pending` restated at rotation as `SegmentBase.owed_markouts` (`engine-types/src/wal.rs`, `#[serde(default)]`) and rebuilt at boot for fills younger than 305 s. `filled_ns` signed stamp reconstructed from venue `fill_ts_ms` against wall clock. Fills report splits late marks into restart-gap and stall. 10 execution/fill-cost tests pass; `engine sim --twice` passes on seeds 7, 26, 31, 42, 166.
  - **F16, opportunity cohort**: Added `engine-tools cohort --wal PATH [--json]` (`engine-tools/src/cohort.rs`). Tracks source rows keyed by `(destination, source, sequence, observation_id)` and order intents by `intent` with settle status. Reports latencies at p50/p90/p99/p99.9. Mainnet 2026-09-08 census (segments 000064–000074): 52 source rows consumed, 11 intents (9 sent, 2 refused StaleQuote), decision-to-wire p50 0.755 ms across 114,809 records.
  - **F17, production day reconciliation**: Implemented `python -m liquidity_migration.research.day_reconciliation` bounding samples between midnight readings against venue transaction logs and copied WAL segments. Same-ms venue rows chained by `cashBalance - change`. Equity recorder (`engine-tools/src/equity_recorder.rs`) updated to write `unrealised_pnl_usdt`, `wallet_cash_usdt`, and `positions`. Mainnet 2026-09-08 reconciled with 0 cash residual.
  - **F18, F19, F21, research standards**: Registered maker experiment `lane2_toxic_flow_quoter_v1` (net -0.171 bp vs -0.248 bp control), execution comparison standard (fixed cohort, decision-time benchmark, unfilled quantity at common horizon), and cross-venue signal standard in [governance.md](docs/research/governance.md). Added rules #36 and #37 to `docs/research/backtesting_errors_we_never_repeat.md`.
  - **Deferred**: F13/F14 fleet capital coordinator deferred per Milestone E sequence pending alt-realm qualification.
  - **Deploy receipt**: Commit `e899ea21` deployed via run `34412394718` at 22:28:33 UTC. Both Bybit engines active on host (`may_open=true`, `strategy_errors=[]`); all watchdogs healthy.

- **2026-09-09 — `floor_usdt` removed from capital reference on owner direction.**
  - Capital reference and derived caps follow verified equity down; 100 USDT in profile serves only as scale reference.
  - Removed `EnvelopeConfig::floor_usdt`, `Envelope::viable_for_new_exposure()`, and `require_viable_reference()` (`engine-risk/src/{config,envelope,kernel}.rs`). `capital_reference.floor_usdt` removed from profile schema (`operational_profile.py`, `real_money_profile.py`, `configs/operational.json` sha256 `0dd6be5a...`).
  - Retained `DenyReason::LossGuardTripped { equity_usdt, floor_usdt }` in `engine-types/src/risk.rs` for log reader backward compatibility.
  - Verified by contracts in `engine-risk/tests/contracts/envelope.rs` and `tests/research/backtest/test_long_live_physics.py`.

- **2026-09-09 — `engine sim` heavy sweep: fixed three replay and accounting discrepancies.**
  - **Root cause 1**: Attribution fill reducers diverged for `amounts: None` fills. Replay (`commit_prepared`) dropped sub-FLAT dust rows, while live path (`commit_portfolio_fill`) retained them, causing sleeve-stop validation failures on seed 7. Fixed by sharing `commit_prepared` legacy tail in `commit_portfolio_fill`.
  - **Root cause 2 & 3**: Recorded allocations over binary64 fills without `ExecutionAmounts` were treated as exact quantities by `commit_portfolio_fill`, `Lots::on_fill_with_economics`, and `position_state_with_adoption`. Boot grid adoption left a floating-point residual (`1/180143985094819840`), causing FIFO rederivation failures (seed 42) and ledger mismatches (seed 166). Fixed by treating binary64 quantities as legacy regardless of durable allocation and settling sub-FLAT residues.
  - Verified: Seeds 1–44, 123, 166, 240, 243, 247 pass `--seconds 300 --symbols 2 --crashes 2 --faults heavy --twice` with byte-for-byte replay agreement. No funded log contains `amounts: None` fills.

- **2026-09-09 — Source audit first hardening batch (commits `98a4be7a`, `4c531869`, `1e2cfc22`, `f6620ff9`).**
  - **F05, asynchronous leverage dispatch** (`98a4be7a`): Removed synchronous `set_leverage` from account owner loop. Leverages dispatched asynchronously via `VenueClient::dispatch_leverage` → `MutationCompletion::Leverage`. Orders with `exact_terms` require exact cumulative fill equality to retire.
  - **F11, equity contraction** (`4c531869`): Reference follows verified equity down (superseded by removal of `floor_usdt`).
  - **MEXC hardening** (`1e2cfc22`):
    - *F01, account binding*: Registry at `/etc/liquidity-migration/mexc-account-bindings.json` validates `uid-<account_uid>` identity before socket creation or mutation.
    - *F04, classified rate pacing*: `mexc/rest.rs` pacer reserves 4 protective slots out of 16 for emergency order cancels and stop reductions.
    - *F06/F07/F08/F24*: Integer leverage validation, separate opening eligibility from cleanup, and removed unproven `live-proven` claim from MEXC.
  - **F10, archive qualification** (`f6620ff9`): Deployable archives enforce embedded qualification evidence for candidate binaries.
  - **F02, F03, F09, F12, F15**: Pre-wire authority validation, priority queue draining in `venue_runtime`, capability matrix readiness, stop charge naming, and backtest evidence metadata.

- **2026-09-09 — Host storage reclamation and sealed WAL pruning.**
  - Implemented `scripts/runtime/reclaim_host_storage.py` running hourly as root systemd service.
  - Sealed WAL segments below `engine_wal::retention_floor` pruned once verified against Google Drive backup manifests under `engine-state/sealed/`. Pruning budgets free space via `statvfs`: reserve `max(12 % of capacity, 8 GiB)`, low water a 6 GiB writer headroom above it. Old release packages, temporary files, and apt caches cleaned automatically.
  - Deploy receipt: Installed on host under `42dd7446`. First run reclaimed ~3.2 GB of disk space. It exited 1 on the hyperliquid family, which has no log; fixed in `14311d1c` (`no log yet`, not an error), on the host only at 22:43 UTC with `e899ea21`, so the 16:41–22:41 runs exited 1 too.
  - Low water sat under the tape recorders' floor (fixed in `930d3be2`, deploy-ok 23:30:37 UTC): 20.04 GiB against `min_free_disk_gb = 25` in both `deploy/capture/*.toml`, so a recorder under 25 GiB counted every frame and wrote none while its own retention pass freed tape, 5 GiB before any verified WAL was reclaimed. That is the `disk_dropped_frames` mechanism on the fleet dashboard: 8,816,049 Bybit and 2,487,759 Binance frames since the recorders' Sep 6 boot, all between 22:0x Sep 7 and 02:0x Sep 8 (the `capture-disk` incident), none since; free had fallen 56.6 → 28.1 GB from 02:00 Sep 8 to 15:00 Sep 9. Fix: `tape_free_floor_bytes` reads the highest `[storage].min_free_disk_gb` under `deploy/capture/` (`--tape-floor-config`, `RECLAIM_TAPE_FLOOR_CONFIG`), low water = writer headroom above `max(reserve, tape floor)` = 31 GiB, `tape_floor_bytes` in `status.json` and the journal line; three tests. First timer run on it, 23:41 UTC: exit 0, first `storage-reclaim.last-success`, free 35.4 GB, low water 33.29 GB, WAL reclamation due in ~5.5 h.
  - The dashboard's `Tape loss · 5m` besides the floor episodes: `dropped_frames` 48, Bybit, 22:09–22:10 UTC Sep 9 — eleven shards lost their Bybit sockets within 90 s (`ping/pong timed out`, `Connection to remote host was lost`), their resubscribe snapshots landed on a 3.9 k → 10.1 k frames/s burst, and the 262,144-frame queue overran against a writer that clears 7–8 k frames/s on one core. `gap` is shard reconnects, venue-side: 349 Bybit (168 lost connections, 77 ping timeouts) and 33 Binance since the Sep 6 boot. Not changed.

- **2026-09-09 — Manifest-driven realm plumbing via `deploy/realms.tsv`.**
  - Unified realm configuration into `deploy/realms.tsv` (# realm-table-v1) defining realm properties, venue mappings, modes, and enabled states.
  - Templated generation of systemd unit files, environment configurations, and Grafana dashboard (`render_dashboard.py`). `deploy/lib_sleeves.sh` dynamically reads realm table.
  - MEXC and Hyperliquid realms held provisioned but stopped (`live-canary`).
  - Dashboard (`930d3be2`): the `Realm` variable's `regex` and `allValue` are the table's `running` realms, so the stopped mexc and hyperliquid realms, which push `up=0` every minute by design, are no longer red `DOWN` / `ATTENTION` cards (owner: "they are just backups, remove them from grafana"). Dashboard `version` 12; re-import the JSON. Test `test_the_realm_variable_lists_only_the_realms_the_table_holds_running`.
  - Deploy receipt: Commit `42dd7446` deployed via run `34368381679`. Verified by 19 tests in `tests/policy/test_realms.py`.

- **2026-09-09 — Hyperliquid canary run 1 fixes in `engine-tools/src/canary.rs`.**
  - Resolved three canary defects: quantized order prices to venue tick size and 5 significant figures; handled unacknowledged execution branches; switched order cleanup from cloid queries to address-based order status checks to prevent `unknownOid` retry loops.
  - Deploy receipt: Installed in `42dd7446`. Tested by five unit tests in `canary.rs`.

- **2026-09-09 — Incident `mexc-signal-intake`: unlisted symbol batch froze signal lane.**
  - Root cause: Signal batch containing seven instruments unlisted on MEXC blocked `queue_signal_observation` indefinitely because unlisted names never produced catalog entries.
  - Fix in `engine-core`: Symbols missing from venue catalog with no open orders or inventory are dropped with a warning instead of blocking signal intake.
  - Deploy receipt: Deployed in `42dd7446`.

- **2026-09-09 — Signal worker modular public data sources (`PublicVenueKind`).**
  - Modularized signal worker venue fetching into `engine/signal-worker/src/venue/` (`bybit.rs`, `mexc.rs`, `hyperliquid.rs`).
  - MEXC and Hyperliquid workers stream native tickers, klines, and funding rates directly from venue APIs rather than proxying Bybit data.
  - Deploy receipt: Deployed in `42dd7446`. Verified by 225 signal worker tests.

- **2026-09-09 — MEXC market feed funding clock alignment.**
  - `engine-marketdata/src/mexc.rs` updated to read contract declared funding intervals and next settlement timestamps directly instead of assuming standard 8-hour UTC epochs.

- **2026-09-09 — Incident `host-22826ce0bb838311`: MEXC signal worker LONG lane degraded by spool cap.**
  - Root cause: MEXC signal worker `current` spool class reached the 8-file cap (`CURRENT_SPOOL_FILE_CAP = 8`), refusing `LongWatermark` commits silently while `CarryWatermark` coalesced into existing paths. Heartbeat published aggregate `spool_backpressured=false`, hiding the blocked class.
  - Fix: `check_fleet_liveness.py` updated to extract `spool_backpressured_classes` and name blocked classes in alerts. Extended diagnose workflow digest to report per-class spool file and byte counts. Handover in `7f0214f` restarted MEXC processes and cleared the stall.

- **2026-09-09 — Incident `host-51b05439c4f09794`: Telegram alert failures obscured by missing error details.**
  - Root cause: Watchdog Telegram alerts failed with HTTP 400 `PEER_ID_INVALID` due to an invalid target chat ID. Generic HTTP error handling dropped the response JSON `description`. Watchdog also printed healthy sign-off text while exiting 1 on delivery error.
  - Fix: `liquidity_migration/ops/telegram.py` parses and logs redacted venue error description from response JSON. Liveness runner suppresses healthy sign-off when alert routing fails.

- **2026-09-09 — Incident `mexc-a361f5d18861421a`: false CRITICAL pages during 600 s private stream resync.**
  - Root cause: MEXC private stream 600 s resync performs sequential history sweep across followed symbols, setting `may_open=false` during the 10–30 s sweep window. The 30 s watchdog sampled during this transient state and paged CRITICAL 8 times.
  - Fix: Separated operator latch (`operator_may_open`) from private stream readiness (`private_stream_ready`) in engine telemetry. Watchdog only alerts if private stream stays unready past 180 s (`private_stream_unready_ms > 180_000`). Registered `private-stream:` alert reference in diagnose and watchdog tables.
  - Deploy receipt: Deployed in `d835b62`. Resync at 03:50:51 UTC verified clean with no alert.

- **2026-09-09 — Incidents `demo-0922e9f30da3bf98`, `mainnet-014ec4a90a2fde5f`, `mexc-d62940e951288d4c`: false CARRY freshness page at UTC roll.**
  - Root cause: Signal workers wait 5–8 minutes at 00:00 UTC for venue funding settlement. Liveness check evaluated CARRY freshness against a flat 180 s threshold from last completion, ignoring the scheduled roll frontier.
  - Fix: Freshness evaluated relative to `carry_cycle_not_before_wall_ts_ms` published by worker, measuring lateness only after the cycle is actually due.
  - Deploy receipt: Deployed in `beef5bc5`.

- **2026-09-08 — Incident `mexc-a361f5d18861421a`: MEXC recovery sweep stalled on rowless history.**
  - Root cause: `RecoveryClient::executions` treated an empty execution history page as lack of forward progress, aborting recovery. Liveness diagnostic also lacked MEXC systemd unit mapping.
  - Fix: Recovery sweep advances progress counter per answered page regardless of row count. Added MEXC unit mappings to diagnose tool.
  - Deploy receipt: Deployed in `62234c95` via run `34291380453`.

- **2026-09-08 — Filter signal worker universe to venue listed symbols.**
  - Added `universe.listed_on` filter in `signal-worker` config to exclude instruments not listed on the engine venue (e.g. Hyperliquid), preventing unlisted symbols from clogging queue.

- **2026-09-08 — Wire Hyperliquid perpetuals as fourth realm.**
  - Added Hyperliquid venue adapter, private stream feed, and signal worker configs. Deployed via run `34289829877`. Held in stopped `live-canary` posture.

- **2026-09-08 — Reclassify rolling-loss restriction as `NOTICE`.**
  - Downgraded `rolling-loss:<unit>` alert from `CRITICAL` to `NOTICE` in `check_fleet_liveness.py`, as envelope loss restriction is an expected risk constraint rather than an infrastructure fault.

- **2026-09-08 — Incident `mexc-first-start`: catalog sort mismatch and REST rate-limit latch.**
  - Root cause: `rules()` and `symbol_pairs()` returned unsorted maps, failing symbol admission checks; resulting rapid re-fetch loops triggered MEXC REST rate limits (HTTP 429/510).
  - Fix: Symbol pairs sorted alphabetically in catalog loaders; added shared rolling-window request pacer in MEXC REST client. Deployed in runs `34282588846` and `34285402045`.

- **2026-09-08 — Wire MEXC USDT perpetuals as third realm.**
  - Integrated MEXC gateway, websocket private order feed, and signal worker. Verified via owner canary order execution (receipt `32f27d4b` at 20:16:27 UTC). Deployed via run `34255009973`.

- **2026-09-08 — Worked-entry test timing fix.**
  - Isolated test clock to avoid wall-clock dependency in 1 ms reprice gate test.

- **2026-09-08 — Incident `mainnet-014ec4a90a2fde5f`: signal worker boot repair page.**
  - Root cause: Cold-start signal worker historical kline backfill exceeded 180 s liveness threshold before finishing catchup.
  - Fix: Added `boot_repair_acceptable` allowing up to 600 s for initial cold-start catchup before paging CRITICAL. Deployed in run `34251758369`.

- **2026-09-08 — Incident `host-51b05439c4f09794`: failed handover left realm timers disabled.**
  - Root cause: Handover script disabled realm systemd timers during deployment; an aborted deployment left watchdog and study timers inactive.
  - Fix: Added `restore_realm_timers` trap to re-enable timers on deployment failure or abort.

- **2026-09-08 — Incident `mainnet-ac90e31c207bc0da`: venue timeout latched funded openings.**
  - Root cause: Bybit REST timeouts during market volatility tripped the engine order recovery latch (`may_open=false`).
  - Fix: Enhanced transient network error handling and retry logic in `bybit/execution.rs`; added explicit journal logging for latch triggers.

- **2026-09-08 — Restore CARRY daily holding on owner direction.**
  - Disabled intraday funding and pre-settlement exits; restored daily holding model with 24-hour quantity persistence. Deployed in `34247719025`.

- **2026-09-08 — Repair execution recovery, account limits, and research costs.**
  - Updated research fee defaults (`configs/bybit_fee_rates.json`). Configured 30 s PostOnly rest for LONG entries. Added 100 bp mark collar on orders. Isolated Bybit book sequence gap recovery to affected L1/L50 topic. Deployed `cecff2e2` via run `34247719025`.

- **2026-09-08 — Work LONG demo entries for 30 s and recover resting entries across restart.**
  - Maintained working-order supervisor across process restarts; demo entries join near touch for 30 s. Deployed `441811eb` via run `34213632476`.

- **2026-09-08 — Check clock units without assuming sub-millisecond scheduling.**
  - Local push gate clock assertions adjusted for variable CI scheduling granularity.

- **2026-09-08 — Measure one-sided execution against actual order intentions.**
  - Added `engine-tools execution-study` command and 15-minute scheduled timer. Compares decision-time benchmarks, fill prices, and slippage. Deployed `06220e56`.

- **2026-09-07 22:56 UTC — Incident `host-681737fd16e1f806`: capture-disk page at 25 GB reserve floor.**
  - Root cause: Host disk space reached 25 GB reserve floor due to accumulated `/var/lib/liquidity-migration/backup` (31.8 GiB) and build artifacts, triggering market tape backpressure.
  - Fix: Added directory disk reporting to `deploy_remote.sh::report_disk_usage`. Implemented backup directory retention and link cleanup. Fixed cross-device link error (`EXDEV`) in storage reclaimer. Deployed in `f1fbe34` and run `34173874312`.

- **2026-09-07 — Historical source adapters and explicit sparse-data execution.**
  - Decoupled recorder decoding from book reconstruction; added trade and bar models with explicit fill timing; verified Bybit CSV and Parquet normalization equivalence.

- **2026-09-07 — Current-WAL accounting and sustained resource qualification.**
  - Reconstructed complete Sep 6 USDT linear cash and execution accounting. Validated WAL v7 rotation across segment boundaries.

- **2026-09-07 — Remove obsolete audits and Python runtime code.**
  - Deleted four obsolete Tier-1 audit docs; removed unused Bybit REST limiter and sleeve file rewriters; moved equity sampling to `engine-tools`.

- **2026-09-06 — Round-3 embedded execution.**
  - Replaced child strategy subprocesses with embedded Rust execution in `engine-core`.
  - Combined checkpoint, intent, verdict, and order dispatch into single main event loop with durable barriers.
  - Introduced WAL v7 format (50 writable kinds, backward-compatible readers) and offline `wal-convert-v5` tool.
  - Enforced exact decimal instrument constraints; removed Python state importers; optimized stop-loss maintenance and portfolio routing paths.
  - Deployed commits `8c92c964` (Stage A) and `937ba60d` (Stage B) via workflows `34114063829` and `34119441979`.

- **2026-09-06 — Round-2 runtime cleanup and integration.**
  - Separated runtime crates into `engine`, `engine-tools`, and `signal-worker`.
  - Fixed in-flight quantity tracking, single callback decoding, and separated risk admission timestamp from decision timestamp.
  - Implemented fair event-loop rotation across market, timer, maintenance, signal, and control lanes.
  - Deployed commits `8f96e603`, `93404ff6`, and `bb4bc3d3` via workflows `34043450919` and `34055716541`.

- **2026-09-06 08:07 UTC — Worker recovery, recorder finalization, and rollback repair.**
  - Retained full daily CARRY scorer windows across hourly source pruning. Fixed quiet recorder symbol hourly finalization under continuous traffic.
  - Hardened automated rollback to preserve candidate binaries unless runtime inputs match. Refreshed Google OAuth credentials for backup and tape sync. Retired legacy signal sources.
  - Deployed `420c7347`.

- **2026-09-06 — Tier-1 exact ownership and recovery qualification.**
  - Implemented 42 audit items: exact sleeve exits, account/risk quantities, bounded terminal order cache, and disk-sorted execution history fold. Passed 2,300 debug and release tests.
