# /etc/liquidity-migration/oncall.env -- root:root, mode 0600
#
# Automated incident responder and external watchdog-plane dead-man only.
# Format: strict KEY=value, one per line, no quotes or inline comments.

INCIDENT_ROUTINE_FIRE_URL=
INCIDENT_ROUTINE_FIRE_TOKEN=

# One external check for the independent host watchdog. Realm watchdogs must
# not ping this URL: a surviving realm must never mask a dead monitoring plane.
ONCALL_DEADMAN_URL=
