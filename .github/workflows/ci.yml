name: CI

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package
        run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"
      - name: Lint
        run: python -m ruff check liquidity_migration tests
      - name: Run tests
        # -v + --tb=short surface the failing test name and a short
        # traceback in the public job summary so failures can be
        # diagnosed without admin-level log download access.
        run: python -m pytest -v --tb=short
