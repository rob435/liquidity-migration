# Active Receipt: LONG volup125

**Status:** binding component of the current long-native v11a profile.

## Decision

`volup125` is part of the promoted-in-code LONG profile, together with `div` and
the weekend 1.5x tilt. The sleeve is demo/paper only and currently toggled off
on the live box.

This receipt does not approve real money. Forward demo/paper and the Tier-3 bar
remain binding.

Historical run tables are in git history.
