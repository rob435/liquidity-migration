schema_version = 1
maximum_baseline_ratio = 1.5

[bench]
events = 2000
rate = 100
every = 20
symbols = ["BTCUSDT"]

[runners.linux-x86_64]
# Provisional engineering references, not observed Linux measurements.
# Calibrate from the first hosted qualification before claiming R3-06 complete.
status = "provisional-unmeasured"
decision_p99_ns = 50000
submit_p50_ns = 5000000

[runners.darwin-arm64]
# Apple M4, same recipe, embedded execution with one order barrier; execution-performance.md.
status = "measured-embedded-one-barrier-uncontended"
decision_p99_ns = 14200
submit_p50_ns = 5570000
