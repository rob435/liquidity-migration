# Operational State

Current operational snapshot of the live trading fleet and host environment.

---

## 1. Live Deployment Snapshot

Verified against the running host and Grafana Cloud on 2026-09-04 18:13 UTC;
GitHub Actions state re-verified 19:16 UTC:

| Property | Value | Description |
| :--- | :--- | :--- |
| **Host** | `ip-208-84-103-4.my-advin.com` (`208.84.103.4`) | 4 vCPU, 8 GB RAM, 118 GB disk (77% used, 27 GB free at the 08:36 UTC deploy on 2026-09-05; the watchdog's floor is 25 GiB on `/var/lib`). |
| **Deployed Commit** | `cece1d9f` (sliding-window uploader; carries every recorder fix and the uploader leak fix) | Deployed by run `33955442044`, `deploy-ok` 08:36:50 UTC on 2026-09-05, the first deploy to reach the host since `65ee75a7`. Both recorders restarted (pids 2383127 Bybit, 2387253 Binance); mainnet handed over atomically with every precondition `[PASS]` and `REAL_MONEY` armed. `main` is one docs commit ahead (`1cfbce5f`, identical `engine` and `market_tape` trees). The `capture-disk` incident's standing cause — tape the Drive already held was never removed until a cap or the 25 GiB floor forced it — is closed by `market_tape pack --keep-hours 24`: the hourly upload run deletes each ledgered hour once it is 24 h old, so the host holds ≤ 24 h of tape per recorder plus the hour in flight. First pruning pass is the 09:10 UTC upload run on 2026-09-05; until then free space sits at 27 GB. GitHub Actions ran only while the repository was public, 08:30–08:47 UTC; it is private again and refuses every job, so `cece1d9f` is the last commit that can reach the host (§CI / Deploy Gate). History of the incident, the 26 pages and the 36 refused deploys: [CHANGELOG.md](CHANGELOG.md), 2026-09-04 18:03 UTC through 2026-09-05 08:36 UTC. |
| **Rollback Target** | `93ab5cda4b3d7138922664682e9f8c1bdfb3a791` | Stored in `/opt/liquidity-migration-engine/previous-commit`. |
| **Funded Status** | `real-money armed` | `REAL_MONEY=true`. The engine is running with healthy heartbeats. |
| **Incident Response** | Telegram + Claude Code routine + external dead-man | Demo, mainnet, and independent host watchdogs run every 3 min. A 16:57 UTC live delivery drill returned `telegram accepted`, created no-change session `session_01Jf15281KD9pJvLF32sFJGw`, and returned `dead-man accepted` in 2.9 s. Telegram remains the human pager, trade feed, and constrained control surface; the routine receives no venue credentials and can diagnose or deploy only through `vps-deploy.yml`. Delivery works, and `vps-deploy.yml` ran end to end once while the repository was public (run `33955442044` deployed `cece1d9f` at 08:36 UTC on 2026-09-05); the repository is private again from 08:47 UTC, so the routine can neither diagnose nor deploy and no incident can be closed on a host receipt (§CI / Deploy Gate). Both realms paged on the same funding-lane defect at 10:36 UTC on 2026-09-05 — `mainnet-014ec4a90a2fde5f` and `demo-0922e9f30da3bf98`, one root cause, fixed on `main` in `7e6fcb93` and undeployed. The demo worker paged a second time at ~20:25 UTC on 2026-09-04 (`demo-0922e9f30da3bf98`, same ref, 62 min into a fresh process): the routine narrowed it by elimination to one Bybit frame drought over `mark_max_age_ms` = 30 000 ms and under the socket's 45 s idle tolerance, and the clause that would name it in the page itself ships with undeployed `697341e4`. One open host reading, in [CHANGELOG.md](CHANGELOG.md). Route files are separated and root-owned mode `0600`. [docs/notifications.md](docs/notifications.md). |
| **CI / Deploy Gate** | Private repository; **hosted runners refused again since ~08:47 UTC on 2026-09-05** | The repository was public for about 17 minutes on 2026-09-05 — long enough for run `33955442044` to deploy `cece1d9f` at 08:36 — and is private again (`updated_at 08:47:59Z`), so the account-payment block is back: three `diagnose` dispatches at 10:39–10:42 (`33961267596`, `33961304356`, `33961354167`) each died in 4–8 s with every other job skipped and `failed to download logs: HTTP 404`. No repository change can reach the fleet until the repository is public again or a private runner is registered. A 24-hour audit found 90 workflow runs and about 2,023 billed job-minutes: every push repeated Python, Rust, release-build, soak, and benchmark work. Pushes now start no cloud job. Code PRs run Python and Rust gates; docs-only PRs run none. `deploy` alone builds an exact-SHA artifact after Python and Rust gates; `qualify` alone runs release tests, soak, and benchmark; verify, rollback, diagnose, and disarm do not build. New artifacts retain for 2 days; 98 obsolete archives were deleted, leaving the latest deployed, rollback, and live-process archives at 37.1 MiB total. Run `33911407276` proved the build-free `verify` routing, then GitHub refused its sole `vps` job before runner assignment with the failed-payment or spending-limit annotation. Re-checked 21:36 UTC 2026-09-04: `verify` run `33921858031` on `e2345ca4` failed after 4 s with `vps` again the only scheduled job, every other job skipped, and no log content at all (log download returns HTTP 404), so the job never started. Hosted capacity remains externally blocked until GitHub restores it or a separate private Linux runner is registered. The funded VPS is never an Actions runner. |
| **Equity History** | `liquidity-migration-equity-recorder.timer`, every minute | One sample per engine, signal worker, and tape recorder goes first to `/var/lib/liquidity-migration/equity/<kind>-<realm>-<YYYY-MM>.jsonl`; a source with no readable heartbeat is written as `state=absent`. `scripts/ops.sh curve mainnet` reads it on the host. Each run then pushes six samples to Grafana Cloud stack `proudtortoise1017` through `influx-prod-55-prod-gb-south-1`; `/etc/liquidity-migration/observability.env` is `root:liquidity-migration` mode `0640`, and the dedicated access policy grants only `metrics:write`. Verified at 18:12 UTC on 2026-09-04: three consecutive journal rows say `recorded and pushed 6 samples`. Both workers are transport-healthy and fully covered while their bounded CARRY cold fill reports `starting`; Bybit is 16/16 connected and Binance 10/10, with zero reconnects, frame drops, or disk drops and no blocked disk. That last clause expired at 22:54 UTC and has not come back: free space on `/var/lib` sits at both recorders' 25 GiB floor and crosses it repeatedly — 22:54, at least once unpaged, 23:50, and by 00:01 on 2026-09-05 it is no longer an episode but a 30-second oscillation, each recorder recovering for exactly one status tick before blocking again (`host-681737fd16e1f806`). The 00:36 page is the same floor with a longer clean stretch in front of it — Binance wrote 16 minutes and Bybit 12 with no disk drops at all, then both crossed 0.75 s apart at 00:35:59 and 00:36:00. The 00:57 page settles what that one could not: it catches six retention passes inside the window, spaced 302.4–304.8 s apart, which is the deployed 300-second clock and nothing waking on the crossing, and five of the six bought the writer nothing at all — the 00:53:44 pass unlinked 91 files and the disk was blocked again 16 s later. Five defects, all fixed on `main` and none deployed: the pruner waited out its 300-second interval (`1d8fad9a`); it stopped deleting at exactly the floor `writable()` unblocks on, so a pass returned zero headroom and the next tick re-blocked (`d275885a`); and the pass that frees room could not open the writer's gate, which only `_maintenance` ever cleared, so every crossing cost a full `status_interval_seconds` of tape on a disk that already had space; and a pass that deleted and left the gate shut ran no successor, because `_maintenance` arms `prune_now` on the crossing only and the blocked `_write_loop` never reaches an append to fail on, so the pruner slept a full `RETENTION_INTERVAL_SECONDS` while the writer was blocked (`fd604613`'s successor; measured on the Bybit unit at 01:40:59, 306.2 s between its own passes with 499 875 frames discarded and 30 rows written in the 180 s after the short pass). The 01:32 page measures that last one on the Bybit unit: the 01:30:50 pass freed room, the gate opened 15.06 s later at the 01:31:05 tick, and the unit wrote 54 rows in an interval that carried 95 657 once it was open. That is what sets the 30-second period of the oscillation — the status interval, not the disk, and only when a neighbouring pass frees room. The 02:30 page is the crossing with no neighbour: the Bybit unit blocked at 02:28:10, no `retention removed` line appears from either recorder across the whole 840.7 s excerpt, and 120.1 s later the writer was still gated with `rows` frozen at 57 611 732, having discarded 345 469 frames (2 876/s) where the interval before it wrote 3 146 rows/s. The 02:33 page, three minutes later on the Binance ref, holds the end of that same block: Bybit's own 02:31:48 pass ran 4 files, the gate opened at the 02:32:10 tick, and the block was 240.2 s and 691 512 frames — not 300 s and 863 000. That page also settles `d275885a` on its own terms: Binance's 02:29:23 pass unlinked 187 files and Bybit's unlinked 4, and both bought the recorder that ran them exactly one status tick, because both stop on the number `writable()` unblocks on. The 187-file pass did not open Bybit's gate at all — 46.9 s later Bybit still read the disk as full, Binance having resumed 7 s earlier into zero margin — and Binance's 02:32:03 unblock followed no pass of its own but Bybit's, 14.8 s earlier: on one filesystem each pruner is the other's only source of room between its own 300-second walks. The 02:48 page withdraws that page's one generalisation. There is **no ceiling on a block**: Bybit was blocked 02:37:40 → 02:43:10, 330.3 s and 903 098 frames, straight through its own 02:41:58 pass, whose 4 files bought nothing because Binance's gate opened 4.8 s later and took the margin. Nor does pass size buy recovery — Binance's 230-file pass opened its gate in 7.3 s, its 290-file pass took 95.1 s and its 27-file pass 32.7 s — because what sets recovery is the margin at the stopping point, which is zero by construction. The 290-file case is also where the payload stops: both recorders were blocked across it, so the tape did not take that room back, and either the filesystem had not released the unlinked blocks or a non-tape writer on `/var/lib` did. The 03:00 page pushes the maximum block to **390.3 s and 1 100 454 frames** (Bybit, 02:45:41 → 02:52:11, straight through its own 7-file pass at 02:47:04), and pushes pass size the rest of the way out of the picture: Binance's 410-file pass at 02:49:33 opened its gate in 0.17 s and it was shut again at the next tick, while its 106-file pass at 02:54:36 opened nothing and the block ended 177.8 s later on Bybit's 4-file pass. A file count never measured room freed for the floor in any case — `prune` deletes for age and for `max_bytes` in the same walk — and the manifest's per-unlink `compressed_bytes` and `age`/`disk_limit` reason is where that separates. That page also finds the **fifth defect, and it is in `06e17d4a` rather than on the host**: the owed-successor retry re-derives its deficit from the same statvfs whose disagreement made the successor owed, so it deletes that deficit again on every retry, back to back, until no non-snapshot file is left. Two windows in the payload show the condition live — 02:47:34 → 02:49:34 and 02:58:11 → 03:00:34, in both of which neither recorder wrote a row, a pass had already run, and free space stayed under the floor — so deploying `06e17d4a` alone would delete the tape roots. Fixed in `3c1ebd22`, which credits each retry with what the burst already unlinked. The 03:21 page finds no sixth defect and settles two things instead. The three incident ids this log has been tracking are one incident under three names: `incident_id` is `sha256(scope + the newly-due CRITICAL refs)[:16]` (`scripts/runtime/check_fleet_liveness.py:806-807`), so `host-681737fd16e1f806` = `{capture-disk}` (Bybit), `host-16171e3c5e186136` = `{capture-disk:forward-market-binance}` and `host-ecbac293ecc90d5e` = both, all three re-derived exactly; the id names whose cooldown cleared first, not a distinct fault. And it prices the margin: one 8-file Binance pass at 03:19:46 ended a 90.1 s block on *both* units, each of which then wrote exactly one 30-second status interval (Bybit 87 124 rows, Binance 35 005) and re-crossed — so the room a pass leaves is ~30 s of the pair's own output, ≤19.8 MB at 661.5 KB/s. (That page called the rate "inbound wire bytes"; the 03:39 page shows `projected_gb` is post-gate raw payload bytes, still an upper bound on disk growth since what lands is compressed, so the conclusion below is a lower bound on the improvement.) That makes `d275885a`'s 1.25 GiB of headroom **≥34 minutes between crossings instead of 30 seconds**, a ≥67× cut, rather than a refinement. Both gates opened on a status tick and not on the pass (18.35 s and 28.23 s of the 90 s spent waiting for one, `fd604613` undeployed), and neither pruner walked again in the 109 s to the last line (`1d8fad9a` undeployed). Crossings and resumptions are 9.86 s and 9.88 s apart — `status_interval_seconds` phase, not the disk: the two recorders cross as one filesystem event. The journal also settles half the open host question without SSH: Bybit logged no pass at all in 990.8 s, of which 810.6 s were unblocked, and a pass logs whenever it deletes anything (`market_tape/record.py:1184`), so neither tape is at its `max_disk_gb` cap and `min_free_disk_gb` is what binds. Cumulative tape discarded and still climbing at the last journal line: 16 065 875 frames (Bybit) and 5 827 039 (Binance), **21 892 914** together, of which 1 161 705 came after the 03:00 page — 937/s over ~20.7 min, well under that page's 3 268/s because the floor went uncrossed for 13.5 minutes and then was crossed again. The incident was episodic through 03:21 — 810.6 s clean at 3 156 rows/s, then a 3-minute blocked phase in which the pair kept 122 129 rows and discarded 505 598, 4.14 discarded per row kept. **The 03:33 page ends that.** No clean stretch survives in it: all ten resumptions across the two units last exactly one 30-second status interval, Bybit is blocked 74.2 % of 930.7 s and Binance 85.2 % of 810.5 s, and the pair discarded 2 481 366 frames in the 720.5 s since the 03:21 page — **3 444/s, the worst sustained rate of the incident**, past the 03:00 entry's 3 268/s. Cumulative **24 374 280** frames, 3.27 discarded per row kept. It also measures the sixth defect's premise from the Bybit side: Binance's 03:24:49 pass satisfied `prune`'s own accounting and `writable()` still read below the floor for **76.0 s across three status ticks**, and Bybit's 03:30:45 → 03:32:45 block ended with **no deletion on either unit** in the 173.8 s before it, on one filesystem where Binance read the disk blocked 10 s either side of that unblock — free space moving under a tape the pruner was not walking, which is what `1702d14d` arms on ([CHANGELOG.md](CHANGELOG.md)). No engine unit is named or affected; the floor is the reservation held for mainnet's WAL and it is intact. `min_free_disk_gb`, not `max_disk_gb`, is what governs tape size on this host, so the crossings repeat until the six fixes deploy. The 03:18 page reaches the same conclusion from the Binance side — 840.5 s clean with `disk_dropped` frozen and no `retention removed` line across three passes due on the 302.5 s clock — and adds the sixth defect above. **The 03:39 page adds the eighth defect above and three measurements.** Over 900.54 s the Binance unit took 1 117 017 frames and kept 121 206 rows — **10.85 % reached the tape**, 8.22 discarded per row kept, 1 105.8 frames/s on the floor against 1 240.4/s inbound — in four blocks of ≥90.05 s, 210.13 s, 360.25 s and ≥150.08 s, each unblock buying exactly one 30 s status interval (+49 500, +35 999, +35 665 rows). Its three passes were 37, 68 and 414 files on the 302.5 s clock, and between the 68-file and the 414-file pass it wrote **27 % fewer rows while its pruner unlinked 6.1× more files** — a pruner's deficit growing while its own tape shrinks is what a foreign writer on the filesystem looks like from inside a recorder, which is independent support for the uploader defect above. That page also closes out the 03:34:54 pass the 03:42 page bounds at "≥71.2 s": the gate opened at 03:36:35.743, so 414 unlinks bought **nothing for 101.21 s**, with three ticks still reading below the floor and no further pass in the 281.3 s to the last line. And its `RESOLVED capture-disk` — the incident's first — is aliasing, not recovery: both configs set `min_free_disk_gb = 25`, `disk_blocked` toggles on a 30 s status interval and the watchdog samples every 3 min, so a `RESOLVED` line in these payloads is never evidence a recorder recovered, and it is why the incident id keeps turning over. **The 03:51 page adds no further defect and one measurement**: a pass can open the neighbour's gate and not its own. Bybit's 03:48:01.724 pass unlinked 6 files, Binance's writer opened 34.4 s later and wrote 63 495 rows into the room with zero frames discarded, and Bybit's own writer stayed gated **164.7 s** longer — five consecutive ticks reading below the same 25 GiB floor Binance read above at 03:48:36 and 03:49:06, on one filesystem, while Bybit discarded 434 506 frames at 2 895/s. Room freed at the zero-margin stopping point is a common pool: the first unit to sample it takes all of it and the unit that ran the walk has no claim on it, which is what `d275885a`'s 1.25 GiB margin removes. The pair is blocked 84.6 % (Bybit, of 780.6 s) and 78.6 % (Binance, of 840.5 s), cumulative **28 020 904** frames discarded, and the rate since the 03:33 page is **3 372/s** — below that entry's 3 444/s, so the escalation has flattened rather than reversed. **The 04:19 page withdraws the 03:33 entry's "no clean stretch survives" and adds no ninth defect.** Both units ran clean with `disk_dropped` frozen and `disk_blocked=False` on every tick — Bybit 750.7 s of a 780.7 s window, Binance 960.6 s of 990.6 s — keeping 3 636 779 rows and discarding **78 frames**, all of them on the single final tick: 0.079 frames/s against the 03:51 page's 3 372/s. Cumulative **29 965 507**, and all 1 944 603 frames lost since the 03:51 page accrued before 04:02:36, in the gap that payload does not cover. Neither pruner deleted anything across two and three passes due on the 300-second clock, which re-confirms from a third window that neither tape is at its `max_disk_gb` cap and `min_free_disk_gb` binds. The crossing that ends the stretch lands 500.4 s (Bybit) and 517.2 s (Binance) after `liquidity-migration-market-tape-upload.timer` fired at 04:10:00 — correlation from one page, not proof, but a second independent pointer at `7fe4fe0c`'s leak after the 03:39 page's. **The 04:37 page ends that lull and adds no ninth defect.** The pair is gated 75.0 % (Binance, of 960.6 s) and 81.5 % (Bybit, of 810.7 s), keeping 732 034 rows and discarding 2 660 810 frames — **3.635 discarded per row kept**, cumulative **33 501 584**, ≈3 230 frames/s across the ~1 096 s since the 04:19 page against that page's 0.079/s and the incident record of 3 444/s. Two measurements are sharper than any before them. The eighth defect is confirmed **59 ticks out of 59**: all 13 `projected_gb` upticks across both units are writing intervals and all 46 downticks are gated ones, with no exceptions, where the 03:39 page had three intervals. And a **183-file Binance pass at 04:30:20.827 opened no gate at all for 226.89 s**, while Bybit's 8-file pass at 04:33:44.432 opened its own in 6.75 s and Binance's in 23.29 s — a 22.9× disparity, the largest recorded, and the zero-margin common pool `d275885a` removes. Inside that block lies the incident's first stretch with **both** writers provably gated and no room: 210.172 s (04:30:21.005 → 04:33:51.177) in which neither unit wrote a row, neither unlinked a file, and every tick on both read below the 25 GiB floor — 16.8 s after 183 files were unlinked and for 210 s after that. The tape consumed nothing across it, so either the kernel held those blocks over 16.8 s or a non-tape writer took them; this is the 03:42 page's 414-file reading with its confounder removed. That page carries no upload fire (`*:10:00`), so it adds no timing correlation, only a window from which the tape is excluded by construction. `d275885a`'s 1.25 GiB margin already covers an archive in flight; `7fe4fe0c` is what stops the permanent orphan. **The 04:52 page is the worst of the incident on three measures and adds no ninth defect.** Bybit is gated **90.9 %** of 990.7 s and discards **9.546 frames per row kept** — both records, past 85.2 % (03:33) and 8.22 (03:39) — and its longest block is **480.364 s**, a quarter longer than the previous maximum of 390.3 s, running straight through *two* retention passes (its own 13-file pass at 04:43:54 and Binance's 21-file pass at 04:45:28) and ending only on its own next pass at 04:49:00; across it the unit discarded 1 396 960 of 1 396 990 frames, 99.998 %. Binance is gated 83.3 % of 900.5 s at 5.062 discarded per row kept. Pair cumulative **36 643 872**, ≈3 432 frames/s since the 04:37 page against the incident record of 3 444/s, so the escalation is at its ceiling rather than past it. The eighth defect is now confirmed **63 ticks out of 63** — all 8 `projected_gb` upticks are writing intervals (28 485–102 342 rows), all 55 downticks are gated ones (≤69 rows), no exceptions and no flat ticks. The sharpest new measurement prices the zero-margin common pool against a *single* unit's output: Bybit's own 13-file pass opened **Binance's** gate 43.8 s later and never its own, and Bybit's next `writable()` sample at 04:44:51.673 — 13.639 s into Binance's writing interval, 16.376 s before Binance read the disk writable again — read below the floor, on a byte-identical `min_free_bytes = int(25 × 1024³)` = 26 843 545 600 B (`market_tape/record.py:624`). It is a statvfs reading and not an append failure: the deployed `_write_loop` logs on the first append to fail after an open gate (`market_tape/record.py:1125-1133`) and no such `ERROR` line appears in either excerpt. So 13 unlinked files bought **≈14 000 Binance rows** of margin at 1 027.6 rows/s — the 03:21 page priced the same headroom at ~30 s of the *pair's* output; this is under 14 s of *one* unit's, and it is exactly what `d275885a`'s 1.25 GiB removes. Binance's pass sizes run 19 → 21 → **398** files inside ten minutes on a unit that kept 154 742 rows, the 19× foreign-writer signature the 03:39 page named at 68 → 414; the payload spans no upload fire (last 04:10:00), so it adds no timing correlation. `ls -l /var/lib/liquidity-migration/market-tape-upload/staging` is the decisive host reading ([CHANGELOG.md](CHANGELOG.md)). **The 05:05 page adds no ninth defect and names the mechanism behind the common pool.** It runs 6.5 minutes past the 04:52 page on both units, and on that non-overlapping tail alone the pair discards **8.289 frames per row kept** — past the incident's previous worst of 8.14 (03:42) — Bybit at 11.028. Over the whole 1 080.8 s window Bybit is gated **94.44 %** at 10.239 discarded per row kept, past 90.9 % and 9.546, with three blocks — the 480.364 s already recorded, **300.214 s** (04:49:51.903 → 04:54:52.117, closed here because that page ended inside it, and running straight through Binance's 398-file pass) and **≥210.150 s** still shut at the last line — and 60.0 s of writing between them. Binance is gated 80.00 % at 3.966. Cumulative **37 973 012**; since the 04:52 page Bybit added 1 009 228 frames over 390.289 s and Binance 319 912 over 360.186 s, **3 474.0 frames/s** as the sum of the exact per-unit rates, which breaks the 3 444/s record of 03:33 and withdraws that page's "at its ceiling, not past it". The eighth defect is confirmed **66 ticks out of 66** (9 upticks all writing intervals, 57 downticks all gated), and on Bybit the meter saw 257 749 of 3 093 231 frames — 8.33 %, about a twelfth of the allowance the unit is spending. The new mechanism is tick phase, not fairness: of six retention passes, five are followed by a gate opening with no other pass intervening and **all five are Binance's**, including all three of Bybit's own, because Bybit's pruner completes at :54, :00 and :06 while Binance ticks at :08/:38 and Bybit at :21/:51 — a **13.693 s mean handicap, spread 0.122 s** over 10.2 minutes, on the unit with the 3× larger tape. `fd604613` deletes it outright: the pass opens the runner's gate itself (`market_tape/record.py:1196-1198`) with no tick in between. Binance's pass sizes now read 19 → 21 → 398 → **112**, so the 04:52 entry's 19× spike does not persist — still 5–6× the 19/21 baseline, but a quarter of the spike, which weakens a monotone reading of pass size without touching the uploader mechanism. **The 05:23 page is the second lull, adds no ninth defect, and measures the incident's ceiling.** Both units run clean with `disk_dropped` frozen to the frame — Bybit 750.7 s, Binance 780.5 s — then cross **13.156 s apart** (05:20:56.099 and 05:21:09.255, the tick-phase spacing the 05:05 page measured, so one filesystem event sampled twice), and every one of the window's 584 728 discarded frames accrues after it, with **zero rows written** in either block: Bybit 453 146 in ≥150.128 s, Binance 131 524 in ≥120.069 s, both gates still shut at the last line. That gives the ceiling directly — **4 113.8 frames/s** for the pair while both are gated, which is the pair's inbound rate and bounds every window figure this log holds: 3 444/s (03:33) and 3 474/s (05:05) are that number diluted by writing intervals, so the escalation had a hard stop and reached it. Cumulative **39 107 219**; the rate since the 05:05 page is **769.9 frames/s**, a 4.5× fall that is the lull and not a repair. Not one `retention removed` line appears in 930.9 s on either unit with three passes due on each 300-second clock, which is the **fourth** independent window fixing `min_free_disk_gb` rather than `max_disk_gb` as what binds — and the page adds why the blocks run their full length: the **deployed `_retention_loop` has no `prune_now` at all** (`1d8fad9a~1`: `self._retention_pass()` then `self.stop.wait(RETENTION_INTERVAL_SECONDS)`, and `stop` is set only at shutdown), so nothing on the host can shorten the gap between a crossing and the next scheduled walk. `1d8fad9a` and `1702d14d` remove that. The eighth defect is confirmed **62 ticks out of 62** (53 upticks all writing intervals, 9 downticks all gated), now 59/59, 63/63, 66/66 and 62/62 across four pages. The upload-timer correlation gets a third data point and weakens: the crossing is 626.1–656.1 s (Bybit) and 639.2–669.3 s (Binance) after the 05:10:00 fire against the 04:19 page's 500.4 s and 517.2 s, so the constant-lag reading is withdrawn while the mechanism stands — `ship` writes `<staging>/<name>.tar`, a full second copy of one finished hour, and unlinks it only in the `finally` (`market_tape/pack.py:371-395`), an ungated GB-scale hourly sawtooth by a non-tape writer on the filesystem the floor guards. Two proposals are recorded for the owner and deliberately not built: a log line for a retention pass that deletes nothing (`market_tape/record.py:1182-1184` logs only a non-empty delete, which is the ambiguity every page in this incident has had to reason around), and a free-space gate on the uploader ([CHANGELOG.md](CHANGELOG.md)). One risk is recorded for after the deploy, not fixed: `_retention_loop` resets `credit = 0` per burst (`market_tape/record.py:1162`), so where the kernel's statvfs lags a pass's unlinks past 30 s, `1702d14d`'s per-tick bursts re-derive the deficit — bounded at one deficit per 30 s, and the deployed 300-second version of the same shape has not drained either tape root in 13 hours ([CHANGELOG.md](CHANGELOG.md)). **The 05:39 page brackets that lag directly and the risk is no longer unproven**: a 214-file Binance pass logged at 05:30:47.843, its own tick read below the floor 21.761 s later and Bybit's read above it 38.780 s later with neither unit deleting anything in between, so the crossing lands in **(21.761 s, 38.780 s]** — straddling `status_interval_seconds` = 30 s. It stays bounded and it stays unbuilt: the fifth defect it rhymes with was unbounded, this costs ≤19.8 MB of the oldest tape per tick, and a tenth undeployed commit does not help a fleet on its twenty-eighth refused deploy. **The 05:39 page ends the second lull, adds no ninth defect, and sets three records.** The Binance unit is gated **90.32 %** of 930.5 s at **9.370 discarded per row kept** (past 85.2 % and 5.062), and its final block is **510.293 s with zero rows written** — the incident's longest, past 480.364 s — running straight through **two of its own passes, 214 and 290 files**, both of which opened **Bybit's** gate instead (+38.780 s → 84 674 rows; +6.385 s → 96 927 rows) while Binance's own stayed shut throughout. 504 files unlinked by one recorder bought the neighbour 181 601 rows and its own writer nothing: the zero-margin common pool at the largest scale recorded, and exactly what `d275885a` and `fd604613` remove. A second measurement is the **fourth independent pointer at a non-tape writer**: Binance's gate opened at 05:29:39.552 with the most recent unlink on either unit 234.294 s earlier and **neither unit writing a row** across the whole stretch, so free space rose above 25 GiB while the tape neither deleted nor wrote — and the only tape-internal source that frees bytes silently, a compression unlinking its raw `.jsonl` (`market_tape/storage.py:322`), drains in seconds because both writers were gated and closed no segments. Binance's pass sizes run **10 → 214 → 290** against Bybit's 6 → 3 → 7 on a tape three times larger, the same foreign-writer signature as 68 → 414 (03:39) and 19 → 21 → 398 (04:52). The eighth defect is confirmed **65 ticks out of 65** (7 upticks all writing intervals, 58 downticks all ≤62 rows), now five pages at 59/59, 63/63, 66/66, 62/62 and 65/65, with Binance's meter seeing 9.64 % of its frames. Cumulative **42 340 901** frames discarded; the rate since the 05:23 page is **3 444.0/s**, level with the 03:33 record and still under the 4 113.8/s ceiling, which stands. **The 05:48 page sets two more records, inverts the tick-phase handicap, and adds no ninth defect in the write path.** The Binance unit is gated **96.77 %** of 930.540 s at **28.141 discarded per row kept** (past 94.44 % and 11.028) and writes **one 30-second interval in 15.5 minutes**; its block runs 05:30:39.593 → 05:41:09.949, **630.356 s with 2 rows written** and 657 213 frames discarded at 1 042.6/s, the incident's longest, past 510.293 s. Pass size is now ordered backwards: the 214-file and 290-file passes inside that block opened only Bybit's gate, and the **11-file** pass at 05:40:52.909 opened **both** (+17.040 s own, +34.197 s neighbour), after which the pair wrote 123 750 rows and was back under the floor inside one status interval — 504 files bought nothing, 11 ended it. The 05:05 page's 13.693 s tick-phase handicap against Bybit has **inverted**: all three Bybit passes open its own gate first (+10.431 s, +5.625 s, +0.725 s) while Binance's next tick reads shut every time (+23.370 s, +18.530 s, +13.534 s), an 18.478 s mean the other way, so neither unit is favoured by anything but the drifting phase of a 30 s clock and `fd604613` is the fix rather than a fairness rule. Retention runs the deployed 300-second clock again — Binance 302.447 s, Bybit 305.029 s and 305.170 s, no pass on a crossing, a sixth independent window. The eighth defect is confirmed **59 ticks out of 59** (7 upticks all 33 212–100 766 rows, 52 downticks all ≤110 rows), six pages now, and Binance's meter saw 33 214 of 967 901 frames — **3.43 %**, a new low. Cumulative **44 133 703**; the rate since the 05:39 page is **3 317.4/s**, below that page's 3 444.0/s and under the 4 113.8/s ceiling. One consequence is measured for the first time and deliberately not fixed: an hourly book re-anchor issued while the gate is shut loses that hour's snapshot, because `reanchor_books` advances `reanchor_cursor` on send (`market_tape/record.py:352-384`) and `reanchored()` then reports the hour anchored (`:386-387`) while the snapshot frame meets the shut gate (`:1098-1108`) — Bybit logged one at 05:40:27.024 and one at 05:45:27.296, both milliseconds after `_maintenance` set `disk_blocked = True`. The fix is new recorder behaviour and is the owner's call; `python -m market_tape book bybit --hour 2026-09-05T05 --symbol BTCUSDT --depth 50` is the reading that settles it ([CHANGELOG.md](CHANGELOG.md)). **The 05:54 page adds no ninth defect, corrects the 05:48 page's pass count, and inverts the pass-size reading a second time.** It cross-validates that page to the frame — at 05:48:10.187 Binance reads `disk_dropped=12 044 944` and Bybit at 05:47:57.448 reads `32 088 759`, summing to its cumulative 44 133 703 — and then shows its window held a **third** Binance pass, 15 files at 05:45:55.281, absent from that entry: the deployed 300-second clock therefore reads 302.447 s and 302.372 s across three consecutive Binance walks, a seventh window with no pass on a crossing, and **contiguous status lines do not imply an excerpt holds every `retention removed` line**, so read "not one pass in N seconds" as "none in this excerpt" wherever this log says it. That 15-file pass left its own gate **shut for 315.015 s** and opened nobody else's, while Bybit's **5-file** pass at 05:50:02.756 bought the pair **386 840 rows** — three consecutive open Bybit ticks, 90.097 s and 323 079 rows, with Binance writing two of its next three intervals concurrently — against the **123 750** the 05:48 page's 11-file pass bought four minutes earlier, 3.1× the rows off 45 % of the files. Ordered by files unlinked the window's four passes buy 11 → both gates, 6 → its own, 15 → nobody, 5 → the best recovery of the incident, so pass size orders recovery in neither direction and the margin at the stopping point is what does (`d275885a`). The tick-phase handicap is mixed *inside* one window — Bybit's tick lands 17.130 s after Binance's, two passes open their own gate at the next tick (+17.040 s, +0.725 s), one opens nobody's, one opens its own two ticks later (+54.835 s) — so the direction is a property of where a pass lands on a drifting 30 s clock, not of a unit, which is `fd604613`. Binance is gated 90.00 % of 900.502 s at 8.787 discarded per row kept and Bybit 77.77 % of 810.708 s at 3.123; the eighth defect is confirmed **57 ticks out of 57** (9 upticks all 30 806–128 962 rows, 48 downticks all ≤110), seven pages now, with the meter seeing 10.22 % of Binance's frames and 22.88 % of Bybit's. Cumulative **45 269 277**; the rate since the 05:48 page is **3 152.0/s**, below its 3 317.4/s and under the 4 113.8/s ceiling, and the 360 s tail past that page is the first sustained recovery since the 05:23 lull at **2.935 discarded per row kept**. The 05:48 page's re-anchor proposal is not re-derivable from this payload — Binance logs no re-anchor in the window and Bybit's three land on tier changes and on writing ticks — so it stands as recorded, awaiting the owner's `book` rebuild. This session's page was dispatched on `d734cbde` at 06:13:02 UTC as run `33949210864` and refused at 06:13:07 for a **thirty-first** consecutive time: `rust` 06:13:03 → 06:13:06, `ci` 06:13:04 → 06:13:06, `Deploy artifact` 06:13:04 → 06:13:07, none of the three alive long enough to check out the repository, `diagnose`, `disarm`, the release-test job and `vps` all skipped, `vps` never scheduled against a runner, and the failed jobs' log downloads returning `failed to download logs: HTTP 404`, re-read this run. This session's page was dispatched on `cb190c86` at 06:41:20 UTC as run `33950499842` and refused at 06:41:26 for a **thirty-second** consecutive time: `ci`, `rust` and `Deploy artifact` all created 06:41:22 and dead 06:41:25, none of the three alive long enough to check out the repository, `diagnose`, `disarm`, the release-test job and `vps` all skipped, `vps` never scheduled against a runner, and all three failed jobs' log downloads returning `failed to download logs: HTTP 404`, re-read this run. This session's page was dispatched on `8fffac96` at 07:02:01 UTC as run `33951429145` and refused at 07:02:05 for a **thirty-third** consecutive time: `Deploy artifact`, `ci` and `rust` all created 07:02:02 and dead 07:02:05, none of the three alive long enough to check out the repository, `diagnose`, `disarm`, the release-test job and `vps` all skipped, `vps` never scheduled against a runner, and all three failed jobs' log downloads returning `failed to download logs: HTTP 404`, re-read this run. This session's page was dispatched on `0dc502f7` at 07:12:38 UTC as run `33951915472` and refused at 07:13:19 for a **thirty-fourth** consecutive time, the first since 04:45 in which a job waited for a runner: `ci` 07:12:40 → 07:12:43 and `Deploy artifact` 07:12:40 → 07:12:44, neither alive long enough to check out the repository and both log downloads returning `failed to download logs: HTTP 404`, re-read this run; `rust` created 07:12:40 and **38 s in `queued` with no runner assigned** before failing at 07:13:18; `disarm` and `diagnose` skipped at 07:12:40, `vps` and the release-test job created 07:13:19 and skipped, `vps` never scheduled against a runner. This session's page was dispatched on `e2391040` at 07:33:03 UTC as run `33952870432` and refused at 07:33:09 for a **thirty-fifth** consecutive time: `rust` 07:33:04 → 07:33:07, `Deploy artifact` and `ci` 07:33:05 → 07:33:08, none alive long enough to check out the repository, `diagnose` and `disarm` skipped, the release-test job and `vps` created 07:33:08 and 07:33:09 and skipped, `vps` never scheduled against a runner, every failed job's log download returning `failed to download logs: HTTP 404` — twelve and a quarter hours in which no repository change can reach the fleet through GitHub. **The 07:25 page adds no ninth defect and returns the incident to episodic.** Incident `host-ecbac293ecc90d5e`, both refs new in one fire, id re-derived exactly from `sha256("host\ncapture-disk\ncapture-disk:forward-market-binance")[:16]`. Binance discarded **0.135** frames per row kept over 870.532 s and Bybit **0.081** over 1 081.103 s, against 5.358 and 9.277 at 07:00: `disk_dropped` is flat on both from 07:06/07:10 to 07:22, so the pair wrote every received frame for twelve to fifteen minutes before crossing the floor within five seconds of each other at 07:22:38 and 07:22:43. Cumulative discarded **56 304 565**. Two readings carry the page. **Neither pruner deleted a file — zero `retention removed` lines in either 40-line window — and on `65ee75a7` neither could**: the deployed `_retention_loop` is one pass then `stop.wait(300.0)`, the deployed `_maintenance` body is `self.disk_blocked = not self.retention.writable()` and nothing else, and `prune_now` is waited on by no one, so a crossing is answered no earlier than the next timer wake; Binance threw away 124 062 frames in 120.067 s with zero rows written, waiting for it. `1702d14d` (arm on every blocked tick), `1d8fad9a` and `fd604613` bound that to one 30 s tick plus a walk, and all three are merged and undeployed. **Bybit's recovery is the tape's alibi**: its gate opened between 07:23:38 and 07:24:08 with no `retention removed` and no `capture storage unblocked` line — the only two lines a pass that ends a block writes — so free space rose over 25 GiB released by a writer that is not this recorder; it then wrote 89 354 rows in 30.018 s and was under the floor again, a gigabyte-scale sawtooth the tape cannot produce. That is `7fe4fe0c`'s staging story: `ship` builds one archive into the upload staging directory, uploads it and unlinks it in a `finally` (`market_tape/pack.py:371,395-396`), inside the 25 GiB reservation and gated on nothing, on a `*:10:00` timer that fired 12.5 minutes before the crossing. The uploader's own journal is not in the payload; the host readings that settle it are in [CHANGELOG.md](CHANGELOG.md). Whether `build_archive` should respect the recorders' floor is proposed to the owner, not built. **The 06:00 page breaks the 05:23 page's discard ceiling, brackets a free-space rise inside one status interval, and adds no ninth defect.** The page ends with **both recorders gated and still gated** — Binance 150.072 s and Bybit 150.108 s with zero rows, received equal to discarded to the frame on both (179 836 and 452 779) — discarding **4 214.7 frames/s** against that page's 4 113.8/s. The number was never a bound: it is the pair's inbound rate while gated, it drifts with the venues, and it has drifted up 2.5 % in 37 minutes, so read every "under the ceiling" in [CHANGELOG.md](CHANGELOG.md) as "under the inbound rate measured that hour". The sharpest new measurement is a **gate that opened with no deletion that could have opened it**: Binance's 05:55:10.435 tick reads open after 05:54:40.414 read shut, the nearest unlink on either unit is Bybit's 4-file pass **1.815 s** earlier and Binance's own last pass **252.574 s** earlier, against a smallest measured own-tick lag of 10.075 s and the 05:39 page's kernel bracket of (21.761 s, 38.780 s] — so free space rose above the 25 GiB floor inside one 30 s tick with nothing the tape did to explain it, and one unit took all of it (Binance +31 952 rows, shut again 30 s later, Bybit shut on both surrounding ticks). Fifth independent pointer at a non-tape writer, and the first bracketed to a single interval. The mechanism now has a run window: `liquidity-migration-market-tape-upload.timer` fires `*:10:00`, its service carries `TimeoutStartSec=3000` (50 minutes) and one run ships **both** tapes, while `ship` writes `<staging>/<name>.tar` per candidate hour and unlinks it only in the `finally` (`market_tape/pack.py:179,219,371,396`) — so a 05:10 run can still be packing at 05:55 and **each hour it packs is one rise and one fall** of free space on the filesystem the floor guards, which is why the deficits (214–414 files) and the unexplained recoveries are the same event seen from both ends. `journalctl -u liquidity-migration-market-tape-upload.service -S 05:00 -U 06:15` is the decisive host reading and is sharper than the `staging` listing alone ([CHANGELOG.md](CHANGELOG.md)). Pass size fails to order recovery a third window running — 4 files → nobody for 79.246 s, 6 → its own at +12.435 s, 5 → its own at +54.835 s, **83 → both** (+10.075 s own, +27.478 s neighbour) for the Binance unit's longest in-block writing stretch of the incident, 90.043 s and 95 874 rows, 289 201 for the pair against the 386 840 a 5-file pass bought. Binance is gated 78.57 % of 840.460 s at 3.810 discarded per row kept and Bybit 79.31 % of 870.711 s at 3.379; the eighth defect is confirmed **57 ticks out of 57** (12 upticks all 29 648–128 962 rows, 45 downticks all ≤48), eight pages now, with the meter seeing 20.79 % and 23.22 % of the two units' frames. Cumulative **46 454 544**; the rate since the 05:54 page is **3 290.1/s**, above its 3 152.0/s and below the 3 474.0/s record, and the 360 s tail past that page runs 3.690 discarded per row kept against the 2.935 it measured, so its recovery did not hold. It also narrows that page's methodological warning: `journalctl -n 40` returns the last 40 lines with no gaps, so a line between an excerpt's first and last is necessarily in it and what the cap removes is the head — read "not one pass in N seconds" as sound only for N inside the excerpt's own span. Both of this page's excerpts sit exactly on the 40-line cap. **The 06:34 page adds no ninth defect and makes the non-tape writer the best-evidenced fact of the incident.** Binance's gate opened at 06:25:41.519 with the nearest unlink on either unit **1.492 s later** (Bybit's 4-file pass at 06:25:43.011, which therefore cannot be the cause) and none at all in the 510.314 s of its own excerpt or the 190.811 s of Bybit's before it, while across 06:22:41.421 → 06:25:32.352 both writers were gated, wrote **3 rows between them** and discarded 758 192 frames — free space rose above the 25 GiB floor with the tape neither writing nor deleting, and a gated pair closes no segment, so compression had nothing to drain. Sixth independent pointer, and the first whose only candidate cause post-dates the recovery. Pass size is now ordered **backwards at 9.3:1**: 93 files unlinked by Binance (88 + 5) opened no gate on either unit, while 10 unlinked by Bybit (4 + 6) opened its own twice, each for exactly one status interval, each on the tick after the pass (+19.379 s, +14.226 s, `fd604613`) and each buying one interval because the pass stops on the number `writable()` unblocks on (`d275885a`). Retention ran the deployed 300-second clock on both units (302.531 s, 305.460 s) with no pass on a crossing, an eighth window for `1d8fad9a` and `1702d14d`. The pair's gated inbound rate is steady to 0.005 % across two disjoint stretches — **4 209.0/s** and **4 209.2/s** against the 06:00 page's 4 214.7/s — so that page's drift is real but slow. Binance is gated 66.66 % of 990.592 s at 1.743 discarded per row kept (its 330.216 s clean head is what holds the number down) and Bybit **91.30 %** of 690.677 s at 9.206; the eighth defect is confirmed **56 ticks out of 56** (14 upticks all writing intervals, 42 downticks all ≤95 rows), nine pages now, with the meter seeing 36.46 % and 9.02 % of the two units' frames. Cumulative **49 499 660**; +3 045 116 since the 06:00 page, whose per-unit endpoints this payload does not carry, so the gap rate is bounded at **1 489.2–1 514.1/s** rather than summed exactly, and the window rate is 3 565.6/s. Two more hourly re-anchors landed on a shut gate (Bybit 06:26:32.407, 2 topics; 06:31:32.717, 1 topic; hour `2026-09-05T06`), which is `_maintenance` setting `disk_blocked` at `market_tape/record.py:1221` and re-anchoring at `:1228` in the same pass — still the owner's call, as the 05:48 page recorded. **The 06:54 page sets the incident's longest block, its tightest pass-to-gate handoff and its worst inter-page discard rate, and adds no ninth defect.** Bybit was gated 06:41:33.286 → 06:52:03.897 — **630.611 s**, past the 05:48 page's 630.356 s — writing **74 rows** and discarding **1 905 686** of 1 905 750 frames received, straight through *both* of its own passes (4 files at 06:46:05.968, 4 at 06:51:11.807), and **both openings those 8 files bought went to Binance** (+6.234 s and **+0.554 s**) while Bybit's own gate opened only 37.244 s after *Binance* unlinked 3 files. That 0.554 s is the smallest pass-to-gate lag in this log, past the +0.725 s of the 05:48 page and the first under a second that is a neighbour's tick: the room a pass leaves at its zero-margin stopping point is taken by whichever `_maintenance` tick samples the filesystem first, which is `d275885a` and `fd604613` undeployed. Across the window the six passes run 6 → own, 4 → nobody, 4 → neighbour, 1 → nobody, 4 → neighbour, 3 → neighbour, so pass size orders recovery in neither direction for a fifth window. What an opening buys is a byte quantity drained at the taker's own rate: Bybit's two openings ran one status interval each (106 119 and 101 481 rows) and Binance's ran two each (65 058 rows over 60.039 s, 64 686 over 60.028 s) at a third of Bybit's frame rate. Binance is gated 87.50 % of 960.509 s at 6.948 discarded per row kept and Bybit **93.33 %** of 900.845 s at **12.521**; the pair kept 337 540 rows and discarded 3 503 312 — **10.379 per row kept**, the worst pair figure of the incident, past 8.289 (05:05). Retention ran the deployed 300-second clock on both units (302.683 s, 302.806 s, 306.252 s, 305.839 s) with no pass on a crossing, a ninth window for `1d8fad9a` and `1702d14d`. The eighth defect is confirmed **62 ticks out of 62** (6 upticks all writing intervals of 31 519–106 119 rows, 56 downticks all ≤112 rows), ten pages now, with the meter seeing **12.58 %** and **6.95 %** of the two units' frames, both new lows. Cumulative **54 157 550**; the rate since the 06:34 page is **3 854.3/s** as the sum of exact per-unit rates (Binance 959.8/s, Bybit 2 894.5/s), which breaks the 3 474.0/s record of 05:05 and every window rate this log holds. The pair's gated inbound rate is 4 098.8/s, down 2.6 % from 4 209.0/s. Neither excerpt carries a re-anchor line, so the 05:48 page's lost-snapshot finding gets no new instance and stands as recorded. **The 07:00 page names the mechanism behind every "pass size orders nothing" reading, withdraws that page's one-interval rule, sets the gated-inbound record, and adds no ninth defect.** `prune` stops deleting for room the moment its running free-space total reaches `free_target` (`market_tape/storage.py:383,403-412`), which on deployed `65ee75a7` is `min_free_bytes` exactly — so the margin a pass leaves above the floor is the **overshoot of its final unlink, one file's compressed size**, and a file count never measured bytes in the first place, since the same walk deletes for age and for room and only the manifest's `compressed_bytes` and `age`/`disk_limit` reason separates them. The prediction holds in the tail: Bybit's **8-file pass at 06:56:17.668 bought two full status intervals** (06:56:34.123 → 06:57:34.188, 60.065 s, 196 771 rows) where both of its openings in the 06:54 window ran one, so that page's "Bybit one interval, Binance two" is withdrawn as a rule — the 05:54 page's 90.097 s Bybit opening off 5 files never fitted it either. The pair's **gated inbound rate sets a record at 4 350.4/s** (Binance 196 838 received and 196 838 discarded in 180.112 s with zero rows, Bybit 490 235 and 490 235 in 150.491 s, both still shut at the last line), +6.1 % on 4 098.8/s minutes earlier, so that page's 2.6 % downward drift does not hold and the rate is the venues', not a repair. The 0.554 s handoff it measured is now decisive rather than merely tightest: `_maintenance` reads free space at its top (`market_tape/record.py:1208`) and logs the status line at its end (`:1315`), a pass this payload prices at **8–15 ms**, so the read landed ≈0.54 s after the unlink — either unlinked blocks are visible to another process in under a second, which means the 04:37 page's 183-file pass (no gate for 226.89 s) and the 03:42 page's 414-file pass (nothing for 101.21 s) were not the filesystem withholding blocks, or the room came from a non-tape writer directly. **The kernel-lag alternative is retired either way**, leaving the hourly uploader (`market_tape/pack.py:371-396`) as the only mechanism standing, and `journalctl -u liquidity-migration-market-tape-upload.service -S 06:30 -U 07:05` as the reading that names it. In the six-minute tail the two units swap places — Binance 11.397 discarded per row kept against Bybit's 4.835 — at **3 617.0/s**, under the 06:54 page's 3 854.3/s record; cumulative **55 461 720** (Binance 14 905 063, Bybit 40 556 657). Retention ran the deployed 300-second clock again (302.549 s, 305.861 s) with no pass on a crossing, a tenth window; the eighth defect is confirmed **65 ticks out of 65**, eleven pages, with the meter seeing 15.73 % and 9.21 % of the two units' frames. The hour boundary re-anchored **40 book topics** for `2026-09-05T07` — `REANCHOR_TOPICS_PER_TICK` (`market_tape/record.py:89`), the first tick of the hour's pass at its cap — 15 ms before a status line reading `disk_blocked=True` and still shut 150 s later: the largest single lost chunk recorded, the first at an hour boundary, and still the owner's call. Once they deploy the tape self-trims to the disk: the headroom is 1.25 GiB against a worst case of 210 MB written per 300 s interval (`projected_gb` 1370.9 + 443.0 = 1813.9 GB/month at 00:57 is post-gate raw payload bytes, which compression only reduces), about 6× margin, and `prune_now` covers a burst. The caps in `deploy/capture/*.toml` stay the owner's size decision — 60 + 18 GB of tape plus the floor is 105 GB of a 118 GB disk — but after the deploy they govern how much history is guaranteed, not whether the recorders block. Whether the tape or non-tape growth took the room is the open host reading, and the 02:48 and 03:00 pages give it windows to look at: `scripts/ops.sh status`, `scripts/ops.sh curve mainnet 240`, then `df -h /var/lib` and `du -sh` over the two tape roots, the mainnet engine's state directory and `/var/log/journal`, plus the manifest's own bytes-freed-per-reason sum ([CHANGELOG.md](CHANGELOG.md)). If the tape roots sum well under their 60 + 18 GB caps while the disk is at the floor, the caps are not the dial to turn. Dashboard UID `liqmig-fleet` is a 15-panel operator view bound to `grafanacloud-proudtortoise1017-prom`: four live status tiles; separate demo/mainnet equity and open-exposure cards with exact USDT values and independently scaled sparklines; execution activity; p99 order-path latency; current freshness; capacity; and fault state. [docs/observability.md](docs/observability.md). The order path is measured on a clock: the demo `probe` sleeve rests one order at :00/:15/:30/:45 and the sampler at :20 reads the engine's 60-second latency ledger, so each probe is caught exactly once. Readings 16:45 and 17:00 UTC: `end_to_end` p50 11.84 / 11.29 ms — market event to submitted order, which stops at the submit. `wire` (11.74 / 11.20 ms) is the whole venue task and so contains the round trip; `ack` is empty on demo because every `place` in the demo WAL carries no transport stamp, while 317 of 320 mainnet places do and report a round trip of p50 3.74 ms, p99 59.48 ms, worst 429.76 ms. `engine latency --wal` is the authority. |
| **Signal IPC** | spool row + `stream.sock` doorbell | Every observation is a spool row first; the socket frame only saves the engine its next poll. Worker generations were renewed on both realms on 2026-09-03 after the desync; the old `g805c44f0…` (mainnet) and `gc4d0071f…` (demo) cursors stay in the WAL. |

---

## 2. Active Strategy Sleeves & Dials

| ID | Sleeve | Status | Rules & Dials | Execution Mandate |
| :---: | :--- | :--- | :--- | :--- |
| **0** | **CARRY** | Active | `configs/lane2_carry_hold_v7.json` | $3\times$ notional, $5\times$ leverage, $35\%$ stop. Entry $\le -10\text{ bp}$, exit $>-3\text{ bp}$. |
| **1** | **LONG** | Active | `configs/long_native_v12.json` | $6.0\times$ operational mult, top-10 volume rank, $15\%$ price velocity, $3\times\text{ATR}$ stop, $3\text{d}$ hold. |
| **2** | **EXODUS** | Active | `configs/lane2_exodus_short_v1.json`| Consumes CARRY pre-settlement event. Enters short; covers at $S+60\text{m}$. |
| **3** (mainnet) | **MAKER** | Disabled | `configs/lane2_toxic_flow_quoter_v1.json` | Microstructural market making on `AGIUSDT` (`quote_enabled = false`). |
| **3** (demo) | **PROBE** | Active | `[[strategy]] probe` in `deploy/engine.demo.toml.template` | Order-path benchmark, not a trading sleeve: one venue-minimum post-only `BTCUSDT` buy 3% under the bid every 15 min on the wall clock, pulled 2 s later, so the latency ledger holds a measurement the :20 sample reads. A fill is closed at market and shows only as an entry blocker. Never a page or a Telegram message. Mainnet has no probe. |

### Shared Account Risk Dials (`configs/operational.json`)
* **Gross Exposure Ceiling**: $5.0\times$ equity.
* **Initial Margin Ceiling**: $1.0\times$ equity.
* **Capital Reference**: Floating equity (floored at $\$100$).
* **Rolling-Loss Circuit Breaker**: $0.10$ ($10\%$ of capital reference lost in 24h trips emergency entry halt). A trip survives a restart, is reported by `rolling_loss_tripped`, and shows every sleeve as `entries_enabled: false`.

---

## 3. Fleet Health & Systemd Unit Matrix

| Systemd Unit | Realm | Health State | Heartbeat SLA |
| :--- | :--- | :--- | :--- |
| `liquidity-migration-engine.service` | Demo | Active | $\le 5\text{s}$ fresh |
| `liquidity-migration-engine-mainnet.service` | Mainnet | Active | $\le 5\text{s}$ fresh |
| `liquidity-migration-signal-worker-demo.service` | Demo | Active, **CARRY lane dead** | $\le 5\text{s}$ fresh. Same fault as the mainnet worker below and from the same ~08:35 UTC restart on 2026-09-05: the funding lane aborts every minute on `funding history rewrote timestamp 1785758400000` and no carry cycle has completed. Its page also carries a boot repair gap open 7295 s, which no evidence in the payload explains. |
| `liquidity-migration-signal-worker-mainnet.service`| Mainnet | Active, **CARRY lane dead** | $\le 5\text{s}$ fresh. Degraded since the 08:36 handover on 2026-09-05: the funding lane aborts every minute on `funding history rewrote timestamp 1785758400000` and no carry cycle has completed, so mainnet produces no CARRY signal. Root cause fixed on `main`, undeployed — the repository is private again and Actions refuses every job. Incident `mainnet-014ec4a90a2fde5f`, [CHANGELOG.md](CHANGELOG.md). |
| `liquidity-migration-forward-capture.service` | Bybit | Active | The tape that gets replayed, shaped for the sleeves' exits first. Domain is crypto only: 517 USDT perpetuals, stocks/ETFs/commodities in no tier. `core` = LONG's live band (turnover rank ≤ 120, leaves below 160, held 96 h after last ranking inside 120) with `book:50` + prints + ticker + liquidations; `crowded` = CARRY's whole hold zone (predicted funding ≤ −3 bp, held 72 h) with book + prints; discovery tiers (`overheated`, `movers`, `bursting`, `surging`, `flooding`, `levering`) with book + prints on what bandwidth is left; `wide` = every other name with **prints**, ticker and liquidations. `monthly_gb = 2400`; shed order `overheated` → `wide:trades` → discovery books → discovery prints; `core:*`, `crowded:*` and every ticker are never shed. Books re-anchor each UTC hour, so every uploaded hour replays on its own. Writer queue `queue_frames = 131072`; shards read frames with websocket-client's pure-Python UTF-8 validation off (`json.loads` rejects a bad frame anyway), which halves receive CPU per frame. |
| `liquidity-migration-forward-capture-binance.service`| Binance | Active | Cross-venue reference only, **no order book**: ticker (funding rate, mark, index) on all ~510 USDT `PERPETUAL`s (`TRADIFI_PERPETUAL` refused) plus trades on the acting tiers; `crowded` mirrors Bybit's −3 bp / 72 h trigger so the two venues' trades line up. `monthly_gb = 700` against ~480 measured. |
| `liquidity-migration-telegram-controls.service` | Global | Active | Polling authorized chats |
| `liquidity-migration-trade-notify.timer` | Global | Active | 5-minute trade scanning |
| `liquidity-migration-equity-recorder.timer` | Global | Active | 1-minute equity and recorder sampling. `independent`: runs through fleet restarts and funded stops, which is what lets it record them. |

---

## 4. Operational Invariants & Reference Runbooks

* **Runbook & CLI**: See [docs/operations.md](docs/operations.md).
* **Architecture & IPC**: See [docs/architecture.md](docs/architecture.md).
* **Engine Internals**: See [docs/engine.md](docs/engine.md).
* **Data & Tapes**: See [docs/data.md](docs/data.md).
* **Trading Logic**: See [docs/trading_logic.md](docs/trading_logic.md).
* **Observability**: See [docs/observability.md](docs/observability.md).
