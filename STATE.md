# Operational state

## Purpose

Record the latest verified host snapshot and the source of each operational setting.

## Spec Tables

| Observation | Value |
| --- | --- |
| Verified at | 2026-09-11 01:36:32 UTC by direct host reading after [deploy run `34549775253`](https://github.com/rob435/liquidity-migration/actions/runs/34549775253) (`9a21c7f5`, `vps` job 01:28–01:36 UTC): `deployed-commit` `9a21c7f5b0c7540de5e389c170360fef4347e7ee`, `previous-commit` `7e0488919e4915ca39468f11e26bf631d75c19cc`; demo engine (pid `26885`, up 01:28:32), mexc engine (pid `29209`, up 01:34:41) and hyperliquid engine (pid `30226`, up 01:35:10) all `engine_commit=9a21c7f5`, `may_open=true`, `private_stream_ready=true`, `strategy_errors=[]`, `positions=[]`; every fleet engine, worker and liveness timer, the equity recorder timer and both recorders `enabled active`; `systemctl --failed` empty; watchdogs `ok scope=hyperliquid units-and-heartbeats-healthy` 01:35:58 and 01:36:27, `ok scope=host sanctioned-deploy-in-progress` 01:35:05 (the deploy window) |
| Prior reading | 2026-09-10 04:12:42–04:14:38 UTC after [deploy run `34434792960`](https://github.com/rob435/liquidity-migration/actions/runs/34434792960) (`6e5ca627`): demo engine pid `3912650` and mainnet engine pid `3914411` both `may_open=true`, `strategy_errors=[]`, each holding its INJUSDT long; mexc and hyperliquid units disabled and inactive; watchdogs `ok`; the last reading before the owner's 2026-09-10 direction |
| Earlier reading | 2026-09-10 00:02:06 UTC after [deploy run `34417903805`](https://github.com/rob435/liquidity-migration/actions/runs/34417903805) (`ae95fedf`): both Bybit engines healthy, `engine venues` `bybit_mainnet` `live-proven`, `mexc_mainnet` and `hyperliquid_mainnet` `live-canary` |
| Evidence | [deploy run `34491454825`](https://github.com/rob435/liquidity-migration/actions/runs/34491454825), dispatched 14:48:21 UTC on `859723b1`: `ci` 14:50:45, `rust` 14:59:34, release artifact 14:59:54, `vps` 14:59:59–15:07:57; demo handover 15:00:36 and the 300 s soak to 15:06:09; `mainnet posture=stopped in deploy/realms.tsv: units stay stopped` 15:06:13; `atomic mexc handover` 15:06:14 with `native-state-ok realm=mexc result=already-complete`; `atomic hyperliquid handover` 15:07:19 with `native-state-ok realm=hyperliquid result=already-complete`; `deploy-ok` 15:07:46. The day's five deploys, two failed handovers and their fixes are in CHANGELOG 2026-09-10 |
| Host | `208.84.103.4` |
| Deployed commit | The host checkout and the three running realms (demo, mexc, hyperliquid) are `9a21c7f5b0c7540de5e389c170360fef4347e7ee`, handed over by [deploy run `34549775253`](https://github.com/rob435/liquidity-migration/actions/runs/34549775253) on 2026-09-11, the second handover of that night: [deploy run `34544358127`](https://github.com/rob435/liquidity-migration/actions/runs/34544358127) installed `7e048891` at 00:20 UTC, and the CARRY refusal hyperliquid raised on it is the fault `3115c92a` fixes. Stored rollback target is `7e0488919e4915ca39468f11e26bf631d75c19cc`. mainnet is `posture=stopped` in `deploy/realms.tsv` since 2026-09-10 (the owner's direction): `REAL_MONEY` stays armed, its units are disabled |
| Hyperliquid realm | `hyperliquid_mainnet` runs as the owner's forward test at readiness `live-canary` (`posture=running` since 2026-09-10; `permits_engine_run` accepts `live-canary` since `e7f3ca52`; the boot log names `unproven="fill-attribution, protection-place, protection-trigger, reconnect-history-recovery"`). Armed in `/etc/liquidity-migration/hyperliquid-mainnet.env` (master `0xcef3cc6085897672efc4bf5d8401f757c9b85b17`, account mode `disabled`, perps 52.4 USDC, no position). The 2026-09-10 11:59:20 UTC canary PASS (`venue_order_id=541177774027`, `client_id=lmcan-1a08b2fcd82-08b6-0000`) made submit, cancel and post-only `observed`. LONG entries on, CARRY and EXODUS off; a thin CARRY universe is a consumed batch since `859723b1`. Worker `status=ready`, 178 listed, 2 LONG / 4 CARRY eligible at 15:09; open findings: kline repair answers `Hyperliquid lists no coin for BTCUSDT` (also `ETHUSDT`, `ATOMUSDT`), the first universe refresh after a boot logs `left a sleeve with no eligible symbol` once and the retry succeeds, one `HTTP 429` on account recovery at 15:08:59 retained for retry |
| MEXC signal intake | Drained at the 15:06:53 UTC boot on `859723b1`: the 173 files that had waited in `/var/lib/liquidity-migration/signals/mexc` since 2026-09-09 are consumed, seven unlisted names' subscriptions dropped with one WARN each (`1000PEPEUSDT`, `ARCUSDT`, `FILUSDT`, `MONUSDT`, `RAYDIUMUSDT`, `SHIB1000USDT`, `TRUMPUSDT`), the spool holds the live `current` file only |
| MEXC worker LONG stall | Closed. The worker restarted 15:06:22 UTC on `859723b1` under producer generation `ff7ad772…` epoch 1 (its Bybit-contract checkpoint archived to `drifted-source-37a18797-1789052783211/`, history rebuilt from MEXC's own data), answered the engine's lifecycle request at 15:09:10 with frontiers long 21 / carry 208, and reads `status=recovering` (backfilling), 1,024 listed symbols, 119 LONG and 149 CARRY eligible, `spool_class_files` `{current: 1}`, nothing backpressured |
| MEXC realm | `mexc_mainnet` runs as the owner's forward test at readiness `live-canary` (`posture=running` since 2026-09-10). Armed and bound: `/etc/liquidity-migration/mexc-account-bindings.json` binds the key to UID `19445654`, `EXPECTED_ENGINE_ACCOUNT_USER_ID=uid-19445654`. The 2026-09-10 11:59:17 UTC canary PASS (`venue_order_id=853000482766018560`, `client_id=lmcan-1a08b2fd4a5-087e-0000`) made submit, cancel and post-only `observed`; fill-attribution, protection-place, protection-trigger and reconnect-history-recovery stay `implemented`. Engine `may_open=true`, equity 30 USDT, no position, LONG entries on, CARRY and EXODUS off; carry and long checkpoints re-initialised at the 14:21 handover after the 2026-09-09 feature-contract change (`ceac3c9b`) |
| Funded permission | Bybit mainnet is stopped by posture with `REAL_MONEY` still armed: its INJUSDT long 4.6 (native stop 5.805, read on the venue by `attest-flat` 15:09 UTC with its stop order working) has no engine managing its exit; the owner hand-trades it. Demo renders every sleeve's entries off and holds nothing. mexc and hyperliquid run LONG entries on real money (~30 USDT and 52.4 USDC) at `live-canary`; every cap is a ratio of verified equity (rolling-loss budget 0.1×), `reduce_only` exits are admitted ahead of the check |
| CARRY holding | CARRY entries are off on every running realm (demo, mexc and hyperliquid by render) and mainnet's CARRY is stopped with its engine; no realm holds a CARRY position. On a venue whose eligible CARRY universe is under 50 symbols the daily batch is consumed without a decision while the sleeve holds nothing (`859723b1`) |
| Alert routing | Every realm watchdog and the host watchdog print `ok` at 15:08 UTC; `mexc-liveness.timer` and `hyperliquid-liveness.timer` are enabled and active, `mainnet-liveness.timer` disabled. The on-call incident routine is **paused**: every fire attempt today answered `HTTP 400 Routine is paused`, so a CRITICAL reaches Telegram (30 min cooldown) and nobody else. The repointed alerts chat id remains unconfirmed until the delivery drill in [notifications.md](docs/notifications.md) §4 runs |
| Runtime state | Engine PIDs demo `4062319`, mexc `4066050`, hyperliquid `4067043`; worker PIDs demo `4062227`, mexc `4065991`, hyperliquid `4066983`; all on `859723b1` from the 15:00–15:07 handovers, `may_open=true`, `strategy_errors=[]`, zero restarts. mainnet units disabled and inactive. No running realm holds a position (demo's INJUSDT 59.5 long stopped out at 5.803 at 15:02:55 UTC, −38.78 USDT net) |
| Readiness boundary | No failed unit at 15:09 UTC. mexc and hyperliquid trade at `live-canary`: the first fill, stop place, stop trigger and reconnect recovery seen on each realm are the receipts that promote its matrix row. Open Hyperliquid worker findings are in the Hyperliquid realm row |
| Execution | Embedded callbacks; every engine unit uses `Type=notify`, `WatchdogSec=30s`. Host swap is 0 at 23:35. LONG uses 30-second PostOnly entry work in every realm; terminal order lookup and exact fill recovery precede an IOC remainder |
| Demo soak | All 31 observations pass from 15:01:07 through 15:06:09 UTC (300 s) before the funded handovers of [deploy run `34491454825`](https://github.com/rob435/liquidity-migration/actions/runs/34491454825); `deploy-ok` at 15:07:46 UTC |
| Disk and WAL | 49 GB free on `/` (118 GB) at 15:09 UTC; `storage-reclaim.service` has not failed since its 04:41 run on the 12 GiB floor and the 1-day release keep (`systemctl --failed` empty). The reclaimer holds low water one writer headroom (6 GiB) above the higher of the engine reserve and the recorders' `min_free_disk_gb` floor (12 GiB); `scripts/ops.sh storage` reads the receipt and `status.json`; live WAL is pruned only there, below the engine's floor, after a verified `sealed/` copy. Recorders `disk_dropped_frames` 0 since their 04:04 restart |
| Retained Stage A release | All three Stage A binaries remain at `/opt/liquidity-migration-engine/releases/8c92c96464bfe66662891c05f53a9510eefe8ba2/` with its staged archive; the reclaimer's keep set names that commit, the deployed and the previous commit, so release pruning never touches them. This historical reader is not requalified against current runtime-reconfiguration records. Retention does not authorize rollback across changed runtime inputs |
| Source qualification | Deployed source passes 2,575 developer Rust / 1,898 Python tests, zero failures, eight Rust ignores and one root/systemd-only Python skip (`scripts/dev.sh check` on `3115c92a`, 00:38–00:50 UTC 2026-09-11; `9a21c7f5` adds one test-path change and passed the pre-push repository gate and the deploy run's own gates). Rust 1.90 formatting and strict Clippy, Ruff, ShellCheck and mypy pass. The Tier 1 regressions (the account refresh during leverage administration, the parked wake, the day-after CARRY batch, the binary64 venue close, the requested withheld row, the replay window on every batch) fail before their fixes and pass afterward. Owed-markout restart, rotation and double-replay regressions, the recorded-allocation binary64 regression and the floor-removal contracts fail before their fixes and pass afterward; the Linux release archive verifies |
| Last latency qualification | [Run `34128439094`](https://github.com/rob435/liquidity-migration/actions/runs/34128439094) is bound to `70f4c557`: 1,981 release tests, account/history workloads and all eight latency cells pass. Candidate median decision p99 / submit p50 is 2,000 / 721,150 ns, within absolute and paired limits. Its separate qualified archive verifies. Tail delays and all prior misses remain in [execution.md](docs/execution.md); no new latency qualification is claimed for `cecff2e2` |
| Copied-data qualification | The compatible Stage A reader passes all 27 original/converted base pairs / 54 boots, complete rotation state and ordered venue effects. Stage B passes all 27 converted-only boots, nine queued/nine prepared retrievals, both known filled archive queries and all 154 input length/mtime checks. Real source-frontier coverage is zero; future fills are absent and transport, risk and collateral are mocked. Original inputs remain unchanged. A complete matched production-day replay remains unqualified |
| Legacy recovery boundary | Ordinary admission is exact-only; archived scalar lineage, provenance-driven grid adoption, simulated binary64 fills and native protective repair remain required. The zero-hit/module-removal criterion remains unmet; see [quantity contracts](docs/engine.md) and [retained recovery](docs/operations.md) |
| Historical adapters | Recorder adapter and normalized Bybit/CSV/Parquet inputs use the same Rust core. Book, trade and bar execution assumptions are separate; [schemas and commands](docs/data.md#historical-adapters-and-execution). These research changes require no unrelated funded-runtime deployment |
| Production-day accounting | 2026-09-08 mainnet, reconciled 22:13 UTC 2026-09-09 by `day_reconciliation` on copied segments `000064`–`000074`, the venue capture with transfer sources and the recorder's midnight samples (00:00:22 and 00:00:20): 16 WAL fills match 16 venue executions with 0.36354034 USDT of fee on both sides; the venue's wallet-cash chain runs 138.24226146 → 74.21308869 with trade −11.40062134, funding +0.19214857 and `TRANSFER_OUT` −52.8207 (the owner's withdrawal), residual 0 and no gaps; seven positions at the start and four at the end in both the WAL fold and the sample. Equity 133.11522018 → 71.13784514; the 2.05179773 USDT equity residual is the mark change and is not decomposable because those samples carry no unrealised P&L or wallet cash, so the gate reads FAIL on that one named requirement. Samples since 22:50 UTC carry `wallet_cash_usdt`, `unrealised_pnl_usdt` and per-symbol marks; [recipe](docs/operations.md#observed-production-day-reconstruction) |
| Current-source measurements | Frozen `80db33df` baseline covers sustained traffic, burst backlog, partial filled-history throughput and delayed replies. A 2M-operation/100K-history offline soak completes; the fixed WAL-reader comparison falls from 5.656 s / 9.41 MB peak Python allocation to 0.103 s / 4.70 MB. Adverse cells, identities and scope remain in [execution.md](docs/execution.md#sustained-workload--reader-measurements); this does not requalify deployed network latency |
| Terminal retention boundary | Three zero-filled cancelled demo requests still lack exact terms in live segment `000063` at the 2026-09-07 21:04:59 UTC read: `eng-1788685989000-{8,9,10}`, retained since `1788702340615` ms. Natural expiry requires both wall time and complete execution history strictly beyond 2026-09-13 13:47:40.615 UTC; neither condition is met. Expiry alone does not retire archived lineage, legacy inventory or protective repair |
| Equity recorder | Rust `engine-tools record-equity` remains scheduled every minute at :20. Since `e899ea21` the engine sample carries `wallet_cash_usdt`, `unrealised_pnl_usdt` and a per-symbol `positions` list with venue marks (`positions_truncated` under the 4,096-byte append cap): the 22:50:20 UTC mainnet sample reads equity 34.73629546, wallet cash 39.25806546, unrealised −4.52177, INJUSDT 4.6 at mark 6.054 and WLDUSDT 73.3 at 0.4136 |
| Execution study | [Contract and commands](docs/execution.md#2-one-sided-execution-study); fifteen-minute timer active. Both LONG realms use the 30-second PostOnly policy. The [annual review](docs/research/annual-execution-2026-09-08.md) retains all queue/latency cells and the selected sample’s limits; realized savings from the new policy are not established |
| Observed fees / model boundary | The 48 h sample ending 07:47:54 UTC has 54 trade fills matching authenticated Bybit execution and transaction records. Actual cost is 10.3433 bp fees + 4.0142 bp slippage = 14.3575 bp per executed side, with 100% cost coverage. Most selected symbols charge 10 / 3.6 bp taker / maker; CAP and HEMI charge 11 / 4 bp. Five usable LONG opening comparisons shape the demo choice; the ARB crossing error of −21.615 bp at 5 ms and small selected sample preclude claiming proven savings |
| Backup / recorder recovery | The 16:02 backup completes at 16:05:36 UTC with 347 files; the independent remote comparison reports 347 matches and zero differences. The timer runs every 15 minutes with a 10-minute job budget. Both market recorders have fresh, unblocked status; cumulative prior disk-drop counters remain explicit in the raw receipts. NTP is synchronized; the 16:08 venue-clock probe reports +8.94 ms offset with ±11.10 ms uncertainty and no alert |

| Release image | SHA256 |
| --- | --- |
| Final engine, loaded in demo, mexc and hyperliquid (`859723b1`) | `1bfe82dd4b2823e4dc00c9cee84a29e2acca04ea17e99676d3bd4bd2f0be2695` |
| Final signal worker, loaded in demo, mexc and hyperliquid (`859723b1`) | `4dd91162971fc169878a57472cea0642d42dc44159a574563daa69d64cc435c6` |
| Final engine tools, installed (`859723b1`) | `2740d67458054877dcb679954c94556d991c799fcc2dc5866c1dd5f202cfa4c8` |
| Final staged release archive (`859723b1`) | `97ecf4fdea10fb90e459ccac8ceb459eb2c37b121e4f5b8b5de83aff4517961d` |
| Retained Stage A engine, retained | `9fc63d5344c9190cb700cef151b6af8c1082eecb64ee347395c1c01302f01ae6` |
| Retained Stage A signal worker, retained | `96fb34e84f174baa1acd931a35202f3d42009b3b8ac099e3916c4fabfa44caf8` |
| Retained Stage A engine tools, retained | `1794e243a263af0b77ec7cbc9639e8fc6aa997562356609e72398015b7d2b1a4` |
| Retained Stage A staged archive | `9765701173dabecb6258d216302bd58d80c420bbc81d53607e54a35d7c5bac2e` |

| Open position | Demo | Mainnet (engine stopped, unmanaged) | MEXC | Hyperliquid |
| --- | --- | --- | --- | --- |
| INJUSDT | none (long 59.5 stopped out at 5.803, 15:02:55 UTC) | long / 4.6 / 5.805, stop order working (venue read 15:09 UTC) | none | none |

| Sleeve ID | Mainnet (stopped by posture) | Demo (entries off) | MEXC (running, forward test) | Hyperliquid (running, forward test) | Repository authority |
| --- | --- | --- | --- | --- | --- |
| 0 | CARRY, stopped | CARRY, entries off | CARRY, entries off | CARRY, entries off | `configs/lane2_carry_hold_v7.json` |
| 1 | LONG, stopped | LONG, entries off | LONG, entries on | LONG, entries on | `configs/long_native_v12.json` |
| 2 | EXODUS, stopped | EXODUS, entries off | EXODUS, entries off | EXODUS, entries off | `configs/lane2_exodus_short_v1.json` |
| 3 | MAKER, quoting disabled, stopped | PROBE, enabled | none | none | `deploy/engine.mainnet.toml.template`, `deploy/engine.demo.toml.template`, `deploy/engine.mexc.toml.template`, `deploy/engine.hyperliquid.toml.template` |

| Setting | Repository authority |
| --- | --- |
| Capital, leverage, exposure and rolling loss | `configs/operational.json` |
| Native configuration | Rust `render-native-config`; generated template regions |
| Account identity and permission | Root-owned realm env/config on the host; authenticated account reader |
| Signal delivery | Durable spool plus `stream.sock` notification; worker owns unpublished/retained prefixes |
| Units and activation | `deploy/fleet_manifest.tsv`; [systemd inventory](deploy/systemd/README.md) |
| Equity and telemetry | Equity-recorder timer, one-minute sampling; [observability](docs/observability.md) |
| History | [CHANGELOG.md](CHANGELOG.md); dated archived entries retain prior incidents and deployments |

## Invariants

- Must distinguish a dated host observation from local source and test results.
- Must replace this snapshot after verifying an actual deployment; commit success alone does not update it.
- Must preserve append-only sleeve IDs, exact ownership and native protection.
- Must verify WAL/worker compatibility before selecting a previous binary.

## Operational Recipes

```sh
scripts/ops.sh status
scripts/ops.sh execution-study
scripts/ops.sh --help
```

[Operations](docs/operations.md) · [Engine](docs/engine.md) · [Data](docs/data.md) · [Trading rules](docs/trading_logic.md)
