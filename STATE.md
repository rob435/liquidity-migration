# Operational State

Current operational snapshot of the live trading fleet and host environment.

---

## 1. Live Deployment Snapshot

Verified against the running host and Grafana Cloud on 2026-09-04 18:13 UTC;
GitHub Actions state re-verified 19:16 UTC:

| Property | Value | Description |
| :--- | :--- | :--- |
| **Host** | `ip-208-84-103-4.my-advin.com` (`208.84.103.4`) | 4 vCPU, 8 GB RAM, 118 GB disk (72% used, 32 GB free; the watchdog's floor is 25 GB on `/var/lib`). |
| **Deployed Commit** | `65ee75a7` (transactional realm handover, deploy-queue repair, producer health, and persistent partial-shard debounce) | `main` carries newer commits the host does not run, among them `697341e4`, which makes a `degraded` signal-worker page name the transport input that decided it, and `10ed1bd2`, which carries the stream's epoch, gap stamp and fault counters across the rebuild an hourly universe refresh performs. Five more now join them, all on the recorder's blocked disk: `1d8fad9a`, which starts a retention pass at once instead of waiting out `RETENTION_INTERVAL_SECONDS`; the pruner's free-space headroom `d275885a`, which stops a pass ending on the exact floor `writable()` unblocks on (`host-681737fd16e1f806`); `fd604613`, which lets the pass that frees room open the writer's gate instead of leaving it shut until the next status tick; `06e17d4a`, which makes a pass that deleted and left the gate shut run the next pass at once instead of sleeping out `RETENTION_INTERVAL_SECONDS` with the only thread that frees room idle (`host-ecbac293ecc90d5e`); and this session's fix `3c1ebd22`, which credits each retry in that burst with what the burst already unlinked. That fifth defect is in `06e17d4a` itself and not on the host: the retry re-derives its deficit from the same statvfs whose disagreement made the successor owed, so it deletes the deficit again on every retry, back to back, until the tape holds no non-snapshot file. The 03:00 page shows the host in the state that fires it — 120.1 s with neither recorder writing a row, a 7-file pass already done, and free space still under the floor (`host-16171e3c5e186136`). A **sixth defect is in `3c1ebd22`**, fixed by `1702d14d`: a credited burst deletes its deficit once and ends on the pass that finds none left, which is the ordinary exit whenever the kernel released the unlinked blocks and the neighbouring recorder took them (the successor opens on `S2 + F1`, so it deletes nothing as soon as `S2 ≥ S1`), and it ends with the gate shut — while `_maintenance` armed `prune_now` on the crossing only, so the writer then waited out the full `RETENTION_INTERVAL_SECONDS`. That is the 330.3 s, 360.2 s and 390.3 s blocks already measured, which the five earlier fixes would have left in place. `1702d14d` arms the pruner on every blocked tick, bounding a block by one `status_interval_seconds` (30 s) plus a walk; it costs no extra tape, because a pass frees to `free_target` and no further, so the same foreign consumption rate takes the same tape at either cadence. **`06e17d4a` must not be deployed on its own**, nor `3c1ebd22` on its own; `1702d14d` carries all six. A **seventh defect is outside the recorder**, found by the 03:42 page and fixed by `7fe4fe0c`: the hourly uploader's `build_archive` wrote `<staging>/.{name}.tar.tmp` and removed it only by `os.replace` on success, so any failure left a partial archive — roughly one hour of tape — in `/var/lib/liquidity-migration/market-tape-upload/staging`, and nothing in the repository ever swept staging. That directory is on the filesystem the 25 GiB floor guards and outside both tape roots, so `Retention.prune`, which enumerates `self.root.rglob("*.zst")` (`market_tape/storage.py:386`) while reading free space for the whole filesystem (`:396`, `:433`), can neither count it nor delete it. The condition that triggers the failure is this incident: on a disk at the floor the uploader is not gated and dies of `ENOSPC`, and the pruner unlinks `.zst` files out from under a build that enumerated them, so `path.open("rb")` (`market_tape/pack.py:234`) raises `FileNotFoundError` mid-archive. Every hour at `*:10` leaves another orphan, the run ships none of its backlog, and the recorders answer by deleting tape that was never the problem. The 03:42 payload is what shows it: a 414-file Binance pass at 03:34:54 bought **zero** seconds of writing — Binance wrote no row for ≥71.2 s after it, Bybit for 260.5 s, `writable()` False on both — so about a gigabyte of free space was consumed or never released across a stretch in which the tape provably consumed nothing. `7fe4fe0c` removes the temporary on any failure and sweeps stray `*.tar` and `.*.tar.tmp` at the start of every run under the exclusive `upload.lock`. It does not touch the `engine` tree. The incident is also no longer episodic: the pair discarded **8.14 frames per row kept** across the 03:42 window against 4.14 at 03:21, each recorder getting one 30-second writing interval per three to six minutes, and cumulative tape discarded reached **26 327 113** frames. Whether staging accounts for the whole 25 GiB is the open host reading, and [CHANGELOG.md](CHANGELOG.md) holds the exact commands. `1d8fad9a`'s dispatched deploy, run `33928248402`, failed 5 s in at 23:06:57 UTC with every job's logs returning HTTP 404 and `vps` skipped. Re-dispatched on `9c53b12e` at 23:59:48 UTC as run `33931474693` after the floor crossing recurred: failed 7 s in, same signature, fifth consecutive refusal since 19:17 UTC. The headroom fix `d275885a` was dispatched at 00:12:15 UTC on 2026-09-05 as run `33932188757` and refused the same way 5 s later — `ci`, `rust` and `Deploy artifact` dead in 3 s with HTTP 404 log downloads and `vps` skipped. Re-dispatched on `c821585d` at 00:38:23 UTC as run `33933636343` after the floor crossed again, and on `fdc6bf7c` at 00:43:47 as run `33933927629`: both failed within 6 s with the same signature — `ci`, `rust` and `Deploy artifact` dead in 3 s, HTTP 404 log downloads, `vps` skipped — the seventh and eighth consecutive refusals since 19:35 UTC on 2026-09-04. Re-dispatched on `bb5d5ec4` at 01:01:12 UTC on 2026-09-05 as run `33934851698` after the 00:57 page: failed 6 s later, same signature, the ninth consecutive refusal, and on `b7bdbe17` at 01:03:22 as run `33934970737` for a tenth. The 01:32 page's fix was dispatched on `1f627520` at 01:49:01 UTC as run `33937280978` for an eleventh and failed at 01:49:43: `ci` and `Deploy artifact` dead 2 s in with HTTP 404 log downloads, `diagnose` and `disarm` skipped, `rust` 39 s in `queued` with no runner assigned before failing, and `vps` and the release-test job skipped. This session's fix was dispatched on `06e17d4a` at 02:11:19 UTC as run `33938359607` after the 01:53 page and failed at 02:11:25 for a twelfth: `ci`, `rust` and `Deploy artifact` all dead 3 s in with HTTP 404 log downloads, `diagnose`, `disarm`, the release-test job and `vps` skipped. Re-dispatched on `0af3fc29` at 02:34:52 UTC as run `33939474310` after the 02:30 page and failed at 02:34:59 for a thirteenth: `ci` dead in 3 s, `Deploy artifact` in 4 s, `rust` in 5 s, `diagnose`, `disarm`, `vps` and the release-test job skipped, every failed job's log download HTTP 404. Re-dispatched on `cdefebcd` at 02:41:10 UTC as run `33939774257` after the 02:33 page and refused at 02:41:17 for a fourteenth: `rust` and `Deploy artifact` dead in 3 s, `ci` in 4 s, four jobs skipped. Re-dispatched on `28dc27a6` at 02:56:17 UTC as run `33940468461` after the 02:48 page and refused at 02:56:23 for a fifteenth: `ci`, `rust` and `Deploy artifact` all dead 3 s in, `diagnose`, `disarm`, `vps` and the release-test job skipped, every failed job's log download HTTP 404. This session's fix was dispatched on `02062266` at 03:15:54 UTC as run `33941368676` after the 03:00 page and refused at 03:16:00 for a sixteenth: `Deploy artifact` dead 2 s in, `ci` and `rust` 3 s in, `diagnose`, `disarm`, `vps` and the release-test job skipped, every failed job's log download HTTP 404. Re-dispatched on `d2e21d83` at 03:25:29 UTC as run `33941811398` after the 03:21 page and refused at 03:25:33 for a seventeenth: `ci`, `Deploy artifact` and `rust` all created and dead 03:25:30 → 03:25:33, four jobs skipped, every failed job's log download HTTP 404. Re-dispatched on `10cb6aeb` at 03:39:20 UTC as run `33942452158` for an eighteenth, refused in 6 s, and on `95798f18` at 03:39:54 as run `33942475801` after the 03:18 page for a nineteenth, refused at 03:40:00: `ci` and `Deploy artifact` dead 3 s in, `rust` 4 s in, `diagnose`, `disarm`, `vps` and the release-test job skipped, every failed job's log download HTTP 404. Re-dispatched on `aa4dde54` at 03:58:24 UTC as run `33943296814` after the 03:51 page and refused at 03:58:29 for a twentieth: `ci`, `rust` and `Deploy artifact` all created 03:58:26 and dead 03:58:29, `diagnose`, `disarm`, `vps` and the release-test job skipped, every failed job's log download HTTP 404. Dispatched on `ce5f5a0a` at 04:00:04 UTC on 2026-09-05 as run `33943362290` after the 03:42 page and refused at 04:00:10 for a twenty-first consecutive time: `rust` and `Deploy artifact` dead 3 s in, `ci` 4 s in, `diagnose`, `disarm`, `vps` and the release-test job skipped, every failed job's log download HTTP 404. The SSH path `EXPECTED_COMMIT=7fe4fe0c1e115f8889eb73dc818726de82421d82 scripts/ops.sh deploy` installs all six recorder fixes and the uploader's leak fix, and needs no runner. It restarts the funded engine: `1702d14d` already carries `697341e4` and `10ed1bd2`, so its `engine` tree differs from the deployed `65ee75a7` and the fingerprint hands over both realms. Every commit since is `CHANGELOG.md` and `STATE.md` only, so the current tip installs the identical `engine` tree. Those fixes are **merged and undeployed**: every GitHub Actions run since 18:03 UTC — including `697341e4`'s checks (`33910211410`) and its dispatched deploy (`33910262256`) — fails within seconds with no job logs, because the account's payments failed. Deploying it hands over both realms (the fingerprint hashes the whole `engine` tree), so the funded engine restarts; the SSH path `EXPECTED_COMMIT=10ed1bd2488570055a37b53b7b92dd959e863850 scripts/ops.sh deploy` needs no runner. Exact release marker written 17:34:41 UTC 2026-09-04. Run `33900447763` passed CI, Rust, and its verified release artifact, then GitHub refused to start the VPS job because account payments failed. The VPS recovery recipe installed that same artifact and commit without restarting the unchanged funded process. Fresh verification: all engines, workers, recorders, and Telegram controls active; every liveness scope returns `ok`; `systemctl --failed` is empty. Mainnet remains on uninterrupted process commit `218905d4`, with 5 positions, `may_open=true`, no rolling-loss trip, no strategy errors, and no pending flatten. |
| **Rollback Target** | `16d52f88f54ee9bd936b821a36ffeecc7852240a` | Stored in `/opt/liquidity-migration-engine/previous-commit`. |
| **Funded Status** | `real-money armed` | `REAL_MONEY=true`. The engine is running with healthy heartbeats. |
| **Incident Response** | Telegram + Claude Code routine + external dead-man | Demo, mainnet, and independent host watchdogs run every 3 min. A 16:57 UTC live delivery drill returned `telegram accepted`, created no-change session `session_01Jf15281KD9pJvLF32sFJGw`, and returned `dead-man accepted` in 2.9 s. Telegram remains the human pager, trade feed, and constrained control surface; the routine receives no venue credentials and can diagnose or deploy only through `vps-deploy.yml`. Delivery works, but GitHub currently refuses to start that workflow's VPS job because of failed account payments, so autonomous host diagnosis and repair remain externally blocked until billing is fixed. The demo worker paged a second time at ~20:25 UTC on 2026-09-04 (`demo-0922e9f30da3bf98`, same ref, 62 min into a fresh process): the routine narrowed it by elimination to one Bybit frame drought over `mark_max_age_ms` = 30 000 ms and under the socket's 45 s idle tolerance, and the clause that would name it in the page itself ships with undeployed `697341e4`. One open host reading, in [CHANGELOG.md](CHANGELOG.md). Route files are separated and root-owned mode `0600`. [docs/notifications.md](docs/notifications.md). |
| **CI / Deploy Gate** | Private repository; release work is explicit | A 24-hour audit found 90 workflow runs and about 2,023 billed job-minutes: every push repeated Python, Rust, release-build, soak, and benchmark work. Pushes now start no cloud job. Code PRs run Python and Rust gates; docs-only PRs run none. `deploy` alone builds an exact-SHA artifact after Python and Rust gates; `qualify` alone runs release tests, soak, and benchmark; verify, rollback, diagnose, and disarm do not build. New artifacts retain for 2 days; 98 obsolete archives were deleted, leaving the latest deployed, rollback, and live-process archives at 37.1 MiB total. Run `33911407276` proved the build-free `verify` routing, then GitHub refused its sole `vps` job before runner assignment with the failed-payment or spending-limit annotation. Re-checked 21:36 UTC 2026-09-04: `verify` run `33921858031` on `e2345ca4` failed after 4 s with `vps` again the only scheduled job, every other job skipped, and no log content at all (log download returns HTTP 404), so the job never started. Hosted capacity remains externally blocked until GitHub restores it or a separate private Linux runner is registered. The funded VPS is never an Actions runner. |
| **Equity History** | `liquidity-migration-equity-recorder.timer`, every minute | One sample per engine, signal worker, and tape recorder goes first to `/var/lib/liquidity-migration/equity/<kind>-<realm>-<YYYY-MM>.jsonl`; a source with no readable heartbeat is written as `state=absent`. `scripts/ops.sh curve mainnet` reads it on the host. Each run then pushes six samples to Grafana Cloud stack `proudtortoise1017` through `influx-prod-55-prod-gb-south-1`; `/etc/liquidity-migration/observability.env` is `root:liquidity-migration` mode `0640`, and the dedicated access policy grants only `metrics:write`. Verified at 18:12 UTC on 2026-09-04: three consecutive journal rows say `recorded and pushed 6 samples`. Both workers are transport-healthy and fully covered while their bounded CARRY cold fill reports `starting`; Bybit is 16/16 connected and Binance 10/10, with zero reconnects, frame drops, or disk drops and no blocked disk. That last clause expired at 22:54 UTC and has not come back: free space on `/var/lib` sits at both recorders' 25 GiB floor and crosses it repeatedly — 22:54, at least once unpaged, 23:50, and by 00:01 on 2026-09-05 it is no longer an episode but a 30-second oscillation, each recorder recovering for exactly one status tick before blocking again (`host-681737fd16e1f806`). The 00:36 page is the same floor with a longer clean stretch in front of it — Binance wrote 16 minutes and Bybit 12 with no disk drops at all, then both crossed 0.75 s apart at 00:35:59 and 00:36:00. The 00:57 page settles what that one could not: it catches six retention passes inside the window, spaced 302.4–304.8 s apart, which is the deployed 300-second clock and nothing waking on the crossing, and five of the six bought the writer nothing at all — the 00:53:44 pass unlinked 91 files and the disk was blocked again 16 s later. Five defects, all fixed on `main` and none deployed: the pruner waited out its 300-second interval (`1d8fad9a`); it stopped deleting at exactly the floor `writable()` unblocks on, so a pass returned zero headroom and the next tick re-blocked (`d275885a`); and the pass that frees room could not open the writer's gate, which only `_maintenance` ever cleared, so every crossing cost a full `status_interval_seconds` of tape on a disk that already had space; and a pass that deleted and left the gate shut ran no successor, because `_maintenance` arms `prune_now` on the crossing only and the blocked `_write_loop` never reaches an append to fail on, so the pruner slept a full `RETENTION_INTERVAL_SECONDS` while the writer was blocked (`fd604613`'s successor; measured on the Bybit unit at 01:40:59, 306.2 s between its own passes with 499 875 frames discarded and 30 rows written in the 180 s after the short pass). The 01:32 page measures that last one on the Bybit unit: the 01:30:50 pass freed room, the gate opened 15.06 s later at the 01:31:05 tick, and the unit wrote 54 rows in an interval that carried 95 657 once it was open. That is what sets the 30-second period of the oscillation — the status interval, not the disk, and only when a neighbouring pass frees room. The 02:30 page is the crossing with no neighbour: the Bybit unit blocked at 02:28:10, no `retention removed` line appears from either recorder across the whole 840.7 s excerpt, and 120.1 s later the writer was still gated with `rows` frozen at 57 611 732, having discarded 345 469 frames (2 876/s) where the interval before it wrote 3 146 rows/s. The 02:33 page, three minutes later on the Binance ref, holds the end of that same block: Bybit's own 02:31:48 pass ran 4 files, the gate opened at the 02:32:10 tick, and the block was 240.2 s and 691 512 frames — not 300 s and 863 000. That page also settles `d275885a` on its own terms: Binance's 02:29:23 pass unlinked 187 files and Bybit's unlinked 4, and both bought the recorder that ran them exactly one status tick, because both stop on the number `writable()` unblocks on. The 187-file pass did not open Bybit's gate at all — 46.9 s later Bybit still read the disk as full, Binance having resumed 7 s earlier into zero margin — and Binance's 02:32:03 unblock followed no pass of its own but Bybit's, 14.8 s earlier: on one filesystem each pruner is the other's only source of room between its own 300-second walks. The 02:48 page withdraws that page's one generalisation. There is **no ceiling on a block**: Bybit was blocked 02:37:40 → 02:43:10, 330.3 s and 903 098 frames, straight through its own 02:41:58 pass, whose 4 files bought nothing because Binance's gate opened 4.8 s later and took the margin. Nor does pass size buy recovery — Binance's 230-file pass opened its gate in 7.3 s, its 290-file pass took 95.1 s and its 27-file pass 32.7 s — because what sets recovery is the margin at the stopping point, which is zero by construction. The 290-file case is also where the payload stops: both recorders were blocked across it, so the tape did not take that room back, and either the filesystem had not released the unlinked blocks or a non-tape writer on `/var/lib` did. The 03:00 page pushes the maximum block to **390.3 s and 1 100 454 frames** (Bybit, 02:45:41 → 02:52:11, straight through its own 7-file pass at 02:47:04), and pushes pass size the rest of the way out of the picture: Binance's 410-file pass at 02:49:33 opened its gate in 0.17 s and it was shut again at the next tick, while its 106-file pass at 02:54:36 opened nothing and the block ended 177.8 s later on Bybit's 4-file pass. A file count never measured room freed for the floor in any case — `prune` deletes for age and for `max_bytes` in the same walk — and the manifest's per-unlink `compressed_bytes` and `age`/`disk_limit` reason is where that separates. That page also finds the **fifth defect, and it is in `06e17d4a` rather than on the host**: the owed-successor retry re-derives its deficit from the same statvfs whose disagreement made the successor owed, so it deletes that deficit again on every retry, back to back, until no non-snapshot file is left. Two windows in the payload show the condition live — 02:47:34 → 02:49:34 and 02:58:11 → 03:00:34, in both of which neither recorder wrote a row, a pass had already run, and free space stayed under the floor — so deploying `06e17d4a` alone would delete the tape roots. Fixed in `3c1ebd22`, which credits each retry with what the burst already unlinked. The 03:21 page finds no sixth defect and settles two things instead. The three incident ids this log has been tracking are one incident under three names: `incident_id` is `sha256(scope + the newly-due CRITICAL refs)[:16]` (`scripts/runtime/check_fleet_liveness.py:806-807`), so `host-681737fd16e1f806` = `{capture-disk}` (Bybit), `host-16171e3c5e186136` = `{capture-disk:forward-market-binance}` and `host-ecbac293ecc90d5e` = both, all three re-derived exactly; the id names whose cooldown cleared first, not a distinct fault. And it prices the margin: one 8-file Binance pass at 03:19:46 ended a 90.1 s block on *both* units, each of which then wrote exactly one 30-second status interval (Bybit 87 124 rows, Binance 35 005) and re-crossed — so the room a pass leaves is ~30 s of the pair's own output, ≤19.8 MB at 661.5 KB/s of inbound wire bytes. That makes `d275885a`'s 1.25 GiB of headroom **≥34 minutes between crossings instead of 30 seconds**, a ≥67× cut, rather than a refinement. Both gates opened on a status tick and not on the pass (18.35 s and 28.23 s of the 90 s spent waiting for one, `fd604613` undeployed), and neither pruner walked again in the 109 s to the last line (`1d8fad9a` undeployed). Crossings and resumptions are 9.86 s and 9.88 s apart — `status_interval_seconds` phase, not the disk: the two recorders cross as one filesystem event. The journal also settles half the open host question without SSH: Bybit logged no pass at all in 990.8 s, of which 810.6 s were unblocked, and a pass logs whenever it deletes anything (`market_tape/record.py:1166`), so neither tape is at its `max_disk_gb` cap and `min_free_disk_gb` is what binds. Cumulative tape discarded and still climbing at the last journal line: 16 065 875 frames (Bybit) and 5 827 039 (Binance), **21 892 914** together, of which 1 161 705 came after the 03:00 page — 937/s over ~20.7 min, well under that page's 3 268/s because the floor went uncrossed for 13.5 minutes and then was crossed again. The incident was episodic through 03:21 — 810.6 s clean at 3 156 rows/s, then a 3-minute blocked phase in which the pair kept 122 129 rows and discarded 505 598, 4.14 discarded per row kept. **The 03:33 page ends that.** No clean stretch survives in it: all ten resumptions across the two units last exactly one 30-second status interval, Bybit is blocked 74.2 % of 930.7 s and Binance 85.2 % of 810.5 s, and the pair discarded 2 481 366 frames in the 720.5 s since the 03:21 page — **3 444/s, the worst sustained rate of the incident**, past the 03:00 entry's 3 268/s. Cumulative **24 374 280** frames, 3.27 discarded per row kept. It also measures the sixth defect's premise from the Bybit side: Binance's 03:24:49 pass satisfied `prune`'s own accounting and `writable()` still read below the floor for **76.0 s across three status ticks**, and Bybit's 03:30:45 → 03:32:45 block ended with **no deletion on either unit** in the 173.8 s before it, on one filesystem where Binance read the disk blocked 10 s either side of that unblock — free space moving under a tape the pruner was not walking, which is what `1702d14d` arms on ([CHANGELOG.md](CHANGELOG.md)). No engine unit is named or affected; the floor is the reservation held for mainnet's WAL and it is intact. `min_free_disk_gb`, not `max_disk_gb`, is what governs tape size on this host, so the crossings repeat until the six fixes deploy. The 03:18 page reaches the same conclusion from the Binance side — 840.5 s clean with `disk_dropped` frozen and no `retention removed` line across three passes due on the 302.5 s clock — and adds the sixth defect above. **The 03:51 page adds no seventh defect and one measurement**: a pass can open the neighbour's gate and not its own. Bybit's 03:48:01.724 pass unlinked 6 files, Binance's writer opened 34.4 s later and wrote 63 495 rows into the room with zero frames discarded, and Bybit's own writer stayed gated **164.7 s** longer — five consecutive ticks reading below the same 25 GiB floor Binance read above at 03:48:36 and 03:49:06, on one filesystem, while Bybit discarded 434 506 frames at 2 895/s. Room freed at the zero-margin stopping point is a common pool: the first unit to sample it takes all of it and the unit that ran the walk has no claim on it, which is what `d275885a`'s 1.25 GiB margin removes. The pair is blocked 84.6 % (Bybit, of 780.6 s) and 78.6 % (Binance, of 840.5 s), cumulative **28 020 904** frames discarded, and the rate since the 03:33 page is **3 372/s** — below that entry's 3 444/s, so the escalation has flattened rather than reversed. One unproven risk is recorded for after the deploy, not fixed: `_retention_loop` resets `credit = 0` per burst (`market_tape/record.py:1144`), so where the kernel's statvfs lags a pass's unlinks past 30 s, `1702d14d`'s per-tick bursts re-derive the deficit — bounded at one deficit per 30 s, and the deployed 300-second version of the same shape has not drained either tape root in 13 hours ([CHANGELOG.md](CHANGELOG.md)). Once they deploy the tape self-trims to the disk: the headroom is 1.25 GiB against a worst case of 210 MB written per 300 s interval (`projected_gb` 1370.9 + 443.0 = 1813.9 GB/month at 00:57 is inbound wire bytes, which compression only reduces), about 6× margin, and `prune_now` covers a burst. The caps in `deploy/capture/*.toml` stay the owner's size decision — 60 + 18 GB of tape plus the floor is 105 GB of a 118 GB disk — but after the deploy they govern how much history is guaranteed, not whether the recorders block. Whether the tape or non-tape growth took the room is the open host reading, and the 02:48 and 03:00 pages give it windows to look at: `scripts/ops.sh status`, `scripts/ops.sh curve mainnet 240`, then `df -h /var/lib` and `du -sh` over the two tape roots, the mainnet engine's state directory and `/var/log/journal`, plus the manifest's own bytes-freed-per-reason sum ([CHANGELOG.md](CHANGELOG.md)). If the tape roots sum well under their 60 + 18 GB caps while the disk is at the floor, the caps are not the dial to turn. Dashboard UID `liqmig-fleet` is a 15-panel operator view bound to `grafanacloud-proudtortoise1017-prom`: four live status tiles; separate demo/mainnet equity and open-exposure cards with exact USDT values and independently scaled sparklines; execution activity; p99 order-path latency; current freshness; capacity; and fault state. [docs/observability.md](docs/observability.md). The order path is measured on a clock: the demo `probe` sleeve rests one order at :00/:15/:30/:45 and the sampler at :20 reads the engine's 60-second latency ledger, so each probe is caught exactly once. Readings 16:45 and 17:00 UTC: `end_to_end` p50 11.84 / 11.29 ms — market event to submitted order, which stops at the submit. `wire` (11.74 / 11.20 ms) is the whole venue task and so contains the round trip; `ack` is empty on demo because every `place` in the demo WAL carries no transport stamp, while 317 of 320 mainnet places do and report a round trip of p50 3.74 ms, p99 59.48 ms, worst 429.76 ms. `engine latency --wal` is the authority. |
| **Signal IPC** | spool row + `stream.sock` doorbell | Every observation is a spool row first; the socket frame only saves the engine its next poll. Worker generations were renewed on both realms on 2026-09-03 after the desync; the old `g805c44f0…` (mainnet) and `gc4d0071f…` (demo) cursors stay in the WAL. |

---

## 2. Active Strategy Sleeves & Dials

| ID | Sleeve | Status | Rules & Dials | Execution Mandate |
| :---: | :--- | :--- | :--- | :--- |
| **0** | **CARRY** | Active | `configs/lane2_carry_hold_v7.json` | $3\times$ notional, $5\times$ leverage, $35\%$ stop. Entry $\le -10\text{ bp}$, exit $>-3\text{ bp}$. |
| **1** | **LONG** | Active | `configs/long_native_v12.json` | $6.0\times$ operational mult, top-10 volume rank, $15\%$ price velocity, $3\times\text{ATR}$ stop, $3\text{d}$ hold. |
| **2** | **EXODUS** | Active | `configs/lane2_exodus_short_v1.json`| Consumes CARRY pre-settlement event. Enters short; covers at $S+60\text{m}$. |
| **3** (mainnet) | **MAKER** | Disabled | `configs/lane2_toxic_flow_quoter_v1.json` | Microstructural market making on `AGIUSDT` (`quote_enabled = false`). |
| **3** (demo) | **PROBE** | Active | `[[strategy]] probe` in `deploy/engine.demo.toml.template` | Order-path benchmark, not a trading sleeve: one venue-minimum post-only `BTCUSDT` buy 3% under the bid every 15 min on the wall clock, pulled 2 s later, so the latency ledger holds a measurement the :20 sample reads. A fill is closed at market and shows only as an entry blocker. Never a page or a Telegram message. Mainnet has no probe. |

### Shared Account Risk Dials (`configs/operational.json`)
* **Gross Exposure Ceiling**: $5.0\times$ equity.
* **Initial Margin Ceiling**: $1.0\times$ equity.
* **Capital Reference**: Floating equity (floored at $\$100$).
* **Rolling-Loss Circuit Breaker**: $0.10$ ($10\%$ of capital reference lost in 24h trips emergency entry halt). A trip survives a restart, is reported by `rolling_loss_tripped`, and shows every sleeve as `entries_enabled: false`.

---

## 3. Fleet Health & Systemd Unit Matrix

| Systemd Unit | Realm | Health State | Heartbeat SLA |
| :--- | :--- | :--- | :--- |
| `liquidity-migration-engine.service` | Demo | Active | $\le 5\text{s}$ fresh |
| `liquidity-migration-engine-mainnet.service` | Mainnet | Active | $\le 5\text{s}$ fresh |
| `liquidity-migration-signal-worker-demo.service` | Demo | Active | $\le 5\text{s}$ fresh |
| `liquidity-migration-signal-worker-mainnet.service`| Mainnet | Active | $\le 5\text{s}$ fresh |
| `liquidity-migration-forward-capture.service` | Bybit | Active | The tape that gets replayed, shaped for the sleeves' exits first. Domain is crypto only: 517 USDT perpetuals, stocks/ETFs/commodities in no tier. `core` = LONG's live band (turnover rank ≤ 120, leaves below 160, held 96 h after last ranking inside 120) with `book:50` + prints + ticker + liquidations; `crowded` = CARRY's whole hold zone (predicted funding ≤ −3 bp, held 72 h) with book + prints; discovery tiers (`overheated`, `movers`, `bursting`, `surging`, `flooding`, `levering`) with book + prints on what bandwidth is left; `wide` = every other name with **prints**, ticker and liquidations. `monthly_gb = 2400`; shed order `overheated` → `wide:trades` → discovery books → discovery prints; `core:*`, `crowded:*` and every ticker are never shed. Books re-anchor each UTC hour, so every uploaded hour replays on its own. Writer queue `queue_frames = 131072`; shards read frames with websocket-client's pure-Python UTF-8 validation off (`json.loads` rejects a bad frame anyway), which halves receive CPU per frame. |
| `liquidity-migration-forward-capture-binance.service`| Binance | Active | Cross-venue reference only, **no order book**: ticker (funding rate, mark, index) on all ~510 USDT `PERPETUAL`s (`TRADIFI_PERPETUAL` refused) plus trades on the acting tiers; `crowded` mirrors Bybit's −3 bp / 72 h trigger so the two venues' trades line up. `monthly_gb = 700` against ~480 measured. |
| `liquidity-migration-telegram-controls.service` | Global | Active | Polling authorized chats |
| `liquidity-migration-trade-notify.timer` | Global | Active | 5-minute trade scanning |
| `liquidity-migration-equity-recorder.timer` | Global | Active | 1-minute equity and recorder sampling. `independent`: runs through fleet restarts and funded stops, which is what lets it record them. |

---

## 4. Operational Invariants & Reference Runbooks

* **Runbook & CLI**: See [docs/operations.md](docs/operations.md).
* **Architecture & IPC**: See [docs/architecture.md](docs/architecture.md).
* **Engine Internals**: See [docs/engine.md](docs/engine.md).
* **Data & Tapes**: See [docs/data.md](docs/data.md).
* **Trading Logic**: See [docs/trading_logic.md](docs/trading_logic.md).
* **Observability**: See [docs/observability.md](docs/observability.md).
