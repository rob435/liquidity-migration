# Operational state

## Purpose

Record the latest verified host snapshot and the source of each operational setting.

## Spec Tables

| Observation | Value |
| --- | --- |
| Verified at | 2026-09-09 15:29–15:31 UTC by direct host reading after [deploy run `34368381679`](https://github.com/rob435/liquidity-migration/actions/runs/34368381679) (`42dd7446`, `deploy-ok` 15:27:45 UTC): `deployed-commit` `42dd7446d81e616e76bf1101a5c36d5ebf063919`, `previous-commit` `7f0214f7b8318bad1526d09c5b3adc521ca30496`; demo engine (pid `3728981`, `engine_commit=42dd7446`, `uptime_s=427`) and mainnet engine (pid `3730852`, `uptime_s=87`) both `may_open=true`, `strategy_errors=[]`, heartbeats 3–4 s old; `signal-worker-demo` pid `3728921`, `signal-worker-mainnet` pid `3730794` active; `engine-mexc`, `signal-worker-mexc`, `mexc-liveness.timer`, `engine-hyperliquid`, `hyperliquid-liveness.timer` all `disabled inactive`; `storage-reclaim.timer` `enabled active`, first fire 15:41:00 UTC; `backup.timer` on cadence. `systemctl --failed`: demo, mainnet and host liveness, all on the refused Telegram route below |
| Prior reading | 2026-09-09 08:10:24–08:10:52 UTC through the `diagnose` workflow ([run `34327669940`](https://github.com/rob435/liquidity-migration/actions/runs/34327669940)), read on incident `host-22826ce0bb838311` as the post-action receipt for the watchdog fix. `systemctl --failed` lists four units — demo, host, mainnet and mexc liveness — each exiting 1 on `CRITICAL telegram: cannot deliver alerts (HTTP 400 (Bad Request: PEER_ID_INVALID))`. The mexc page now names its own cause: `reports 'degraded': signal spool refuses new files in class 'current'; LONG cycle is 3936s old (limit 180s)` at 08:09:28, 08:09:59 and 08:10:30. The stall is otherwise unchanged — `last_long_cycle_completed_wall_ts_ms` still 07:04:53.362, `spool_files=170` with `spool_class_files.current` 8 of 8, `long_output_sequence` still 18, and the spool listing still 172 files (`funding_update` 161, `llm_gate_candidates` 5). Demo and mainnet workers read `ready` with `spool_files=1`. Engines: demo pid `3630462` on `5b9b72b` at `uptime_s=323`, mainnet pid `3558832` and mexc pid `3559376` still the 03:40 `d835b62` build at `uptime_s` 16233 / 16195, all `NRestarts=0`, positions 4 / 4 / 0, `may_open=true`, `strategy_errors=[]`, `orders_sent=0`. No realm prints an `ok scope=` verdict in this reading, which is `5b9b72b` withholding it from a run that cannot route. The reading below is the 07:26:44–07:26:58 one it replaces; everything this reading does not re-read keeps that provenance | 
| Earlier reading | 2026-09-09 07:26:44–07:26:58 UTC through the `diagnose` workflow ([run `34323854875`](https://github.com/rob435/liquidity-migration/actions/runs/34323854875)), read on incident `mexc-d62940e951288d4c`, the mexc watchdog's own page for the same fault as `host-22826ce0bb838311`. This is the first reading to carry the spool class inventory, and it names the block exactly: the mexc worker holds `spool_class_files` `{catchup: 1, current: 8, lifecycle: 161, other: 0}` against caps `{1024, 8, 2048, 1024}`, so the `current` class is at its **file** cap while its bytes read 6,168,277 against a 369,098,752 soft threshold — a file block, not a byte block. `spool_files=170`, `spool_bytes=8,176,257`, `replaceable_outputs_coalesced=6517`, `spool_backpressured=false` with `spool_backpressured_classes=["current"]`. Its LONG cycle is still frozen at 07:04:53.362, 1393 s at this reading, `long_output_sequence=18`, `carry_output_sequence=179`, and it reports `degraded`. The demo worker, up the same 3.5 h, reads `spool_files=1`, `current: 1`, `lifecycle: 0`, 55 coalesces, `ready`, both lanes inside 35 s — so the mexc spool is not draining at all and 161 undrained `funding_update` files have piled up for hours. Nothing is impaired in trading: the mexc engine holds no position, `orders_sent=0`, `entry_blockers=0`. The reading below is the 07:10:53–07:11:08 one it replaces and is the fuller unit-by-unit snapshot; this one re-reads only the worker digests, so everything else keeps that reading's provenance | 
| Oldest kept reading | 2026-09-09 03:43:06 UTC through the `diagnose` workflow ([run `34308168256`](https://github.com/rob435/liquidity-migration/actions/runs/34308168256)), the post-deploy receipt for `d835b62` and read after the eighth `may-open:` mexc page at ~03:21:3x: arming and readiness, every engine, worker and liveness unit's state, all three running engine heartbeats, all three worker heartbeats, every liveness unit's recent journal and the mexc engine's journal from 03:24:52 through 03:42:09. All three engines read `engine_commit=d835b62`, `may_open=true`, `strategy_errors=[]`, `orders_sent=0` at PIDs demo `3557650` / mainnet `3558832` / mexc `3559376`, uptimes 508 / 169 / 130 s, `NRestarts=0`, `stream_resets` 0 / 0 / 0 — all three generations are from this handover, so no engine here has yet reached its first private-stream resync. All three workers read `ready`, none backpressured, every carry cycle under a minute old. `systemctl --failed` reads `0 loaded units listed.` in this reading. Watchdogs after the deploy: `ok scope=mexc units-and-heartbeats-healthy` at 03:41:45, 03:42:15 and 03:42:46, `ok scope=host units-and-heartbeats-healthy` at 03:41:52, and demo and mainnet `ok … warnings-present-no-critical` on their rolling-loss NOTICEs at 03:42:15 and 03:42:46. The equity recorder pushes 10 samples at 03:40:21, 03:41:21 and 03:42:21. The 02:42:54 reading it replaces is [run `34304281101`](https://github.com/rob435/liquidity-migration/actions/runs/34304281101), taken after the seventh page on the previous generation, and the 02:32:48 reading before it is [run `34303611711`](https://github.com/rob435/liquidity-migration/actions/runs/34303611711). The two intervening on-call readings, each after its own page, are [run `34302281315`](https://github.com/rob435/liquidity-migration/actions/runs/34302281315) (02:12:36–02:12:50) and [run `34302946042`](https://github.com/rob435/liquidity-migration/actions/runs/34302946042) (02:22:33–02:22:46), and the 01:52:48 reading ([run `34300920725`](https://github.com/rob435/liquidity-migration/actions/runs/34300920725)) precedes them. The older reading, kept for what none of these re-read, is 01:34:03 ([run `34299689880`](https://github.com/rob435/liquidity-migration/actions/runs/34299689880)), the post-deploy receipt for `beef5bc5`, which read the deployed commit and the same units, heartbeats and journals at uptimes 530 / 196 / 156 s with every realm watchdog healthy and no `worker-status:` page. `systemctl --failed` is the 00:09:31 reading ([run `34293758810`](https://github.com/rob435/liquidity-migration/actions/runs/34293758810)). Position counts and rolling-loss figures are this reading. The hyperliquid realm's rendered files, directories, user, `engine venues` and account reads keep their 23:34–23:35 SSH provenance; stops, WAL inventory, swap and the MEXC wallet keep their earlier provenance and are not re-read here |
| Evidence | [deploy run `34368381679`](https://github.com/rob435/liquidity-migration/actions/runs/34368381679) (dispatched 15:09:31 UTC on green checks [run `34367272968`](https://github.com/rob435/liquidity-migration/actions/runs/34367272968), `ci`, `rust` and the venue-conformance run all green): `vps` job 15:20:54–15:27:45, release bytes verified 15:21:20, both captures `unchanged-left-running`, demo handover 15:21:3x, the full 300 s demo soak 15:22:06–15:27:06, `atomic mainnet handover` 15:27:08, `mexc posture=stopped in deploy/realms.tsv: units stay stopped` 15:27:44, `hyperliquid armed but the installed engine reports hyperliquid_mainnet readiness=live-canary: units stay stopped` 15:27:45, `deploy-ok commit=42dd7446…`, `rollback-target 7f0214f7…`. Earlier receipts for the day are in CHANGELOG |
| Host | `208.84.103.4` |
| Deployed commit | The host checkout and both running realms are `42dd7446d81e616e76bf1101a5c36d5ebf063919`, handed over by [deploy run `34368381679`](https://github.com/rob435/liquidity-migration/actions/runs/34368381679): demo worker pid `3728921` and engine pid `3728981` up 15:21:3x–15:21:5x, mainnet worker pid `3730794` and engine pid `3730852` up 15:27:1x–15:27:3x. The engine tree changed (venue module, intake fix, canary fix, WAL retention floor), so both Bybit realms paid a restart. Stored rollback target is `7f0214f7b8318bad1526d09c5b3adc521ca30496`. This is the first deploy rendered from `deploy/realms.tsv`: mexc stopped by posture, hyperliquid stopped by readiness |
| Hyperliquid realm | `hyperliquid_mainnet` is armed in `/etc/liquidity-migration/hyperliquid-mainnet.env` (master account address, approved API-wallet key, `REAL_MONEY=true`) and the installed engine reports it `live-canary`; the realm table also holds it `stopped`, so `liquidity-migration-engine-hyperliquid`, `signal-worker-hyperliquid` and `hyperliquid-liveness` are installed, disabled and inactive. The installed `engine-tools` carries the canary fix (exact-spec pricing, a create error that survives a failed cleanup, `unknownOid` after an unacknowledged create read as never accepted); the owner's rerun is pending and no Hyperliquid order lifecycle has been observed. Account `0xcef3…`: 52.4 USDC in the main perps clearinghouse, no positions or orders. `configs/signal-worker.hyperliquid.json` sets `sources.public_venue = "hyperliquid"`; the worker's native module has not run on the host yet |
| MEXC signal intake | Fixed in the engine (`42dd7446`: an unfollowable name's subscription is dropped from its batch), not yet drained: the mexc realm is `stopped` by posture, so `/var/lib/liquidity-migration/signals/mexc` still holds 172 files (161 `funding_update`, the CARRY batch that froze the lane, five gate rows). The drain happens on the realm's next start and sends nothing (CARRY entries off, every row far past its validity) |
| MEXC worker LONG stall | Closed 08:52:52 UTC by the `7f0214f` handover, which restarted the mexc pair; the incidents it paged were `mexc-d62940e951288d4c`, `host-22826ce0bb838311` and `host-e8a8b4a3bf4e85c7` — three pages, one fault. The fresh process re-read the spool inventory and the engine retired exactly one `current` file, which is all the LONG watermark needed: at 08:55:33 the worker reads `status=ready`, `spool_backpressured_classes=[]`, `spool_class_files` `{catchup: 1, current: 7, lifecycle: 161, other: 0}`, `last_long_cycle_completed_wall_ts_ms` 1 s old and `long_output_sequence` 19 against the 18 it held frozen from 07:04:53. Contained, not repaired: the class sits one file below its 8-file cap and the intake row above is still open, so the same block returns the moment a destination stops consuming again |
| MEXC realm | `mexc_mainnet` is armed and the installed engine reports it `live-proven`; the realm table holds it `stopped`, so deploy stopped and disabled `liquidity-migration-engine-mexc`, `signal-worker-mexc` and `mexc-liveness.timer` at 15:27:44 UTC (`mexc posture=stopped in deploy/realms.tsv: units stay stopped`). No positions, empty futures wallet, `orders_sent=0` over its life. `configs/signal-worker.mexc.json` sets `sources.public_venue = "mexc"`; its native module has not run on the host yet. Flipping `posture` to `running` in `deploy/realms.tsv` and deploying starts it |
| Funded permission | Mainnet remains armed with sole leverage authority; configured CARRY/LONG/EXODUS entries remain enabled in both Bybit realms, with demo PROBE enabled. Both Bybit engines report `may_open=true`. Both rolling-loss restrictions are tripped and every sleeve refuses entries under them: at 03:43:02 UTC mainnet reads −16.00052509 USDT against a 10 USDT limit and demo −209.49862852 against 157.801152702, and the demo engine journal shows `risk refused the order tag=carry-native reason="RollingLossTripped"` on every minute from 03:31:17 through 03:42:17. The mexc realm's own restriction is untripped. `reduce_only` exits are admitted ahead of the check |
| CARRY holding | Both realms hold fixed quantity targets until the next daily decision; intraday funding, upcoming-book drop and pre-settlement exits are disabled. Current HEMI anchors remain demo `14131` / mainnet `1161`; native stops and explicit reductions remain active. The `FLOCKUSDT` exit tombstone for decision `1788825600000` remains intact; no new pre-settlement fires feed EXODUS |
| Alert routing | Telegram alert delivery is refused fleet-wide with `HTTP 400` from 06:46:12 UTC; the demo, mainnet and mexc liveness units are the ones failed at 07:28:50, each exiting 1 on a refused send, and no watchdog page reaches the owner by Telegram. The last accepted delivery is not established — a send is attempted only when an alert comes due. The on-call fire path is unaffected and fired at 06:48:40 for `host-51b05439c4f09794` and again at 07:08:08 for the mexc page. The refusal reason is known on the host as of 07:28:28: `CRITICAL telegram: cannot deliver alerts (HTTP 400 (Bad Request: PEER_ID_INVALID))`, read after [deploy run `34322539866`](https://github.com/rob435/liquidity-migration/actions/runs/34322539866) succeeded at 07:28:04. Telegram is refusing the destination, not the message, so no repository change clears it: the alerts chat id in the root-owned realm env must be repointed by the owner. The on-call fire path also fired at 07:28:02 for `host-bf5dcb6544d0dfdc`, the second read of this outage. The delivery drill in [notifications.md](docs/notifications.md) §4 is the check that confirms the repointed id without waiting for an alert to come due |
| Runtime state | Engine PIDs demo `3728981` / mainnet `3730852` and worker PIDs demo `3728921` / mainnet `3730794`, from the 15:21–15:27 `42dd7446` handover; both engine and worker units active at 15:30 UTC with zero restarts, `engine_commit=42dd7446`, `may_open=true`, `strategy_errors=[]`, heartbeats under 5 s old. mexc and hyperliquid units disabled and inactive. Positions are this reading's Bybit holdings; neither funded alt realm holds any |
| Readiness boundary | Three failed liveness units at 15:30 UTC by direct host reading: demo, mainnet and host, all on the refused Telegram route (`PEER_ID_INVALID`, the owner's chat-side fix). Both funded alt realms are stopped and their watchdog timers disabled, so they do not page; `trade-notify` is also failed on the same route |
| Execution | Embedded callbacks; every engine unit uses `Type=notify`, `WatchdogSec=30s`. Host swap is 0 at 23:35. LONG uses 30-second PostOnly entry work in every realm; terminal order lookup and exact fill recovery precede an IOC remainder |
| Demo soak | All 31 observations pass from 08:46:49.845 through 08:51:49.846 UTC, reaching 300 seconds before mainnet handover. `deploy-ok` is recorded at 08:53:21.885 UTC. The soak the generation before it refused at 151 s is incident `demo-0922e9f30da3bf98` in CHANGELOG |
| Disk and WAL | 28.0 GiB writable free on `/` (117 GiB) after the reclaimer's first timer run, 15:41:00–15:45:59 UTC: 34 staged tarballs (0.43 GiB) and the apt cache (0.40 GiB) gone, 11 quarantine files (1.94 GiB) uploaded to Drive `engine-state/sealed/` and verified before deletion, 5.4 GB of quarantine still pending at 2 GiB a run; every sealed WAL segment below the floors verified against `latest/` (demo 52 of 81, mainnet 51 of 80; floors 54 and 53 from a retained callback cursor in each newest restatement), none reclaimed because free (28.0 GiB) is above the low-water mark (20.0 GiB; high water 30.0 GiB). The run exited 1 on the hyperliquid family, which has no log yet, so no `receipts/storage-reclaim.last-success` exists; the fix (a family with no log is nothing to reclaim) is in CHANGELOG. `scripts/ops.sh storage` reads the status; live WAL is pruned only there, below the floor, after a verified `sealed/` copy |
| Retained Stage A release | All three Stage A binaries remain at `/opt/liquidity-migration-engine/releases/8c92c96464bfe66662891c05f53a9510eefe8ba2/` with its staged archive; the reclaimer's keep set names that commit, the deployed and the previous commit, so release pruning never touches them. This historical reader is not requalified against current runtime-reconfiguration records. Retention does not authorize rollback across changed runtime inputs |
| Source qualification | Deployed source passes 2,079 developer Rust / 2,081 hosted Rust / 1,744 Python tests, zero failures, eight Rust ignores and one root/systemd-only Python skip. Rust 1.90 formatting and strict Clippy, Ruff, ShellCheck and mypy pass. Daily holding, compatible state recovery, history timeout and crossed-stop regressions fail before their fixes and pass afterward; the Linux release archive verifies |
| Last latency qualification | [Run `34128439094`](https://github.com/rob435/liquidity-migration/actions/runs/34128439094) is bound to `70f4c557`: 1,981 release tests, account/history workloads and all eight latency cells pass. Candidate median decision p99 / submit p50 is 2,000 / 721,150 ns, within absolute and paired limits. Its separate qualified archive verifies. Tail delays and all prior misses remain in [execution.md](docs/execution.md); no new latency qualification is claimed for `cecff2e2` |
| Copied-data qualification | The compatible Stage A reader passes all 27 original/converted base pairs / 54 boots, complete rotation state and ordered venue effects. Stage B passes all 27 converted-only boots, nine queued/nine prepared retrievals, both known filled archive queries and all 154 input length/mtime checks. Real source-frontier coverage is zero; future fills are absent and transport, risk and collateral are mocked. Original inputs remain unchanged. A complete matched production-day replay remains unqualified |
| Legacy recovery boundary | Ordinary admission is exact-only; archived scalar lineage, provenance-driven grid adoption, simulated binary64 fills and native protective repair remain required. The zero-hit/module-removal criterion remains unmet; see [quantity contracts](docs/engine.md) and [retained recovery](docs/operations.md) |
| Historical adapters | Recorder adapter and normalized Bybit/CSV/Parquet inputs use the same Rust core. Book, trade and bar execution assumptions are separate; [schemas and commands](docs/data.md#historical-adapters-and-execution). These research changes require no unrelated funded-runtime deployment |
| Production-day accounting | 2026-09-06 USDT linear window: all 38 demo / 77 mainnet trade fills and 28/29 funding executions match copied WAL/private cash records. Net transaction cash changes are `159.55178845` / `10.56366464` USDT. Order snapshots match cumulative fills/fees for 109/49 engine requests; one historical XCN request/terminal difference remains explicit. No independent midnight position/balance pair or complete chronological lifecycle/public-data reproduction is established; [exact gaps](docs/operations.md#observed-production-day-reconstruction) |
| Current-source measurements | Frozen `80db33df` baseline covers sustained traffic, burst backlog, partial filled-history throughput and delayed replies. A 2M-operation/100K-history offline soak completes; the fixed WAL-reader comparison falls from 5.656 s / 9.41 MB peak Python allocation to 0.103 s / 4.70 MB. Adverse cells, identities and scope remain in [execution.md](docs/execution.md#sustained-workload--reader-measurements); this does not requalify deployed network latency |
| Terminal retention boundary | Three zero-filled cancelled demo requests still lack exact terms in live segment `000063` at the 2026-09-07 21:04:59 UTC read: `eng-1788685989000-{8,9,10}`, retained since `1788702340615` ms. Natural expiry requires both wall time and complete execution history strictly beyond 2026-09-13 13:47:40.615 UTC; neither condition is met. Expiry alone does not retire archived lineage, legacy inventory or protective repair |
| Equity recorder | Rust `engine-tools record-equity` remains scheduled every minute; its 16:12:22 UTC job completes successfully after handover. The demo and mainnet heartbeats identify `21bd9227` at 21:07 UTC |
| Execution study | [Contract and commands](docs/execution.md#2-one-sided-execution-study); fifteen-minute timer active. Both LONG realms use the 30-second PostOnly policy. The [annual review](docs/research/annual-execution-2026-09-08.md) retains all queue/latency cells and the selected sample’s limits; realized savings from the new policy are not established |
| Observed fees / model boundary | The 48 h sample ending 07:47:54 UTC has 54 trade fills matching authenticated Bybit execution and transaction records. Actual cost is 10.3433 bp fees + 4.0142 bp slippage = 14.3575 bp per executed side, with 100% cost coverage. Most selected symbols charge 10 / 3.6 bp taker / maker; CAP and HEMI charge 11 / 4 bp. Five usable LONG opening comparisons shape the demo choice; the ARB crossing error of −21.615 bp at 5 ms and small selected sample preclude claiming proven savings |
| Backup / recorder recovery | The 16:02 backup completes at 16:05:36 UTC with 347 files; the independent remote comparison reports 347 matches and zero differences. The timer runs every 15 minutes with a 10-minute job budget. Both market recorders have fresh, unblocked status; cumulative prior disk-drop counters remain explicit in the raw receipts. NTP is synchronized; the 16:08 venue-clock probe reports +8.94 ms offset with ±11.10 ms uncertainty and no alert |

| Release image | SHA256 |
| --- | --- |
| Final engine, loaded in all three realms | `6b280e5119efc98ebf8675de1a00c2aab8394c9b11d68c36103114f6552d15df` |
| Final signal worker, loaded in all three realms | `713bb9cd9118b3f6a91d2457392ac06901b5059246b020f3faa826fc6ab027aa` |
| Final engine tools, installed | `25596636f1325adaded21ac53da87ff252c3830737f4d0af01888e4950f97549` |
| Final downloaded release archive | `b10e4f83b99dd6342855072958251a58f0669bc7a6c3e59b3f2f68b80e82e344` |
| Retained Stage A engine, retained | `9fc63d5344c9190cb700cef151b6af8c1082eecb64ee347395c1c01302f01ae6` |
| Retained Stage A signal worker, retained | `96fb34e84f174baa1acd931a35202f3d42009b3b8ac099e3916c4fabfa44caf8` |
| Retained Stage A engine tools, retained | `1794e243a263af0b77ec7cbc9639e8fc6aa997562356609e72398015b7d2b1a4` |
| Retained Stage A staged archive | `9765701173dabecb6258d216302bd58d80c420bbc81d53607e54a35d7c5bac2e` |

| Open position | Demo side / quantity / native stop | Mainnet side / quantity / native stop |
| --- | --- | --- |
| HEMIUSDT | long / 14131 / 0.007911 | long / 1161 / 0.007907 |
| INJUSDT | long / 59.5 / 5.804 | long / 4.6 / 5.805 |
| LINKUSDT | long / 53.9 / 11.997 | long / 4.2 / 11.971 |
| LTCUSDT | long / 21.4 / 49.18 | long / 1.7 / 49.14 |
| TAOUSDT | long / 3.031 / 231.82 | long / 0.248 / 232 |
| WLDUSDT | long / 852.7 / 0.4044 | long / 73.3 / 0.4055 |

| Sleeve ID | Mainnet | Demo | MEXC (installed, stopped by posture) | Hyperliquid (installed, stopped) | Repository authority |
| --- | --- | --- | --- | --- | --- |
| 0 | CARRY | CARRY | CARRY, entries off | CARRY, entries off | `configs/lane2_carry_hold_v7.json` |
| 1 | LONG | LONG | LONG, entries on | LONG, entries on | `configs/long_native_v12.json` |
| 2 | EXODUS | EXODUS | EXODUS, entries off | EXODUS, entries off | `configs/lane2_exodus_short_v1.json` |
| 3 | MAKER, quoting disabled | PROBE, enabled | none | none | `deploy/engine.mainnet.toml.template`, `deploy/engine.demo.toml.template`, `deploy/engine.mexc.toml.template`, `deploy/engine.hyperliquid.toml.template` |

| Setting | Repository authority |
| --- | --- |
| Capital, leverage, exposure and rolling loss | `configs/operational.json` |
| Native configuration | Rust `render-native-config`; generated template regions |
| Account identity and permission | Root-owned realm env/config on the host; authenticated account reader |
| Signal delivery | Durable spool plus `stream.sock` notification; worker owns unpublished/retained prefixes |
| Units and activation | `deploy/fleet_manifest.tsv`; [systemd inventory](deploy/systemd/README.md) |
| Equity and telemetry | Equity-recorder timer, one-minute sampling; [observability](docs/observability.md) |
| History | [CHANGELOG.md](CHANGELOG.md); dated archived entries retain prior incidents and deployments |

## Invariants

- Must distinguish a dated host observation from local source and test results.
- Must replace this snapshot after verifying an actual deployment; commit success alone does not update it.
- Must preserve append-only sleeve IDs, exact ownership and native protection.
- Must verify WAL/worker compatibility before selecting a previous binary.

## Operational Recipes

```sh
scripts/ops.sh status
scripts/ops.sh execution-study
scripts/ops.sh --help
```

[Operations](docs/operations.md) · [Engine](docs/engine.md) · [Data](docs/data.md) · [Trading rules](docs/trading_logic.md)
