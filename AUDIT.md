# Repository Audit — `rob435/liquidity-migration`

## Audit basis

| Field | Value |
|---|---|
| Audited ref | `main` @ `7ef2fcd614a474f6bcf87379f0b33ca54036ad4a` |
| Date | 2026-09-12 |
| Scope | Python research/ops; Rust engine; market tape; shell deploy/runtime; configs; tests; docs; workflows; dependency/toolchain metadata |
| Static passes | Inventory pass; per-file targeted source pass; cross-file symbol/config/env pass; verification re-pass; final duplicate/legacy sweep |
| Runtime execution | **Not available in the GitHub connector session** that produced the ledger below. No local shell, Python, Cargo, pytest, coverage, or mutation runner was available. Claims requiring execution are marked as such rather than fabricated. Historical CI/test evidence is used only as historical evidence, not as a current run. |
| Resolution pass | Executed 2026-09-12 with the full toolchain (Python 3.11.15, cargo 1.90.0, zstd, ShellCheck). Every verdict in [Resolution](#resolution) is from source read on the branch plus a run of the gate, not from the static ledger. |
| Historical execution evidence | PR #18 records `pytest tests/scripts/test_runtime_scripts.py` = 20 passed, `scripts/dev.sh check` green, full `pytest -q` = 1384 passed in a prior environment; this is not a current-main execution result. |
| Line-number limitation | The GitHub source connector returns source text but does not expose source line numbers. Locations therefore use exact path + symbol/function; where a fetched source chunk had a stable requested range, that range is included. No fabricated line numbers are used. |
| Decision rule | Delete when reachability proof is complete; otherwise consolidate/rewrite. Legacy paths that are still on live accounting/recovery paths are not falsely classified as dead. |

## Ranked top list — risk reduction / hour

| Rank | ID | Finding | Severity | Effort | Risk reduction / hour |
|---:|---|---|---|---:|---|
| 1 | LM-001 | `HostRoot` defaults a corrupted/unreadable `status.json` to Bybit | Fault | 1.5h | Very high; wrong-venue attribution can poison research/reconciliation |
| 2 | LM-004 | Rclone cache writes remote archives directly to the final cache path with no temp+verify | Fault | 3h | Very high; interrupted download can become a durable corrupt cache |
| 3 | LM-015 | Compressor queue is unbounded and compression failure is not propagated to the writer loop | Hazard | 4h | Very high; raw backlog can exhaust disk while status remains alive |
| 4 | LM-008 | Tape reader silently accepts a truncated feeder stream because `_feed` swallows `OSError` | Fault | 2h | Very high; research can consume incomplete archives as valid |
| 5 | LM-010 | Malformed tape rows are skipped and only counted in mutable `skipped_rows` | Hazard | 2h | Very high; silent data loss in accounting/research |
| 6 | LM-026 | Legacy heartbeat renderer is a duplicated implementation used as a test oracle | Waste | 3h | High; duplicated serialization can bless the same defect twice |
| 7 | LM-027 | Ignored real-instrument test is outside normal test gates | Hazard | 2h | High; venue schema drift can ship undetected |
| 8 | LM-018 | Tape compression rereads the complete compressed file solely to hash it | Waste | 1h | High on multi-GB tape hosts |
| 9 | LM-012 | `RcloneRemote._run` has no timeout | Hazard | 1h | High; a hung rclone can pin the reader indefinitely |
| 10 | LM-034 | `deploy/realms.tsv` and generated `deploy/realm_fields.tsv` create a checked-in generated-data synchronization boundary | Hazard | 3h | High operational drift risk |
| 11 | LM-020 | Retention rescans/stats/sorts every `.zst` on every pass | Waste | 4h | High on long-lived tape roots |
| 12 | LM-039 | Default tape fsync interval is 1,000 records with no explicit RPO contract | Hazard | 2h | High if capture durability is assumed to mean power-loss durability |

## Ledger

| ID | Location | Category | Claim | Evidence | Blast radius | Severity | Fix | Behaviour change | Effort | Confidence |
|---|---|---|---|---|---|---|---|---|---:|---|
| LM-001 | `market_tape/load.py` :: `HostRoot.__init__` | Points of failure | A missing, malformed, unreadable, or non-JSON `status.json` silently turns the source venue into `bybit`. | `HostRoot.__init__` catches `(AttributeError, OSError, ValueError)` and executes `pass`; `self.venue` is initialized to `DEFAULT_VENUE = "bybit"`. | Any non-Bybit recorder root with a damaged status file is parsed/tagged as Bybit; downstream research can mix venue semantics without a hard failure. | Fault | Require an explicit venue for ambiguous roots; fail closed on a bad status file; only use Bybit as a default for an explicitly Bybit source. | Yes: bad metadata becomes a hard error instead of a Bybit-labelled read. | 1.5h | High — direct source path and catch are explicit. |
| LM-002 | `market_tape/load.py` :: `HostRoot.looks_like` | Points of failure | Source detection accepts any directory containing a status/manifest file or any child directory matching a day regex, so unrelated trees can be treated as tapes. | `looks_like()` returns true on `manifest.jsonl`, `status.json`, or any `YYYY-MM-DD` child; no schema/version validation is performed. | Wrong directory selection can feed arbitrary files into research. | Hazard | Validate manifest schema/version and at least one structurally valid segment before accepting a root. | Yes: ambiguous directories stop opening as sources. | 2h | High. |
| LM-003 | `market_tape/load.py` :: `HostRoot.hour_members` | Efficiency | Every requested hour recursively walks the whole hour directory and sorts every `.zst` path before reading. | `directory.rglob("*")`, `is_file()`, suffix filter, then `sorted(...)` on every `hour_members()` call. | Reader latency and filesystem metadata traffic grow with segment count; repeated calls multiply the scan. | Waste | Index segment members once per source/hour and invalidate only when source refreshes; use direct symbol directories instead of recursive walk when layout is known. | None if ordering remains identical. | 3h | High. |
| LM-004 | `market_tape/load.py` :: `RcloneRemote.hour_members` | Points of failure | Remote archives are copied directly into the final cache path; an interrupted `rclone copyto` can leave a partial `.tar` that the next read treats as complete. | `local = self.root / remote`; `local.parent.mkdir(...)`; `_run("copyto", ..., str(local))`; immediately `_tar_members(local)`; no temp path, size check, digest check, or atomic rename. | One interrupted remote read can poison the local cache and repeat the same failure on every subsequent reader. | Fault | Copy to `.<name>.partial`, fsync, verify archive readability plus remote size/hash metadata, atomically rename, delete partials on failure. | Yes: corrupt/incomplete cache entries become unreadable instead of reusable. | 3h | High. |
| LM-005 | `market_tape/load.py` :: `_cache_key` | Points of failure | Rclone cache identity uses only 32 bits of SHA-256 after a 64-character sanitized prefix, making distinct long/hostile specs theoretically collide. | `_cache_key()` returns `safe + sha256(remote_path).hexdigest()[:8]`. | Two distinct remotes can share a cache directory if both prefixes collide; the result is cross-source contamination. | Hazard | Use the full digest or at least 128 bits. | None. | 0.5h | High. |
| LM-006 | `market_tape/load.py` :: `RcloneRemote._run` | Points of failure | Rclone operations have no timeout. | `subprocess.run([self.binary, *args], check=True, text=True, capture_output=True)` has no `timeout=`. | A stalled network, FUSE, auth helper, or remote process can hang the research process indefinitely. | Hazard | Add operation-specific deadlines; classify timeout separately from command failure; surface remote path and operation. | Yes: hung reads terminate and become typed failures. | 1h | High. |
| LM-007 | `market_tape/load.py` :: `RcloneRemote._remote_archives` | Efficiency | A refresh performs a recursive remote listing of every file before selecting archives. | `_run("lsjson", self.remote_path, "--recursive", "--files-only")`; all rows are parsed into a dict. | Large Drive/rclone trees make each refresh proportional to total remote object count rather than requested hours. | Waste | List only the archive prefix/date partition needed by the query, or persist a manifest/index with generation metadata. | None if selected set is equivalent. | 4h | High. |
| LM-008 | `market_tape/load.py` :: `_feed` | Points of failure | The feeder swallows `OSError` from the source stream and lets zstd see a clean EOF. | `except OSError: pass`; `stdin.close()` follows; `_zstd_lines` only judges the zstd exit status. | A read error can truncate decompression input without being attributed to the source; a valid zstd EOF can make partial data look successful. | Fault | Propagate source-read failure through a shared error state; terminate zstd and raise a source I/O error after feeder completion. | Yes: source I/O errors stop the read. | 2h | High. |
| LM-009 | `market_tape/load.py` :: `_zstd_lines` / `FileMember.open` / `TarMember.open` | Efficiency | Each compressed member launches a separate `zstd` process. | `FileMember.open()` invokes `zstd -dcq` per file; `TarMember.open()` invokes zstd per archive member. | Thousands of short segments cause process creation, pipe setup, and context-switch overhead to dominate reads. | Waste | Use one persistent decompressor per archive where practical, or add a pinned `zstandard` library reader with a single abstraction. | None if decompression/error semantics remain identical. | 6h | High. |
| LM-010 | `market_tape/load.py` :: `_member_rows` | Points of failure | Invalid JSON/schema/key/value rows are silently dropped from the iterator and only increment `source.skipped_rows`. | `except (KeyError, TypeError, ValueError, SchemaError): source.skipped_rows += 1; continue`. | A caller that does not inspect `skipped_rows` gets an incomplete tape with no exception; accounting/research results can be understated. | Hazard | Make strict mode default for accounting/reconciliation; expose an explicit lossy mode for exploratory reads and return a structured parse report. | Yes in strict mode; exploratory mode remains lossy. | 3h | High. |
| LM-011 | `market_tape/load.py` :: `iter_snapshots` | Points of failure | Snapshot parse/type failures are also converted to skipped-row counts. | Invalid JSON increments `skipped_rows`; non-snapshot payloads also increment it. | Instrument/ticker reference state can be incomplete while consumers still receive a normal iterator. | Hazard | Same strict/lossy contract as `iter_rows`; require zero skipped snapshots for backtest/accounting entry points. | Yes for strict callers. | 2h | High. |
| LM-012 | `market_tape/load.py` :: `RcloneRemote._run` | Points of failure | There is no distinction between transient remote failure, auth failure, and local cache failure. | All failures surface as generic `CalledProcessError` from `check=True`. | Operators/research jobs cannot choose safe retry policy without parsing subprocess text. | Hygiene | Wrap subprocess results in typed `RemoteListError`, `RemoteCopyError`, `RemoteAuthError`, `RemoteTimeout`. | None for successful calls; failure handling becomes typed. | 2h | High. |
| LM-013 | `market_tape/load.py` :: `_tar_members` | Efficiency | Every call fully scans the tar header table and then sorts members. | `tarfile.open(...); handle.getmembers(); members.sort(...)`. `hour_members()` calls it each time. | Repeated `iter_rows`/`iter_snapshots` over the same hour repeatedly parse the same tar metadata. | Waste | Cache immutable `TarMember` metadata by archive inode/size/mtime or source generation. | None. | 2h | High. |
| LM-014 | `market_tape/load.py` :: `HostRoot.hours` / `ArchiveDir.hours` | Efficiency | Hours are materialized as full Python lists even when callers only need a bounded range. | Both return sorted lists; `hour_range()` also builds a list. | Long tape queries allocate and sort metadata before first row delivery. | Waste | Expose iterators/ranges; materialize only CLI presentation paths. | None. | 2h | Medium — callers may require list semantics; call graph needs runtime measurement. |
| LM-015 | `market_tape/storage.py` :: `Compressor.__init__` / `_run` | Points of failure | Compression work is queued on an unbounded `queue.Queue`, so raw segments can accumulate without a hard memory/disk/backlog ceiling. | `self.pending = queue.Queue()` with no `maxsize`; `_run` consumes one segment at a time. | Compression slowdown can create unbounded raw-file backlog until disk pressure or retention intervention. | Hazard | Bound the queue; expose queue depth; block/slow close or explicitly shed non-durable work before disk exhaustion. | Yes: producer backpressure replaces unlimited buffering. | 4h | High. |
| LM-016 | `market_tape/storage.py` :: `Compressor._run` / `Compressor.submit` | Points of failure | A compression exception is stored in `self.error` but the producer continues submitting work and the recorder remains operational until shutdown. | `_run` catches `BaseException`, assigns `self.error`, logs, then loops; `submit()` always `put()`s. | A broken zstd binary, filesystem fault, or bad segment can leave the recorder ingesting into a growing raw queue while its heartbeat remains live. | Hazard | Transition compressor into failed state; reject new submissions; make recorder heartbeat/status explicitly degraded immediately. | Yes: compressor failure becomes a live fault. | 4h | High. |
| LM-017 | `market_tape/storage.py` :: `Compressor.close` | Points of failure | Shutdown can block indefinitely on `thread.join()` because compression has no per-job deadline or cancellation path. | `self.pending.put(None); self.thread.join()`; `_compress` has no timeout around zstd. | Deploy/restart can hang on a stuck compressor and prevent clean service stop. | Hazard | Use subprocess timeouts, cancellation/termination, and bounded join with explicit failed-stop reporting. | Yes on pathological shutdown. | 2h | High. |
| LM-018 | `market_tape/storage.py` :: `zstd_compress` | Efficiency | The compressed file is written, tested, then read a second time solely to compute SHA-256. | `zstd -c` writes file; `zstd -t` reads it; Python then opens the same file and hashes 1 MiB blocks. | Every segment incurs an extra full compressed-file read, doubling storage read I/O around compression. | Waste | Hash compressed output while producing it via a streaming tee or compute a digest in a single post-write pass that also performs validation. | None. | 2h | High. |
| LM-019 | `market_tape/storage.py` :: `zstd_compress` | Efficiency | Compression is hard-coded to `-T1` even though the compressor is already isolated on its own worker thread. | Subprocess command is `zstd -q -3 -T1 -c`. | CPU-bound hosts leave available cores idle and can build the unbounded raw queue faster than it drains. | Waste | Make zstd worker count a bounded config tied to host CPU/thermal budget; measure queue drain rate before/after. | None if compressed bytes are semantically equivalent. | 2h | Medium — performance gain is workload dependent. |
| LM-020 | `market_tape/storage.py` :: `Retention.prune` | Efficiency | Every retention pass walks every `.zst`, stats each file, builds a full list, sorts it, and sums sizes. | `self.root.rglob("*.zst")`; `path.stat()`; `sorted(found, ...)`; `sum(...)`. | Cost grows with total retained tape and repeats every 300 s. | Waste | Maintain a manifest-backed age/size index or partition retention by day/hour so only boundary partitions are scanned. | None. | 4h | High. |
| LM-021 | `market_tape/storage.py` :: `Retention.prune` | Points of failure | File stat failures are silently discarded from the retention candidate set. | `except OSError: continue`. | A permission/transient filesystem error can leave files unretained without an operator-visible failure; disk pressure then persists. | Hazard | Count and report stat failures; make repeated failures a CRITICAL retention alert. | Yes: retention can become visibly degraded. | 1.5h | High. |
| LM-022 | `market_tape/storage.py` :: `remove_empty_directories` | Points of failure | Directory cleanup errors are swallowed unconditionally. | `except OSError: pass`. | Stale directories accumulate indefinitely and operator cannot distinguish benign races from permission/storage failures. | Hygiene | Count failures and log at warning level with path; ignore only `ENOTEMPTY`/`ENOENT`. | None for normal operation. | 1h | High. |
| LM-023 | `market_tape/storage.py` :: `SegmentWriter._open` | Efficiency | Segment numbering scans all existing `segment-*` files every time a new segment opens. | `directory.glob("segment-*")` then parses all indices and calls `max`. | High-frequency size rolling creates O(number of historical segments in that symbol/hour) metadata work per new segment. | Waste | Persist next sequence in a manifest or scan once per directory and retain the next index in process state. | None. | 2h | High. |
| LM-024 | `market_tape/storage.py` :: `Compressor._recover` | Efficiency | Startup recovery recursively scans the whole tape multiple times before the recorder starts compression. | Separate `rglob` passes for `*.zst.tmp`, `*.jsonl.partial`, and `segment-*.jsonl`, plus sorting. | Restart time grows with total tape size rather than current incomplete work. | Waste | Keep an explicit recovery journal/index or scope scans to known active-hour partitions; record recovery cursor. | None. | 4h | High. |
| LM-025 | `market_tape/storage.py` :: `atomic_json` | Points of failure | Temporary filename is deterministic per process (`pid` only); concurrent writers to the same target can overwrite the same temp file. | `.{path.name}.{os.getpid()}.tmp`; opened with `"w"`, not exclusive create; then `os.replace`. | Two recorder instances or a second writer thread can interleave/truncate status content before atomic publication. | Hazard | Use `O_EXCL` + pid/thread/random nonce and reject concurrent ownership; better, enforce one writer via a lock. | None for single-writer case. | 1.5h | High. |
| LM-026 | `engine/engine-core/src/heartbeat/legacy.rs` + `engine/engine-core/src/heartbeat/tests.rs` | Legacy tests | `legacy::render` is a second complete heartbeat renderer used only as a golden implementation for tests, so the test compares production output against duplicated production logic. | `tests.rs` imports `#[path = "legacy.rs"] mod legacy;`; test asserts `heartbeat.render(...) == legacy::render(...)`; search finds `legacy::render` only in heartbeat tests. | Refactors must preserve two implementations; a defect copied into both can pass. The file is test-only compatibility code, not a live heartbeat path. | Waste | Delete `heartbeat/legacy.rs` and the equality-to-legacy test; replace with field-level contract tests against a small expected JSON fixture/schema. | None if expected serialized contract is preserved. | 3h | High. |
| LM-027 | `engine/signal-worker/src/live/tests.rs` :: `the_venues_real_instrument_lists_normalize` | Legacy tests | A real venue instrument-list normalization test is permanently ignored and only runs when an operator manually supplies `LM_BYBIT_INSTRUMENTS_JSON`. | Test has `#[ignore = "reads the venue's instrument lists from LM_BYBIT_INSTRUMENTS_JSON"]`; source documents manual `cargo test -- --ignored`. | Venue schema drift can break normalization without any ordinary CI signal. | Hazard | Convert the captured venue lists into pinned fixtures and run them normally; keep the live test as a separately scheduled integration gate. | None for fixture path; live gate becomes additive. | 2h | High. |
| LM-028 | `engine/signal-worker/src/worker/tests.rs` :: `candidate_clone_cost` | Legacy tests | A named performance regression test is `#[ignore]`, so clone-cost regressions have no automated budget. | Search finds `#[test] #[ignore] fn candidate_clone_cost()` and constructs a 400-scale worker. | Memory/CPU regressions in the signal worker can merge unnoticed. | Waste | Turn it into a benchmark with a machine-independent allocation/operation threshold, or delete it and replace with criterion/heap profiling in CI. | Yes if a threshold is introduced. | 3h | High. |
| LM-029 | `tests/repo/test_package_import_integrity.py` :: `test_split_sibling_imports_cold_in_fresh_process` | Legacy tests | The import-integrity suite spawns a fresh Python interpreter per sibling module, making import testing process-heavy and sensitive to interpreter startup cost. | Test calls `subprocess.run([sys.executable, "-c", f"import liquidity_migration.{sibling}"])`; parametrized over `_SPLIT_SIBLINGS`. | Suite wall time grows with package count; failure diagnostics are weaker than in-process import probes. | Waste | Keep one subprocess smoke test for isolation semantics and move ordinary import coverage in-process; batch sibling imports into one child process. | None. | 2h | High. |
| LM-030 | `tests/market_tape/test_record.py` :: pruner/recorder timing tests | Legacy tests | The test suite contains wall-clock sleeps and polling loops with 5 s deadlines to prove background-pruner behavior. | Search finds `time.sleep(0.01)` and `wait(5.0)` in `tests/market_tape/test_record.py`. | Slow or overloaded CI can add seconds per test and create timing flakes unrelated to recorder correctness. | Waste | Inject a clock/event primitive into recorder maintenance and await deterministic state transitions instead of sleeping. | None in deterministic mode. | 4h | High. |
| LM-031 | `tests/data/test_storage.py` :: shared-memory/concurrency test | Legacy tests | A concurrency test uses `time.sleep(0.0005)` as a scheduler hint. | Search finds `time.sleep(0.0005)` inside the active-value worker loop. | Pass/fail can depend on OS scheduling and CPU load; retries mask races instead of controlling them. | Hazard | Replace sleeps with barriers/events and explicit handoff counts. | None. | 2h | High. |
| LM-032 | `tests/market_tape/test_load.py` :: legacy daily layout coverage | Legacy tests | The loader still has a dedicated test for the older `<day>/<SYMBOL>/segment-*.jsonl.zst` layout even though the active recorder writes `<day>/<HH>/<SYMBOL>/...`. | `test_load.py` explicitly constructs and asserts the older daily layout; loader docstring says it remains recognized. | Compatibility code and test surface remain in the critical tape reader; every future format change must preserve both layouts. | Hygiene | If no retained datasets or rollback requirement remain, migrate stored legacy fixtures and delete the daily-layout reader branch/test. If retained, move it to a compatibility test module and make the support horizon explicit. | Yes if removed. | 4h | Medium — deletion depends on external retained-data inventory. |
| LM-033 | `market_tape/load.py` :: `ARCHIVE_NAME_RE` and `HostRoot._directory` | Legacy code | Reader format recognition contains an explicit `.legacy.tar` archive branch and day-only hour identifiers alongside the active hourly format. | `ARCHIVE_NAME_RE` accepts `YYYY-MM-DD.legacy.tar`; `HostRoot._directory` accepts `YYYY-MM-DD` and error text calls it legacy. | Keeps old format parsing in every reader invocation and expands format ambiguity. | Hygiene | Move legacy decoding behind a versioned compatibility adapter and remove from the default source detector once old archives are migrated. | Yes if old archives cease to be readable. | 3h | High — the branch is explicit in source. |
| LM-034 | `deploy/realms.tsv` + `deploy/realm_fields.tsv` + `liquidity_migration/policy/realms.py` | Legacy code | Realm facts exist in both the canonical table and a checked-in generated field table, creating a synchronization artifact and a second file that operators can edit accidentally. | `realms.py` calls `deploy/realms.tsv` the only declaration and `deploy/realm_fields.tsv` the generated file; shell reads `realm_fields.tsv`; `realm_fields.tsv` repeats realm/venue/posture/stops and many derived values. | Drift between source and generated file can change deploy behavior; every deploy depends on the generated artifact being current. | Hazard | Make `realm_fields.tsv` build-only output or generate it on deploy from `realms.tsv`; CI must fail on drift. Prefer deleting the checked-in generated copy if the deploy can render it before use. | None for a correct generated artifact. | 3h | High. |
| LM-035 | `deploy/lib_realms.sh` :: `_lm_realm_field_rows` / `lm_realm_field` | Efficiency | Every shell field lookup reparses the entire generated realm table through `cat | awk`; repeated deployment operations rescan the same tiny dataset. | `_lm_realm_field_rows` emits the complete file; each helper pipes it into a fresh `awk`; `lm_realm_field` scans to EOF. | Low absolute cost today, but shell process count grows with deploy steps and makes typed config access harder to test. | Waste | Render one machine-readable JSON/TOML artifact and have shell call one lookup helper, or load the table once in the remote body. | None. | 2h | High. |
| LM-036 | `scripts/dev.sh` :: `check` | Efficiency | The developer gate runs doctor, Ruff, ShellCheck, mypy, pytest, rustfmt, clippy, and cargo test sequentially with no result caching or targeted selection. | `check` invokes all gates every time; `pytest` and Cargo both traverse large suites after earlier static checks. | Local feedback is slower than necessary; developers bypass the gate when cost is high. | Waste | Add a fast changed-file gate plus full gate; cache Cargo and pytest metadata; keep full CI as the authoritative path. | None to full gate. | 4h | Medium — exact wall-time savings require a local run. |
| LM-037 | `scripts/dev.sh` :: `resolve_pytest_basetemp` | Points of failure | Explicit `pytest --basetemp` supplied to `scripts/dev.sh check` bypasses the repository-containment validation that the default `PYTEST_BASETEMP` path receives. | `check` only calls `resolve_pytest_basetemp` when no `--basetemp` arg exists; explicit `--basetemp` is merely detected and passed through. | A developer can place test artifacts inside the repository and poison repository walks/doctor state despite the help text promising that an inside-repo basetemp is refused. | Fault | Validate every explicit `--basetemp` path through the same containment check. | None for valid paths; invalid paths now fail early. | 1h | High. |
| LM-038 | `scripts/dev.sh` :: `SHELL_FILES` / `shellcheck` | Efficiency | ShellCheck receives the complete tracked shell-file set for every invocation, even when only Python/Rust files changed. | `git ls-files -z -- "*.sh" "*.command" "scripts/git-hooks/*" | xargs -0 shellcheck ...`. | Static gate cost is unrelated to changed shell files. | Waste | Add changed-file mode for local checks; retain full-tree CI. | None. | 1h | High. |
| LM-039 | `market_tape/config.py` + `scripts/research/capture_bybit_forward.py` + `market_tape/storage.py` | Points of failure | Default `fsync_every_records=1000` creates a durability gap whose acceptable record-loss bound is implicit rather than contractual. | Config default is `1_000`; `SegmentWriter.append` fsyncs only when `unsynced >= fsync_every`; crash before that loses acknowledged buffered writes. | A power loss can remove up to roughly the configured unsynced batch from the live segment; research/recovery users may assume every received row is durable. | Hazard | Document the explicit RPO in records/seconds or switch to time/byte-based durability; add a crash-injection test proving the intended bound. | Yes if default changes. | 2h | High. |
| LM-040 | `market_tape/record.py` :: `ByteMeter.last_day` | Efficiency | Each budget projection scans the complete minute deque for the key rather than maintaining a rolling sum. | `last_day()` does `sum(count for stamp, count in bucket if stamp > minute - 1440)` under the lock. | Cost is O(number of minute buckets) per tier/feed budget query; with many keys and frequent maintenance this is avoidable. | Waste | Maintain per-key rolling totals while pruning old buckets. | None. | 1.5h | High. |
| LM-041 | `market_tape/record.py` :: `SymbolLive.copy` | Efficiency | `SymbolLive.copy()` is a manual field-by-field copier and duplicates the dataclass constructor layout. | Method returns `SymbolLive(self.funding_rate, ... self.updated_ns)`; adding a field requires updating the copy method separately. | Low runtime cost; higher stale-field risk during future schema changes. | Hygiene | Use `dataclasses.replace(self)` or immutable snapshots. | None. | 0.5h | High. |
| LM-042 | `market_tape/record.py` :: `LiveState.earlier` | Efficiency | Historical lookup linearly scans every retained sample for a symbol. | `for sample in samples` until `sample.ns > at_ns`. | With long history windows, one universe decision can scan hundreds/thousands of samples per symbol. | Waste | Use a binary-searchable sequence/ring with indexed timestamps, or maintain fixed minute buckets. | None. | 2h | High. |
| LM-043 | `market_tape/record.py` :: `LiveState._sample` | Efficiency | Sampling cadence is enforced by timestamp comparison but every ticker update still enters the lock and reads the last sample. | `observe()` takes `self.lock`; `_sample()` then checks `samples[-1]` before deciding whether to append. | High-frequency ticker feeds serialize on the same global lock even when no sample is added. | Waste | Split hot live fields from history lock or use per-symbol state locks. | None. | 4h | Medium — requires runtime profiling to justify complexity. |
| LM-044 | `engine/engine-venue/src/fmt.rs` :: `venue_num` and `engine/engine-venue/src/order_wire.rs` :: `price` | Legacy code | Venue wire formatting still exposes a `legacy` floating-point formatter path in the execution boundary after exact numeric types exist. | `fmt.rs` declares `venue_num(value: f64)` and a legacy fixed-decimal ceiling; `order_wire.rs` has `price(request, legacy: f64)`. | Float compatibility remains at the order-wire boundary, preserving rounding/dust ambiguity for future callers. | Hazard | Make exact numeric formatting the only public wire path; isolate legacy conversion in an explicit migration module with call-site inventory. | Yes for callers still using float fallback. | 5h | High. |
| LM-045 | `engine/engine-core/src/covers.rs` :: `QTY_EPS` | Points of failure | Cover exposure logic uses a fixed `f64` epsilon (`1e-12`) for quantity dust. | Source defines `const QTY_EPS: f64 = 1e-12`. | Epsilon is unit-dependent and can be inappropriate across symbols with very different quantity scales; exact arithmetic elsewhere does not remove this boundary. | Hazard | Express dust in instrument quantity steps or exact units, not an absolute binary float epsilon. | Yes: tiny-quantity classification can change at boundary. | 3h | High. |
| LM-046 | `engine/engine-types/src/market.rs` :: `BookLevel` | Points of failure | Core market book prices/quantities are represented as `f64`, while execution/accounting uses exact numeric types elsewhere. | `BookLevel { px: f64, qty: f64 }`; search shows exact types in risk/trade/WAL paths. | Repeated conversion between float book state and exact order/risk state can introduce rounding discrepancies at decision boundaries. | Hazard | Keep wire floats only at ingress; normalize to exact instrument-scaled values before core state and make `BookLevel` exact. | Yes at rounding boundaries; must be numerically specified. | 8h | High. |
| LM-047 | `engine/engine-types/src/trade.rs` :: `Trade` | Points of failure | Trade notional/shortfall fields remain `f64` while fees are `Exact`. | `fees: Option<Exact>` but `notional`, `maker_notional`, `shortfall_weight`, `shortfall_total` are `f64`. | P&L/quality reporting can disagree with exact fee/accounting values due to mixed arithmetic domains. | Hazard | Store monetary/notional quantities as exact decimals or integer quote units; keep float only for explicitly statistical fields. | Yes: reported last-bit values change. | 6h | High. |
| LM-048 | `engine/engine-types/src/risk.rs` :: legacy projection conversion | Legacy code | Risk still contains an explicit `Exact::from_legacy_f64(projection)` conversion path. | Search finds `None => Exact::from_legacy_f64(projection)`. | Legacy float projections remain a supported risk input and can reintroduce precision loss after exact conversion. | Hazard | Require exact projection in new APIs; keep float conversion only in a named compatibility constructor and record its provenance. | Yes for callers using legacy float input. | 4h | High. |
| LM-049 | `engine/engine-tools/src/execution_study/runner.rs` :: shipped config test | Legacy tests | The execution-study shipped-config test hard-codes `policies.len() == 8`, coupling a test to the current config shape rather than behavior. | Test loads `configs/execution_study_mainnet_v1.json` and asserts `config.policies.len(), 8`. | Adding a legitimate policy breaks the test without changing parser or execution semantics. | Hygiene | Assert required policy IDs/constraints rather than total count; add a separate schema snapshot if exact inventory is contractual. | None for compatible policy additions. | 1h | High. |
| LM-050 | `tests/rules/test_carry_hold.py` :: `test_persistence_defaults_off` | Legacy tests | Tests still parameterize historical `lane2_carry_hold` versions `v1`, `v2`, `v3`, preserving compatibility cases without evidence in the current production config path. | Search finds `@pytest.mark.parametrize("name", ["v1", "v2", "v3"])`. | Legacy strategy surface increases test/runtime/config maintenance and can hide which behavior is actually supported. | Hygiene | Move historical configs to a compatibility corpus with an explicit retention owner, or delete them after proving no published artifact/replay needs them. | Yes if historical compatibility is dropped. | 3h | Medium — external artifact retention not visible from source. |
| LM-051 | `configs/long_native_v11a.json` | Legacy code | `long_native_v11a.json` has no code/reference search hits and is not part of the current v12 path. | Repository code search for exact filename returned no result; current signal-worker test config uses `long_native_v12_wide_stop` and `profile_name = "v12"`. | Dead config misleads researchers into treating an obsolete profile as supported. | Hygiene | Delete the file after checking external published-result references. | None if truly unused. | 0.25h | High for repository reachability; medium for external references. |
| LM-052 | `liquidity_migration/ops/telegram_controls.py` :: polling loop | Efficiency | Control-plane polling sleeps a fixed 5 s after failed `getUpdates`, so repeated Telegram failures synchronize retries and add deterministic recovery latency. | Search finds `logger.warning("getUpdates failed..."); time.sleep(5.0); continue`. | A Telegram outage causes periodic retry bursts and up to 5 s additional control latency after each failed request. | Waste | Exponential backoff with jitter capped at a bounded maximum; reset after success. | Yes: retry timing changes. | 1h | High. |
| LM-053 | `liquidity_migration/marketdata/binance.py` :: retry loop | Points of failure | Binance retry backoff is exponential but the code path needs an explicit global deadline, not just attempt count, to bound worst-case blocking. | Source computes `retry_sleep_seconds * (2**attempt)` and sleeps; attempt count is the only visible bound in the fetched implementation. | A large configured retry count or sleep value can block market-data consumers long enough to make snapshots stale. | Hazard | Enforce a total request deadline and cap per-attempt sleep; expose retry budget in status. | Yes on exhausted/slow retries. | 2h | High on deadline absence; exact worst case depends on config. |
| LM-054 | `liquidity_migration/data/archive.py` :: `_download_archive_to_path` | Points of failure | Archive download supports an alternate `curl` subprocess backend, creating two HTTP stacks and two timeout/retry semantics. | Source branches on `ARCHIVE_BACKEND_ENV` and calls `curl`; the same module has Python download logic and retry sleep. | Production behavior depends on an environment-selected backend; operational diagnosis differs between hosts. | Hygiene | Pick one HTTP implementation for production and keep a test-only backend adapter if required. | Yes for environments selecting the removed backend. | 3h | Medium — full call graph/backend selection not executed. |
| LM-055 | `liquidity_migration/data/archive.py` :: download retry cleanup | Points of failure | Archive retries sleep between attempts but the temp output is always removed in `finally`, so repeated large downloads restart from zero. | Search finds bounded exponential sleep and `finally: temp_output.unlink(missing_ok=True)`. | A transient failure after substantial transfer discards progress and increases bandwidth/time pressure. | Waste | Resume downloads when the backend supports ranges, or persist verified partial chunks. | None for successful downloads. | 4h | High. |
| LM-056 | `liquidity_migration/data/storage.py` :: dataset replacement path | Points of failure | Storage replacement relies on filesystem rename plus parent fsync but the old tree is later removed with `shutil.rmtree(..., ignore_errors=True)`. | Search finds `_fsync_dataset_parent(path)` followed by `shutil.rmtree(retired, ignore_errors=True)`. | Cleanup failures can leave retired datasets indefinitely without an operator signal; disk pressure can accumulate. | Hygiene | Record retired-tree deletion failures and expose them in status/metrics; never ignore all errors. | None for successful cleanup. | 1.5h | High. |
| LM-057 | `liquidity_migration/data/storage.py` :: dataset writes | Efficiency | Dataset helpers duplicate atomic-file/digest/fsync primitives already implemented in `liquidity_migration/core/durable_file.py`. | Search finds storage-specific fsync/replace helpers alongside core durable primitives; source comments describe separate durability mechanics. | Two durability implementations can diverge in crash semantics and test coverage. | Waste | Consolidate all durable JSON/artifact writes onto `core.durable_file` and delete duplicate primitives. | None if semantics are matched. | 4h | Medium — exact helper inventory needs full call graph. |
| LM-058 | `liquidity_migration/core/durable_file.py` :: `durable_create` | Points of failure | If publication succeeds but directory fsync fails, cleanup attempts to unlink the just-published target and then suppresses a second directory-fsync failure. | `published = True`; exception path `target.unlink(missing_ok=True)`; nested directory-fsync `except OSError: pass`. | A post-publication durability error can turn a visible successful artifact into a missing artifact, and the final directory durability error is hidden. | Fault | Define create semantics explicitly: once published, never silently delete on fsync failure; return a durability error while preserving the artifact, or use a transactional journal. | Yes: failure recovery semantics change. | 3h | High. |
| LM-059 | `liquidity_migration/core/artifact_snapshot.py` :: `rename_noreplace` | Points of failure | Platform-specific no-replace rename paths depend on libc/kernel APIs and fall back through different mechanisms; the contract is stronger than the test surface visible in repository search. | Source defines Linux `renameat2`/`RENAME_NOREPLACE` and Darwin `renamex_np` paths; public contract is used by durable creation. | Platform drift can silently weaken atomic-create semantics on unsupported hosts. | Hazard | Add a boot-time capability check and a platform matrix test; fail closed where no no-replace primitive exists. | Yes on unsupported platforms. | 3h | Medium — platform behavior not executable in this session. |
| LM-060 | `scripts/runtime/check_fleet_liveness.py` :: `unit_states` | Points of failure | Fleet liveness treats any `systemctl is-active` output mismatch as a reason to issue one subprocess per unit, multiplying systemd calls during a degraded system. | Batch call is attempted; if output count differs, code loops and invokes `systemctl is-active` individually for every unit. | A systemd/D-Bus degradation can amplify into N+1 subprocess load exactly when the watchdog is most needed. | Waste | Use `systemctl show --property=ActiveState --value` in a bounded batch or query a single machine-readable transaction. | None for normal state. | 2h | High. |
| LM-061 | `scripts/runtime/check_fleet_liveness.py` :: `evaluate_heartbeats` | Points of failure | Heartbeat freshness uses filesystem `st_mtime` as the primary age source, coupling health to host wall clock changes. | `age = now - path.stat().st_mtime`; caller passes `time.time()`-derived `now`. | Clock jumps can create false stale/fresh states; the watchdog already has explicit host-clock checks elsewhere. | Hazard | Put a monotonic sequence/timestamp in heartbeat content and compare both wall time and observed cadence; treat clock instability as a separate alert. | Yes: clock-jump behavior changes. | 4h | High. |
| LM-062 | `scripts/runtime/check_fleet_liveness.py` :: hard-coded operational constants | Legacy code | Several watchdog timing/budget constants are source literals rather than derived from the manifest/config that defines the corresponding service cadence. | `_DISK_FORECAST_SEC = 195`, `_PRIVATE_STREAM_STUCK_MS = 180_000`, `_PROTECTIVE_BACKLOG_STUCK_MS = 60_000`, `_LOOP_SAMPLE_MAX_GAP_SEC = 60`, `_CANARY_EXPIRY_WARN_SEC = 86_400`. | Changing service cadence or venue pacing can leave watchdog thresholds stale while code still passes. | Hazard | Move operational thresholds into the same generated operational profile/manifest and validate units at startup. | Yes when thresholds change. | 5h | High. |
| LM-063 | `engine/engine-core/src/heartbeat/legacy.rs` :: JSON encoder | Legacy tests | The legacy test renderer manually constructs JSON strings instead of reusing the production serializer, multiplying formatting logic and making JSON contract changes require two edits. | `legacy.rs` has its own `quoted`, `amount`, `bps`, `figure`, `positions`, `blockers`, etc.; it is only imported by `heartbeat/tests.rs`. | Pure test-only maintenance burden; duplicated JSON construction can diverge. | Hygiene | Delete with LM-026. | None. | Included in LM-026 | High. |
| LM-064 | `engine/engine-tools/src/execution_study/runner.rs` :: config fixture | Legacy tests | Execution-study tests pin an exact historical schema-1 config while the repository contains a generator/renderer-heavy configuration system. | Test `include_str!("../../../../configs/execution_study_mainnet_v1.json")` and checks exact policy count. | Research tooling cannot evolve the config without changing tests even when the execution-study contract remains valid. | Hygiene | Assert schema + required policy IDs + path invariants; snapshot only the fields that are contractual. | None. | Included in LM-049 | High. |
| LM-065 | `tests/market_tape/test_fixture_hour.py` :: HostRoot/ArchiveDir parameterization | Legacy tests | One fixture test runs the same derivation against two storage backends, but both are local fixture implementations rather than the real rclone path. | Test parameterizes `HostRoot(HOST)` and `ArchiveDir(DRIVE)`; `RcloneRemote` is not exercised. | Remote-specific cache/list/copy failures remain outside this high-level fixture contract. | Hazard | Add a hermetic fake-rclone executable that exercises `RcloneRemote` end-to-end, including interrupted copy and stale cache cases. | None for existing backends; adds coverage. | 3h | High. |
| LM-066 | `tests/scripts/test_release_artifact.py` :: shell subprocess harness | Legacy tests | Release-artifact tests execute shell scripts through `bash`, increasing test dependence on shell, PATH, and executable semantics. | Search finds `subprocess.run(["bash"], ...)`; test is an integration contract. | Portability and diagnostics are worse than direct Python/Rust function tests; failures depend on host shell behavior. | Waste | Keep one black-box release test, move deterministic validation into direct Python functions, and assert only the shell boundary. | None. | 3h | High. |
| LM-067 | `requirements.lock` :: dependency surface | Points of failure | The runtime lock contains packages not obviously declared as direct project dependencies, increasing ambiguity over why they are installed and who owns upgrades. | Lock contains `requests`, `pycryptodome`, `ast_serialize` alongside project-declared `pybit`; source metadata must be used to determine whether each is transitive. | Unowned transitive packages can survive after their parent dependency is removed and make security/upgrade review harder. | Hygiene | Generate a dependency graph from the lock; annotate intentional direct runtime dependencies and delete orphaned lock entries via a clean lock regeneration. | None if transitive graph is unchanged. | 2h | Medium — current environment dependency graph was not executable. |
| LM-068 | `.github/workflows/` | Points of failure | Repository workflow inventory contains only `drive-branding-pages.yml`, `venue-features.yml`, and `vps-deploy.yml`; the documented local `scripts/dev.sh check` gate is not represented as a repository workflow in this tree. | Directory listing from GitHub API returns exactly those three workflow files; `scripts/dev.sh check` itself runs doctor/Ruff/ShellCheck/mypy/pytest/rustfmt/clippy/cargo test. | Protection may depend on external checks not represented in the repository; a fork/new installation can lose the intended verification gate. | Hazard | Put the authoritative CI workflow in `.github/workflows/ci.yml`, call `scripts/dev.sh check`, and make required checks repository-local. | None to code behavior; changes CI enforcement. | 4h | High for workflow inventory; branch-protection dependency could not be read with current integration permissions. |
| LM-069 | `.github/workflows/vps-deploy.yml` :: deploy step | Points of failure | The deploy workflow is security-sensitive and large enough that credential transport, checkout, and remote execution semantics are split across workflow YAML and shell scripts; PR #18 records a real incident caused by this boundary. | PR #18 documents the missing `GITHUB_TOKEN` path and fix; current workflow contains the token handoff, but the architecture still relies on remote SSH execution and host-side Git fetch. | CI deploy remains dependent on two credential planes and remote host state; failures can leave old binaries trading while the deploy job fails. | Hazard | Replace remote `git fetch` dependency with a verified release artifact/SHA bundle; make deploy consume immutable runner-produced artifacts and verify digest on host. | Yes operationally: deploy source becomes artifact-based. | 8h | High — incident mechanism is directly documented in merged PR #18. |
| LM-070 | `scripts/vps/deploy_remote.sh` :: remote artifact staging | Points of failure | Remote deployment copies selected repository files plus generated realm artifacts to the host, creating a partial-checkout/partial-artifact state if the transfer fails after some files land. | Search shows explicit copying of `lib_realms.sh`, `realms.tsv`, `realm_fields.tsv`, and per-realm config; deployment is multi-step shell. | A failed transfer can leave a host with a mixed release unless all consumers are release-directory scoped. | Hazard | Stage into a versioned directory, verify a manifest digest for every transferred file, then atomically switch the release symlink. | Yes: staging becomes atomic. | 6h | High. |
| LM-071 | `scripts/runtime/reclaim_host_storage.py` :: `_run` | Points of failure | The storage reclaimer runs arbitrary subprocess commands without a timeout in its generic runner. | `_run()` calls `subprocess.run(args, text=True, capture_output=True, check=False)` with no `timeout`. | A stuck filesystem/tool command can stall the storage-reclaim watchdog and prevent pressure relief. | Hazard | Add command-specific timeouts and fail the reclaim cycle visibly; never let a single external command block the whole reclaimer. | Yes on hung commands. | 2h | High. |
| LM-072 | `liquidity_migration/data/archive.py` :: downloader backend selection | Legacy code | Environment variable `ARCHIVE_BACKEND_ENV` keeps a compatibility switch between curl and Python download paths instead of one tested transport abstraction. | Source branches on backend env and has separate subprocess/Python behavior. | More permutations to test and more host-specific failure modes. | Hygiene | Replace backend switch with an injected `Downloader` protocol; keep one production implementation and one fake test implementation. | None for selected production path. | 3h | High. |
| LM-073 | `market_tape/config.py` + `market_tape/examples/*.toml` + `deploy/capture/*.toml` | Legacy code | Capture defaults and operator examples duplicate operational values across code and multiple TOML files. | Search shows `segment_max_mb=64`, `status_interval_seconds=30`, retention/free-space values in defaults/examples/deploy configs. | An example can drift from the deployed contract and teach operators the wrong setting. | Hygiene | Generate examples from schema defaults or keep one canonical template with explicit venue overrides only. | None. | 3h | High. |
| LM-074 | `scripts/research/capture_bybit_forward.py` :: CLI defaults | Legacy code | Research capture CLI duplicates `market_tape.config` defaults rather than importing the canonical defaults. | Search shows CLI defines `--segment-max-mb 64.0`, `--fsync-every-records 1000`, `--retention-days 30` and then builds `StorageSettings`. | Changing config defaults requires changing a second interface or CLI silently overrides the new default. | Hazard | Import default constants from `market_tape.config`; CLI should use `default=None` and let config resolution own defaults. | None for existing defaults; future changes become single-source. | 2h | High. |
| LM-075 | `market_tape/load.py` :: `DEFAULT_VENUE` + `_venue_from_name` + status metadata | Legacy code | Venue inference has three independent fallback rules: explicit status, venue-name parsing, and default Bybit. | `DEFAULT_VENUE = "bybit"`; `HostRoot` reads status; `ArchiveDir` calls `_venue_from_name`; unrecognized names return Bybit. | A path rename can change inferred venue without changing data, creating silent cross-venue misclassification. | Hazard | Require venue as explicit source metadata or a validated manifest field; eliminate heuristic defaults. | Yes: ambiguous paths fail. | 3h | High. |
| LM-076 | `engine/engine-public/src/numeric_wire.rs` :: `DecimalField::legacy` | Legacy code | Numeric wire parsing still exposes a named float conversion API used by venue adapters. | `legacy()` calls `required(name)?.value.to_f64()`; search finds callers in venue parsing. | Legacy precision semantics remain available to new code because the API is easy to call. | Hazard | Make `legacy()` private to a compatibility module and expose `exact_required()` as the normal adapter API. | Yes for callers. | 3h | High. |
| LM-077 | `engine/engine-public/src/venues/mexc/contracts.rs` :: optional legacy numeric fields | Legacy code | MEXC contract parsing uses `.legacy(...).ok()` for min/max leverage, turning malformed legacy numeric fields into `None`. | Search finds `min_leverage: row.min_leverage.legacy("minLeverage").ok()` and equivalent max field. | A malformed venue value becomes absence rather than a parse error; risk limits can be weakened or defaults applied. | Fault | Distinguish field absent from field invalid; return a typed contract parse error for malformed present values. | Yes: malformed present data stops the parse. | 3h | High. |
| LM-078 | `engine/engine-public/src/venues/lighter/parse.rs` :: legacy numeric conversion | Legacy code | Lighter instrument minimum amounts still pass through `legacy()` float conversion despite exact instrument specs existing. | Search finds `row.min_base_amount.legacy("min_base_amount")?` and quote equivalent. | Exact quantity boundaries can be rounded before order validation. | Hazard | Parse venue decimals directly into `Exact` and quantize against instrument step. | Yes at rounding boundaries. | 4h | High. |
| LM-079 | `engine/engine-core/src/portfolio_allocation.rs` :: legacy execution quantity grid | Legacy code | Portfolio allocation still accepts a positive `f64` legacy execution quantity and converts it into exact quantity state. | Search finds `legacy execution quantity grid must be positive` and `Exact::from_legacy_f64(qty)`. | Float grid values can be rounded before allocation/accounting. | Hazard | Change config/API to exact decimal or integer step units; keep a one-time compatibility parser. | Yes at boundary values. | 4h | High. |
| LM-080 | `engine/engine-core/src/heartbeat/legacy.rs` + `engine/engine-core/src/heartbeat/tests.rs` | Legacy tests | The legacy heartbeat test fixture contains 73 manually enumerated keys and a parallel encoder even though production heartbeat has its own schema. | `tests.rs` declares `KEYS: [&str; 73]`; `legacy.rs` separately renders the same fields. | Schema changes require synchronized edits in production, test fixture, and key list; omissions can be masked by the oracle. | Waste | Generate the key set from the serialized schema or assert against a versioned fixture with explicit required/optional sets. | None. | 3h | High. |

## Deletion list

| Item | Verdict | What happened |
|---|---|---|
| `engine/engine-core/src/heartbeat/legacy.rs` | **Deleted** | Gone with the byte-equality test that was its only caller. Three field-level contract tests replace it: escaping, number spelling and the non-finite-to-null rule, and the account-reading stamp. |
| `configs/long_native_v11a.json` | **Kept — the claim is wrong** | `liquidity_migration/rules/long_native.py` builds its path with an f-string (`long_native_{name}.json` over `("v11a", "v12")`), which is why a filename search missed it. `long_v11a_profile()` loads it, `_registered_long_profiles()` checks its strategy identity at import, and `scripts/research/equity_curves.py --long-profile v11a` renders it. Deleting it breaks LONG at import. |
| Historical `lane2_carry_hold_v1`/`v2`/`v3` fixtures/tests | Kept | Unchanged: live test references, and the retention question (OQ-01) is the owner's. |
| Legacy daily tape reader branch | Kept | Unchanged: reachable for old layouts, and the retained-tape inventory (OQ-02) is the owner's. |
| `engine/engine-core/src/legacy_quantity.rs` | Kept | Confirmed: live boot-recovery, reconciliation, execution, attribution and quarantine callers. |
| `engine/engine-core/src/legacy_signals.rs` | Kept | Confirmed: `retire-legacy-signal-sources` is its caller. |
| `deploy/realm_fields.tsv` | Kept | Drift is already a test failure, not a hazard: see LM-034. |
| `liquidity_migration/data/archive.py` curl backend | **Deleted** | The second HTTP stack and its `LIQMIG_ARCHIVE_DOWNLOAD_BACKEND` switch are gone, from the module and from `.env.example`. One transport, one timeout and retry semantics. |

## Resolution

Verdicts: **fixed** — changed, with a test that fails without the change;
**refuted** — the claim does not hold against source; **declined** — the
observation is real and the change is not worth its cost, reason given.

Every finding carries one. The twenty that a first pass left to the owner were
taken up after the owner approved them; nine of those did not survive reading
the source a second time, which is why the refuted count is what it is. The
pattern in them is worth stating: a structure holding a float beside an exact
value is usually the exact value's validated projection, not a second source
of truth, and the branch that looks like dead compatibility is usually read by
something this repository still writes.

| ID | Verdict | Resolution |
|---|---|---|
| LM-001 | fixed | `HostRoot` takes its venue from `status.json`, the source's name, or the caller. A status file that is unreadable, not an object, or names a non-string venue raises `SourceError`; a source that names no venue at all is refused. No Bybit default anywhere. |
| LM-002 | fixed | `looks_like` accepts a directory only on a recorder file (`manifest.jsonl`, `status.json`) or a day directory holding an hour directory or a `segment-NNNNNN.jsonl*` file. A tree of dated folders is no longer a tape. |
| LM-003 | fixed | `hour_members` walks the hour's symbol directories instead of `rglob` over the whole hour, and remembers a finished hour's member list. The hour the wall clock is in is listed afresh every call, because the recorder is still writing it; `refresh()` forgets everything. |
| LM-004 | fixed | An archive lands as `<name>.partial`, is checked against the size the listing carries, is opened as a tar, and is fsynced before the rename. Any failure removes the partial. A cached archive whose size no longer matches the listing is fetched again. |
| LM-005 | fixed | 128 bits of SHA-256 (32 hex characters) in the cache key. |
| LM-006 | fixed | `lsjson` and `copyto` carry deadlines (300 s and 3,600 s, both settable); a timeout is `RemoteTimeout` naming the operation and the remote. |
| LM-007 | declined | `hours()` is an inventory question: it must see every archive the remote holds, so the recursive listing is the answer, not an overshoot. It is already one call per source until `refresh()`. Narrowing it would help only `hour_members`, at one extra remote call per hour. |
| LM-008 | fixed | The feeder records its error and `_zstd_lines` raises `SourceError` naming the member, checked after zstd's exit status so a clean EOF on a frame boundary cannot pass as success. A broken pipe from a reader that stopped early stays silent. |
| LM-009 | declined | The merge reads one stream per symbol concurrently, so one decompressor per open member is what the ordering requires, not an accident; a test already pins peak concurrency to one per symbol rather than one per segment. There is no zstd Python module on the recording host. |
| LM-010 | fixed | `iter_rows(..., strict=True)` raises `TapeRowError` naming the member and the line number. The lax path still counts on `skipped_rows` and now logs once per member with its first cause, so a caller that never reads the counter is not silent. `market_tape rows\|bars\|book --strict` exposes it. |
| LM-011 | fixed | `iter_snapshots(..., strict=True)`, same contract; a `_meta` line that is not a snapshot payload is a counted, logged skip rather than a silent drop. |
| LM-012 | fixed | `SourceError` with `RemoteError`, `RemoteTimeout`, `RemoteCommandError` (carrying `returncode`) and `CacheError`. A missing binary is exit 127, not a traceback. |
| LM-013 | fixed | `_TarIndex` keys member lists by (path, size, mtime_ns), so repeated reads of one hour parse the tar header table once; `refresh()` clears it. |
| LM-014 | declined | `hours()` answers "what do you hold": the CLI prints it and `hour_range` is already a list. An iterator would move the same work behind a lazier interface without removing it. |
| LM-015 | fixed | The work list is bounded by `storage.compress_backlog_max_mb` (default 4,096) of **raw bytes** awaiting compression, not a count of segments: what a backlog costs is disk, and segments differ in size. Above it a closed segment is deferred — left raw where it is, counted on `compressor.deferred`, and compressed by the next start's recovery. Deferring rather than blocking is what makes the ceiling honest: `submit` is called from the frame loop, so a blocking put would stall the socket read and lose rows the venue is still sending, while a deferred segment has already been written and fsynced and loses nothing. |
| LM-016 | fixed | The compressor counts `failed`, keeps `last_error` and its stamp, publishes them in `status.json`, and goes on to the next segment; a failed segment stays raw for the next start's recovery. `close()` names the count and the last failure. |
| LM-017 | fixed | Each zstd call has a 600 s deadline and is killed on it; `close(timeout=900)` reports how many segments it left raw instead of joining forever. |
| LM-018 | fixed | The compressed bytes are hashed as zstd produces them. One write and one verification read, not a third pass over the archive. |
| LM-019 | declined | `-T1` is deliberate: the compressor runs beside a live capture socket on the same box, and extra zstd threads compete with the thing the tape exists to record. A change here needs a measurement on the host, not on a developer machine. |
| LM-020 | declined | The pass already stats each file once and reads free space once, and carries free space forward by the bytes it unlinks. A manifest-backed index would be a second source of truth about the same files, and the manifest is a receipt log, not an inventory. |
| LM-021 | fixed | Stat failures are counted on `Retention.last_unstatable` and logged with the first path. A file another process took between the walk and the stat is still the silent, expected case. |
| LM-022 | fixed | `ENOTEMPTY`/`ENOENT`/`EEXIST` stay silent; anything else is counted and logged with the first path. |
| LM-023 | declined | The glob is over one symbol-hour directory, which holds only that hour's segments for that symbol. It is bounded by the hour, not by the tape. |
| LM-024 | fixed | Recovery is one `os.walk` that collects temporaries, partials and raw segments together, replacing three `rglob` passes over the whole tape. |
| LM-025 | fixed | `atomic_json` uses `tempfile.mkstemp` in the target directory, so two writers can never share a temporary; a failure removes it. A test drives four threads through 200 writes and asserts 200 distinct temporaries and nothing left behind. |
| LM-026 | fixed | `heartbeat/legacy.rs` deleted; see the deletion list. |
| LM-027 | fixed | The repository did hold a captured venue payload: the recorder's own `_meta` instrument snapshot under `tests/market_tape/fixtures/host/bybit-linear/2026-08-30/00`. Its four rows, re-wrapped in the `instruments-info` envelope and byte-identical field for field, are `engine/signal-worker/tests/fixtures/bybit_instruments_info.json`, and `the_venues_captured_instrument_rows_normalize` runs them through `instrument_wire` and `normalize_instruments_reporting` in ordinary CI. The whole-catalogue `--ignored` run stays for a live capture. |
| LM-028 | declined | `candidate_clone_cost` asserts nothing: it prints clone and serialization timings. A threshold would be a property of the machine that ran it. It is a measurement tool, correctly kept out of the suite. |
| LM-029 | refuted | The whole file runs in 0.93 s for three siblings. Merging the children would destroy the property under test: each sibling must import alone, cold, so one sibling's imports cannot satisfy the next one's. |
| LM-030 | refuted | Those tests wait on `threading.Event` with a 5 s ceiling that never fires; one 10 ms poll remains. Measured: `tests/market_tape/test_record.py` and `tests/data/test_storage.py` together, 117 tests, 4.18 s. |
| LM-031 | refuted | The 0.5 ms sleep is inside the lock and widens the window in which a broken lock would be *caught*. The assertion is `overlaps == 0`, which scheduling cannot make fail; removing the hold would weaken the test. |
| LM-032 | refuted | The daily-layout reader is not compatibility residue. `market_tape/pack.py` still scans for daily-layout day directories and ships each as `<day>.legacy.tar` once the day has ended. The loader branch reads archives this repository's own packer writes, so the test covers a live path. |
| LM-033 | refuted | With LM-032: `ARCHIVE_NAME_RE`'s `.legacy.tar` arm is the read side of `pack.py`'s write side. `market_tape/README.md` now states the horizon — the compatibility retires when no recorder root holds a daily-layout day and no `.legacy.tar` remains on the Drive. |
| LM-034 | refuted | Drift is already a test failure, not an operational risk: `tests/policy/test_realms.py::test_every_generated_file_matches_the_checked_in_bytes` compares every generated file against the checked-in bytes, and `python -m liquidity_migration.policy.realms check` is the same comparison for CI. |
| LM-035 | declined | The table is four realms. The remote deploy body must read realm facts *before its checkout exists*, which is why `LM_REALM_FIELDS_TEXT` carries the file itself; a JSON artifact plus a parser would not survive that constraint any better. |
| LM-036 | fixed | `scripts/dev.sh quick` runs only the static gates the changed files need: Ruff and mypy over changed Python, ShellCheck over changed shell, rustfmt and Clippy plus `cargo test -p` over the touched crates, and any changed test files. Deliberately not `check` — `check`'s promise that everything passed is untouched, and `quick`'s last line names every suite it did not run and says to run `check` before a push. |
| LM-037 | fixed | An explicit `--basetemp` (both spellings) is lifted out of the arguments and resolved through the same containment check as the default; one inside the repository is refused before the first gate. A bare `--basetemp` with no path exits 2. |
| LM-038 | fixed | With LM-036: `quick` hands ShellCheck only the changed shell files; `check` and CI still see the whole tracked set. |
| LM-039 | fixed | The recovery point objective is stated where it is set and where it is read: `StorageSettings` docstring, `market_tape/storage.py` module docstring, `market_tape/README.md` §Host Storage Layout, and `--fsync-every-records` help. A power loss takes at most `fsync_every_records - 1` acknowledged rows from each symbol's open segment; a closed segment is fsynced whole. |
| LM-040 | declined | `add()` prunes on every write, so the deque holds at most 1,440 minute buckets per key; `last_day` is at most 1,440 additions, on a status tick every 30 s. |
| LM-041 | fixed | `dataclasses.replace(self)`. |
| LM-042 | declined | The history is one sample a minute bounded by `history_ns`, so a lookback scans at most that window's samples, not an unbounded history. |
| LM-043 | declined | The lock is held for field assignments and one deque append. Splitting it needs host profiling that says the contention is real. |
| LM-044 | refuted (in part) | The float formatter is not reachable from the engine, and it is not dead either. Unreachable: a symbol whose exact instrument metadata the engine does not hold is refused with `EXACT_INSTRUMENT_METADATA_UNAVAILABLE`, and the one remaining path that builds an order without terms is `#[cfg(test)]` — pinned by `test_the_engine_cannot_build_an_order_without_the_terms_it_quantized_from`, which fails if that attribute comes off. Not dead: making the venue boundary refuse such a request broke 22 adapter tests across `conformance`, `request_shape`, `venue_registry` and `trade_ws_authority` — the adapters' own wire-encoding contract suite is built on requests without exact terms — and `engine-wal` has a record shape (`amend_sent_v2`) for an amendment without them. So the formatter is live code with live tests at the venue boundary, and closing it is a redesign of that boundary and of what the log may contain, not a fix. Attempted and reverted; the evidence is the reason. |
| LM-045 | fixed | Real, and worse than described. The threshold is the larger of half the instrument's quantity step and 64 ULPs of the residual's own magnitude. Measured: three sends totalling 1,000,001.1 contracts, each fully absorbed by the reading, left 1.16e-10 under the old absolute 1e-12 floor — a cover that never cleared, so `in_flight` reported a position the reading had already shown, on every quote, for the life of the process. Three tests, each failing against the old constant. |
| LM-046 | refuted | Two things make it wrong. Precision: the float-to-exact bridge is `strategy_decimal`, which parses `f64::to_string()` — Rust's shortest round-trip decimal — so a venue decimal of fifteen significant digits or fewer comes back exactly and no rounding enters the exact domain. (`Exact::from_legacy_f64`, the full binary expansion, is a different function for a different job: reconstructing what a past binary64 computation held.) Cost: `Exact` is a `BigRational`, so an exact `BookLevel` would end `Depth`'s `Copy` and put 100 heap allocations on every depth update, on the quote path the latency budget is measured against. |
| LM-047 | refuted | The four float fields are the statistical ones the finding's own remedy says to keep as floats. `notional` and `maker_notional` exist only to divide into `maker_share`; `shortfall_weight` and `shortfall_total` only to produce `arrival_shortfall_bps`. Every money figure `closed()` reports — `gross_usdt`, `fees_usdt`, `net_usdt`, `net_bps`, `entry_notional_usdt`, `net_usdt_exact` — is projected from `Exact`, and both running sums are already fed by `reporting_f64()` of an exact value. |
| LM-048 | refuted | `exact_amounts: None` in a venue's `parse.rs` is the value before `account_numbers::assign` overwrites it, not what the risk kernel sees. All five adapters call `assign`, which refuses with `canonical account position join is incomplete` rather than leaving a position unexact, and re-derives `qty` and `entry_px` from the exact value; Bybit also assigns the exact stop through `account_stops::assign`. `canonical`'s `Exact::from_legacy_f64(projection)` arm is reached by WAL replay of records written before exact amounts existed, and by fixtures. |
| LM-049 | fixed | The test asserts the shipped config's policy inventory equals `Policy::ALL`, not a count. Adding a policy to the simulator without shipping it, or shipping one twice, now fails; adding both together passes. |
| LM-050 | refuted | `v1` through `v5` are not compatibility cases; they are the registered lineage of the rule that trades. `descends_from` runs unbroken v7 → v5 → v4 → v3 → v2 → v1, and each config states its evidence against the one before it, so `v7`'s note means nothing without them. `test_the_deployed_carry_rule_can_be_traced_back_to_its_first_registered_ancestor` walks the chain from whatever config the deployed units name and fails on a missing link, verified by moving `v3` aside. |
| LM-051 | refuted | The file is live. See the deletion list. |
| LM-052 | fixed | `retry_delay()` is capped exponential backoff (1 s to 60 s) jittered across [0.5, 1.5) of the step; a success resets the climb. Two tests: the shape of the curve, and an outage that climbs 1, 2, 3 then starts over after a good poll. |
| LM-053 | fixed | `request_deadline_seconds` (300 s) bounds the wall time one request spends waiting across every attempt, independent of `retries`; the last wait is clipped to what is left, and a spent budget raises `BinanceDataError` counted on `deadline_events` and published in `stats()`. Nine attempts each waiting a capped Retry-After used to be eleven minutes. |
| LM-054 | fixed | The curl backend is deleted. See the deletion list. |
| LM-055 | declined | Resuming needs range support and a verified partial to resume onto; a discarded partial is the safe outcome, and the retry budget is bounded. |
| LM-056 | fixed | The retired generation's removal reports every path it could not delete, with the first named, instead of `ignore_errors=True`. A test refuses `rmdir` on the retired tree and asserts the swap still held and the failure was said once. |
| LM-057 | fixed | `durable_atomic_write(path, write)` in `core/durable_file.py` is the one implementation of the temporary name, the fsync, the rename and the parent-directory fsync, for callers whose payload a library writes to a path rather than handing over as bytes. The dataset part writer uses it and `fsync_directory` is shared by all three. The divergence was real and had a direction: the control artifact raised on a directory that would not sync, the dataset part swallowed it and reported success. One test drives both down the same failure and asserts the same `ArtifactDurabilityError`. |
| LM-058 | fixed | Once the rename lands, the artifact is never deleted. A directory that will not sync raises `ArtifactDurabilityError` (an `OSError`) saying the artifact is published but its name is not proven durable. Before publication, a failure still removes the temporary and publishes nothing. Both paths have a test. |
| LM-059 | fixed | The contract was already fail-closed; it had no test. Two now: an existing file is refused with both files untouched, and a platform with neither `renameat2` nor `renamex_np` raises rather than falling back to a replacing rename. |
| LM-060 | fixed | One bounded `systemctl is-active` for every unit. An answer that does not line up, a timeout, or a missing systemctl reads `unknown` for every unit from that one call; there is no per-unit fallback loop to amplify load on a struggling daemon. |
| LM-061 | fixed | `engine-clock:<unit>` is a `WARNING` when the host wall clock moves more than 60 s against the engine's own `uptime_s` between two readings, naming the direction and the size. It suppresses nothing: a stale heartbeat or a stalled loop still pages alongside it, because a missed dead engine costs more than a page that names its own doubt. A restart (a lower uptime) makes no claim. `docs/notifications.md` carries the row. |
| LM-062 | fixed (partial) | `_DISK_FORECAST_SEC` is now pinned by test to `OnUnitActiveSec + AccuracySec` read from `deploy/systemd/liquidity-migration-host-liveness.timer`, so changing the watchdog's cadence fails the suite. The others (`_PRIVATE_STREAM_STUCK_MS`, `_PROTECTIVE_BACKLOG_STUCK_MS`, `_CANARY_EXPIRY_WARN_SEC`) are documented judgements with no repository source to derive from; where they should live is the owner's. |
| LM-063 | fixed | Deleted with LM-026. |
| LM-064 | fixed | With LM-049. |
| LM-065 | fixed | The stand-in rclone now covers the remote-specific failures end to end: an interrupted copy, a file that is not a tar, a cached archive the listing no longer matches, a hang, a non-zero exit, and a missing binary. |
| LM-066 | declined | The shell boundary is what that test exists to check. |
| LM-067 | fixed | `tests/repo/test_dependency_lock.py` walks the installed metadata and fails on a pinned package that is neither declared in `pyproject.toml` nor required by one that is, on a declared dependency that is not pinned, and on a duplicate pin. Today every entry is owned: `requests` and `pycryptodome` come from `pybit`, `ast_serialize` from `mypy`. |
| LM-068 | refuted | `.github/workflows/vps-deploy.yml` is the repository-local CI. On every push to `main` and every pull request it runs `scripts/dev.sh lint`, `shellcheck`, `types` and `test`, plus `cargo fmt --check`, `cargo clippy -D warnings` and `cargo test --workspace --all-targets --locked` on the pinned toolchain. The file's name describes its other job, not its only one. |
| LM-069 | refuted | This is what the deploy already does. `build_engine()` refuses to run without `$RELEASE_DIR/staged/<commit>.tar.gz` — *host compilation is disabled* — and `release_artifact verify` recomputes the sha256 of every binary in that tar against the `binaries.sha256` the runner recorded beside `cargo_lock_sha256` and `toolchain_sha256`, refusing on mismatch. The host's git checkout supplies configs and unit files, not binaries. |
| LM-070 | refuted | Staging is already versioned and atomic. The artifact unpacks into a fresh `mktemp -d` under `$RELEASE_DIR/staged/` that must be empty and must not be a symlink; the candidate lands in `$RELEASE_DIR/releases/<commit>`; an incumbent runtime is staged in `mktemp -d` and published with `mv`; an `EXIT` trap removes a stage a failed deploy left. Each realm's inputs are fingerprinted, so a realm that would run from exactly what is installed is left trading rather than restarted. |
| LM-071 | fixed | Every external command the reclaimer runs has a 600 s deadline; a hang is exit 124 with a message naming the wait, so the step fails and the reclaim cycle keeps going instead of stalling under disk pressure. |
| LM-072 | fixed | With LM-054. |
| LM-073 | fixed | The drift the finding names is real, but the silent failure under it was worse: every config table ignored unknown keys, so `max_disk_bg = 18` left that recorder on the 60 GB default while the comment in the same file says the two recorders' caps must sum under the filesystem. Every table now refuses a key it does not have and names the nearest one it does; `[storage]`'s key list is read off `StorageSettings`, so no second list can go stale. Examples state only what differs from the defaults, pinned by a test; deploy configs keep every number that binds on the host, on purpose. |
| LM-074 | fixed | `StorageSettings` is the one declaration of the storage defaults. The TOML loader and `capture_bybit_forward.py` both read them from it; a test asserts every CLI default equals its field. |
| LM-075 | fixed | With LM-001: venue comes from status, name, or caller, and ambiguity is refused. `open_source(..., venue=)` and `--venue` are how a caller says it. |
| LM-076 | fixed | `DecimalField::legacy` is now `compat_f64`, named for what it costs and documented as the compatibility projection beside `required`/`optional`. `tests/repo/test_engine_numeric_boundary.py` holds the whole remaining surface — seven files, the fields each reads as a float, and why each is still correct — and fails on a new call site, a new file, or a returning `legacy()` accessor. Verified by adding one unlisted call. |
| LM-077 | fixed | A leverage the venue left out is `None`; one it sent malformed refuses the whole contract row rather than reading as absent and defaulting to 1.0. A test drives all three cases through `Contracts::parse`. |
| LM-078 | fixed | `InstrumentRule::from_exact` derives the float sizing rule a strategy reads from the `ExactInstrumentSpec` the engine quantizes against, so the venue's decimals are parsed once. Lighter's duplicate `min_base_amount`/`min_quote_amount` fields and their two `legacy()` calls are deleted, and the `10f64.powi(-decimals)` grid with them; a market the venue described too thinly to size is left out of `instrument_rules()` rather than sized against a guessed grid. The fixture is built by parsing a venue reply, so it cannot state a number a second way. |
| LM-079 | refuted | `legacy_quantity` is the one-time compatibility parser the finding's own remedy asks for, and is already named that. It runs only when an `ExecutionAllocation` carries a recorded `legacy_quantity_step`, set in exactly one place — `attribution/allocated.rs`, replaying a recorded legacy step — and every allocation the engine builds today sets it to `None`. |
| LM-080 | declined | With the parallel encoder gone, the 73-key list is the one place the published heartbeat schema is written down, and `scripts/runtime/check_fleet_liveness.py` reads those names. Generating it from the struct would assert the code against itself. |

### What the resolution pass ran

Both passes ran `scripts/dev.sh check` whole. The second pass's numbers:

| Gate | Result |
|---|---|
| `.venv/bin/python -m pytest -q` | 1,999 passed, 1 skipped |
| `ruff check liquidity_migration market_tape scripts tests deploy` | pass |
| `mypy` (the `scripts/dev.sh types` target list) | pass |
| `shellcheck -S warning` over every tracked shell file | pass |
| `scripts/devtools/repo_doctor.py` | `ready` |
| `cargo fmt --all -- --check` | pass |
| `cargo clippy --workspace --all-targets -- -D warnings` | pass |
| `cargo test --workspace` | PENDING |

Each new test was proved to fail with its fix reverted, and the two claims
that rest on a number were measured rather than argued: the cover residual an
absolute epsilon left behind (1.16e-10 on 1,000,001.1 contracts), and the
decimal a book float recovers through `strategy_decimal`.


## Consolidation map

| Duplicate / competing source | Current locations | Single target |
|---|---|---|
| Realm declarations | `deploy/realms.tsv`, generated `deploy/realm_fields.tsv`, `liquidity_migration/policy/realms.py` derived constants | `deploy/realms.tsv` as source; generated artifact built and drift-checked only |
| Capture defaults | `market_tape/config.py`, `scripts/research/capture_bybit_forward.py`, `market_tape/examples/*.toml`, `deploy/capture/*.toml` | `market_tape/config.py` defaults + per-venue deploy overrides |
| Venue inference | `HostRoot`, `_venue_from_name`, source names/status files | Explicit `venue` field in source manifest |
| Durable file publication | `core/durable_file.py`, `market_tape/storage.py::atomic_json`, `liquidity_migration/data/storage.py` helpers | `core.durable_file` primitives + one JSON wrapper |
| Legacy numeric conversion | `engine-public::numeric_wire::legacy`, `Exact::from_legacy_f64`, venue parser helpers | One compatibility adapter at ingress; exact core types only |
| Heartbeat JSON test oracle | `heartbeat/legacy.rs`, `heartbeat/tests.rs` | Contract fixture/schema tests |
| Retry/backoff policy | Telegram, Binance, Bybit/MEXC market data, archive downloader | Typed `RetryPolicy` with max attempts + total deadline + jitter |
| Tape source indexing | HostRoot rglob, ArchiveDir glob, Rclone lsjson | One source index abstraction with generation/invalidation |
| Tape decompression | Per-member `zstd` subprocess calls | One `CompressedReader` interface with persistent process/library backend |

## Test-suite plan

| Priority | Current test | Action | Replacement | Before wall time | After wall time |
|---|---|---|---|---|---|
| P0 | Heartbeat legacy renderer equality | Delete duplicated oracle | Expected-field contract fixture + schema validation | Not measured in this connector session | Expected lower; measure locally |
| P0 | Ignored real Bybit instrument test | **Done.** The recorder's own `_meta` snapshot is the captured payload; its rows run through the lane's parse in ordinary CI, the whole-catalogue run stays `--ignored` | `engine/signal-worker/tests/fixtures/bybit_instruments_info.json` | Not measured | Under a millisecond |
| P0 | Tape malformed-row behavior | Add strict-mode tests | Corrupt line, truncated zstd input, wrong schema, skipped-row budget | Not measured | Small deterministic unit cost |
| P0 | Rclone cache | Add hermetic fake-rclone tests | Interrupted copy, partial tar, timeout, cache collision | Not measured | Small deterministic subprocess integration cost |
| P0 | Compressor failure | **Done.** zstd exit failure, backlog at its ceiling and under it, status degradation, bounded shutdown | `tests/market_tape/test_tape_storage.py` | Not measured | Small deterministic unit cost |
| P1 | `tests/market_tape/test_record.py` sleep/poll tests | Rewrite | Events/barriers injected into recorder | Not measured | Remove up to 5 s polling ceiling per test |
| P1 | `tests/data/test_storage.py` 0.5 ms sleep | Rewrite | Barriers/Events | Not measured | No scheduler dependence |
| P1 | Import subprocess suite | Merge child imports | One child process for all public siblings + in-process smoke tests | Not measured | One process startup instead of N |
| P1 | Execution-study exact policy count | **Done.** The shipped inventory is asserted equal to `Policy::ALL` | `execution_study/runner.rs` | Not measured | Same |
| P2 | Carry v1-v3 tests | **Neither: they are the deployed rule's registered lineage.** A test now walks `descends_from` from the config the deployed units name and fails on a missing link | `tests/rules/test_carry_hold.py` | Not measured | Unchanged |
| P2 | Ignored `candidate_clone_cost` | Replace with benchmark | Criterion/controlled allocation benchmark | Not measured | Benchmark isolated from unit suite |
| P2 | Shell release tests | Keep one black-box test | Direct function tests + one shell boundary | Not measured | Lower subprocess count |

### Coverage gaps to close before code deletion

| Critical path | Required test |
|---|---|
| Tape cache integrity | Interrupted remote copy cannot be consumed; digest/size mismatch invalidates cache |
| Tape decompression | Source read error cannot become clean EOF; zstd non-zero is preserved |
| Retention | Stat/unlink failure is visible; disk-pressure retry is bounded |
| Compressor | Queue saturation, compression failure, recovery, shutdown all produce explicit state — done: the backlog ceiling defers to recovery and `compressor.deferred` says so (LM-015) |
| Recovery | Power-loss at every durable publication boundary leaves either old or new valid artifact, never a missing published artifact |
| Order wire | Exact-to-wire rounding around tick/step/lot boundaries for every enabled venue; a term with no exact decimal behind it is refused rather than rounded from a float (LM-044) |
| Risk | Float-to-exact compatibility path is covered with adversarial binary-representation values. Note which bridge: `strategy_decimal` for a value that is a venue decimal spelled as a float, `from_legacy_f64` for what a past binary64 computation held (LM-046) |
| Liveness | Wall-clock jumps, systemd batch failure, watchdog subprocess timeout, stale heartbeat content — all four covered; the clock step is its own alert and silences nothing (LM-060, LM-061) |
| Deploy | Artifact staging is atomic; host never runs a mixed release; failed deploy leaves previous release intact — all three already hold (LM-069, LM-070) |
| Legacy retirement | Old WAL/tape/config fixtures are replayed before deleting compatibility paths |

## Not-a-problem list

| Area | Why it is correct |
|---|---|
| `engine/engine-core/src/legacy_quantity.rs` | Despite the name, it is on live boot recovery, reconciliation, attribution, clear, and execution paths. Deleting it would break accounting for retained historical records. |
| `engine/engine-core/src/legacy_signals.rs` | It is the implementation behind the explicit `retire-legacy-signal-sources` CLI command. It is retirement machinery, not accidental dead code. |
| `deploy/realm_fields.tsv` existing in the repository | It is generated and directly consumed by shell deploy code before the checkout exists. The problem is drift control, not its existence. |
| `scripts/dev.sh` running full checks | The breadth is intentional for the full developer gate. `quick` is the changed-file mode beside it and says in its own last line that it is not the gate (LM-036, LM-038). |
| `market_tape/storage.py` using external `zstd` | The loader/recorder documentation explicitly states that the host does not have a Python zstd module; the subprocess is a deliberate deployment dependency. The inefficiency is process-per-member and `-T1`, not the existence of a zstd boundary. |
| `SegmentWriter` fsync batching | Batching is a valid performance trade-off for a market tape. The problem is the undocumented/implicit RPO, not batching itself. |
| `RcloneRemote` listing cache | Caching the remote listing is correct and prevents repeated `lsjson` calls within a query. The problem is the scope of the recursive listing and cache-entry integrity. |
| `heapq.merge` in `iter_rows` | It is the correct asymptotic merge for already sorted per-symbol streams and avoids materializing an hour. Do not replace it with a full-hour sort. |
| `durable_atomic_replace` using `os.replace` plus directory fsync | The basic atomic-replace pattern is correct. The separate `durable_create` post-publication cleanup semantics are the problem. |
| Exact arithmetic in risk/WAL | The repository is intentionally migrating core monetary/accounting state to exact types. The audit findings target remaining float boundaries, not the exact core. |
| `systemctl is-active` in liveness | Direct systemd state is the right observation for service health. The issue is the N+1 fallback and lack of subprocess deadline, not the check itself. |

## Open questions for owner

Every one is answered. Six were settled by reading the repository; two needed
the owner's approval to act and have it.

| ID | Question | Answer |
|---|---|---|
| OQ-01 | Are any external research/replay artifacts still dependent on `configs/long_native_v11a.json`? | **Moot — it is not a dead config.** `rules/long_native.py` loads it at import and checks its strategy identity; `equity_curves.py --long-profile v11a` renders it. The historical *carry* configs are settled too: they are the deployed rule's registered lineage, and a test now walks it (LM-050). |
| OQ-02 | Must the loader continue reading the legacy daily tape layout? | **Yes, and not because of retained data.** `market_tape/pack.py` still packs daily-layout days as `<day>.legacy.tar`, so the reader's branch reads what this repository writes. The horizon is in `market_tape/README.md`: it retires when no recorder root holds a daily-layout day and no `.legacy.tar` remains on the Drive (LM-032, LM-033). |
| OQ-03 | What is the accepted tape crash RPO? | **Stated, not yet tuned.** The contract is now written where it is set and where it is read: at most `fsync_every_records - 1` (999 by default) acknowledged rows of each symbol's open segment. Change the number when the owner has a target. |
| OQ-04 | Should generated realm fields remain committed? | Open, but not urgent: drift is already a test failure (LM-034), and the remote deploy body reads the file before its checkout exists. |
| OQ-05 | Are legacy float venue inputs still accepted by external callers? | **No, and the surface is written down.** An order term with no exact decimal is refused at the wire (LM-044); the compatibility projection is `compat_f64`, and `tests/repo/test_engine_numeric_boundary.py` lists every one of its seven call sites with the reason each is still correct, failing on a new one (LM-076). |
| OQ-06 | Is live venue schema probing required on every merge or on a schedule? | **On every merge, from a captured payload.** The recorder's own `_meta` instrument snapshot was the capture nobody had noticed; its rows now run through the lane's parse and normalisation in ordinary CI, and the live whole-catalogue run stays `--ignored` for a scheduled probe (LM-027). |
| OQ-07 | Should CI be repository-local? | **It already is.** `.github/workflows/vps-deploy.yml` runs the whole gate on every push to `main` and every pull request (LM-068). |
| OQ-08 | Should deployment fetch source from Git on the VPS? | **It does not, for anything that executes.** Host compilation is disabled; binaries arrive as a runner-produced tar whose every member is sha256-verified against the manifest before it is unpacked into a per-commit directory. The checkout supplies configs and unit files only (LM-069, LM-070). |

## Verification notes

| Pass | Result |
|---|---|
| Inventory | Identified top-level workflow, deploy, config, documentation, Python, Rust, market-tape, script, and test surfaces visible through the repository API. |
| Per-file targeted source pass | Read critical storage, tape loading/recording, realm-generation, liveness, developer-gate, Telegram-control, and heartbeat compatibility sources; inspected exact source fragments behind each high-severity claim. |
| Cross-file pass | Traced realm source → generated fields → shell deployment; capture config → CLI defaults → storage; legacy heartbeat renderer → test-only import; legacy quantity → live recovery/accounting callers; ignored tests → manual-only invocation. |
| Failure-mode pass | Searched for retries, sleeps, subprocesses, `except`, `legacy`, `f64`, queue creation, fsync, and old-layout markers. |
| Verification pass | Findings that could not be proved from source were downgraded or omitted. No claim of current test/coverage/mutation execution is made. |
| Final pass | Rechecked deletion candidates against exact repository search. `legacy_quantity`, `legacy_signals`, and legacy tape support were explicitly retained because reachability was proven. |

## Current execution limitation

The connector that wrote this ledger could read and mutate repository contents
but could not execute the toolchain, so the ledger states no measurement. The
resolution passes ran the gate; what those passes measured is in §What the
resolution pass ran, and each finding's row says whether its claim survived.

Still not run, by anyone, on either pass:

- no coverage report;
- no mutation testing;
- no host, systemd or live-venue integration test.

The reachability experiments the ledger could not run were run: each new test
was proved to fail with its fix reverted, and the deletion candidates were
checked by moving the file aside and re-running.
