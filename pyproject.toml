[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "liquidity-migration"
version = "0.1.0"
description = "PIT data, research, evidence, and operations tooling for the Rust trading engine."
requires-python = ">=3.11"
dependencies = [
    "certifi>=2024.0.0",
    "matplotlib>=3.8,<4",
    "numpy>=2.1,<3.0",
    "Pillow>=10.0,<13",
    "polars>=1.0,<2",
    "pyarrow>=15.0,<26",
    "pybit>=5.8,<6",
    "PyYAML>=6.0,<7",
    "websocket-client>=1.8,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<10.0",
    "ruff>=0.6,<1.0",
    "mypy>=1.11,<3.0",
    "types-PyYAML>=6.0.12,<7.0",
]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]

# Static type checking is incremental. Run package-wide mypy before making a
# package-wide claim; any narrower passing scope must be named explicitly.
[tool.mypy]
python_version = "3.11"
files = ["liquidity_migration"]
ignore_missing_imports = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true

# Polars aggregate scalar stubs overapproximate the values used in these paths.
[[tool.mypy.overrides]]
module = [
    "liquidity_migration.data.trade_lifecycle",
]
disable_error_code = ["arg-type"]

[tool.setuptools.packages.find]
include = ["liquidity_migration*"]
